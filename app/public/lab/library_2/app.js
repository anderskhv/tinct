import {books} from './books.js';
import {categories,eras,metadata} from './taxonomy.js';
import {clamp,ease,mix,destination,bookFrame,orbFrame,dockPosition,sceneCrop,panelBounds} from './motion.js';
const $=id=>document.getElementById(id), all=s=>[...document.querySelectorAll(s)];
const reduced=matchMedia('(prefers-reduced-motion: reduce)');
const iconPaths={readArrow:'<path d="M2 12h20m-6-6 6 6-6 6"/>',search:'<circle cx="10.8" cy="10.8" r="7.3"/><path d="m16 16 5 5"/>',play:'<path d="m6 3 15 9-15 9z"/>',minus:'<path d="M5 12h14"/>',mic:'<rect x="9" y="2" width="6" height="13" rx="3"/><path d="M5 10v2a7 7 0 0 0 14 0v-2M12 19v3m-3 0h6"/>',mute:'<path d="m3 3 18 18M9 9v3a3 3 0 0 0 5 2M9 5a3 3 0 0 1 6 0v4M5 10v2a7 7 0 0 0 12 5M19 10v2M12 19v3"/>',chat:'<path d="M21 11.5a8.5 8.5 0 0 1-12.5 7.4L3 21l2.1-5.5A8.5 8.5 0 1 1 21 11.5Z"/>',end:'<path d="m6 6 12 12M18 6 6 18"/>',send:'<path d="m22 2-7 20-4-9L2 9 22 2ZM11 13 22 2"/>'};
function icon(name){return `<svg viewBox="0 0 24 24" aria-hidden="true">${iconPaths[name]||''}</svg>`;}
all('[data-icon]').forEach(e=>e.innerHTML=icon(e.dataset.icon));
function el(tag,cls,text){const e=document.createElement(tag);if(cls)e.className=cls;if(text!==undefined)e.textContent=text;return e;}
let noticeTimer;function notice(text){$('notice').textContent=text;$('notice').hidden=false;clearTimeout(noticeTimer);noticeTimer=setTimeout(()=>$('notice').hidden=true,5000);}
const images={};const textures=new Map();
async function loadImage(key){const im=new Image();im.src=`assets/${key==='room-wide'?'room-wide-v2':key==='room'?'room-sharp':key}.jpg`;await im.decode();images[key]=im;return im;}
function triangle(ctx,img,s,d){const [p,q,r]=s,[a,b,c]=d;const det=(q[0]-p[0])*(r[1]-p[1])-(r[0]-p[0])*(q[1]-p[1]);const aa=((b[0]-a[0])*(r[1]-p[1])-(c[0]-a[0])*(q[1]-p[1]))/det,cc=((c[0]-a[0])*(q[0]-p[0])-(b[0]-a[0])*(r[0]-p[0]))/det,bb=((b[1]-a[1])*(r[1]-p[1])-(c[1]-a[1])*(q[1]-p[1]))/det,dd=((c[1]-a[1])*(q[0]-p[0])-(b[1]-a[1])*(r[0]-p[0]))/det;ctx.save();ctx.beginPath();ctx.moveTo(...a);ctx.lineTo(...b);ctx.lineTo(...c);ctx.closePath();ctx.clip();ctx.transform(aa,bb,cc,dd,a[0]-aa*p[0]-cc*p[1],a[1]-bb*p[0]-dd*p[1]);ctx.drawImage(img,0,0);ctx.restore();}
function makeTextures(){for(const book of books){const c=document.createElement('canvas');c.width=480;c.height=720;const ctx=c.getContext('2d');if(book.crop){const [x,y,w,h]=book.crop;ctx.drawImage(images.plate,x,y,w,h,0,0,480,720);}else{const corners=[[740,133],[1006,154],[1012,585],[739,588]];const point=(u,v)=>[mix(mix(corners[0][0],corners[1][0],u),mix(corners[3][0],corners[2][0],u),v),mix(mix(corners[0][1],corners[1][1],u),mix(corners[3][1],corners[2][1],u),v)];for(let j=0;j<12;j++)for(let i=0;i<8;i++){const u=i/8,v=j/12,u2=(i+1)/8,v2=(j+1)/12;const s=[point(u,v),point(u2,v),point(u2,v2),point(u,v2)],d=[[u*480,v*720],[u2*480,v*720],[u2*480,v2*720],[u*480,v2*720]];triangle(ctx,images.original,[s[0],s[1],s[2]],[d[0],d[1],d[2]]);triangle(ctx,images.original,[s[0],s[2],s[3]],[d[0],d[2],d[3]]);}}textures.set(book.id,c);}paintCovers();}
function paintCover(c,id){const texture=textures.get(id);if(!texture)return;c.width=texture.naturalWidth||texture.width;c.height=texture.naturalHeight||texture.height;const ctx=c.getContext('2d');ctx.drawImage(texture,0,0);const book=books.find(b=>b.id===id);if(book.typeset){const w=c.width,h=c.height;
const designs={
 'julius-caesar':{lines:['JULIUS','CAESAR'],y:.16,size:.16,align:'left',x:.10},
 'the-prince':{lines:['THE','PRINCE'],y:.68,size:.15,align:'center',x:.5},
 'jane-eyre':{lines:['Jane','Eyre'],y:.24,size:.21,align:'center',x:.5,italic:true},
 'odyssey':{lines:['THE ODYSSEY'],y:.12,size:.086,align:'center',x:.5},
 'crime-and-punishment':{lines:['CRIME','AND','PUNISHMENT'],y:.17,size:.115,align:'left',x:.09},
 'pride-and-prejudice':{lines:['Pride &','Prejudice'],y:.67,size:.12,align:'center',x:.5,italic:true}};
const d=designs[book.id];ctx.fillStyle='#efe6cc';ctx.textAlign=d.align;ctx.textBaseline='middle';ctx.shadowColor='#090b08';ctx.shadowBlur=w*.013;
ctx.font=`${d.italic?'italic ':''}${w*d.size}px Georgia`;d.lines.forEach((line,i)=>ctx.fillText(line,w*d.x,h*d.y+i*w*d.size*1.15,w*.83));ctx.font=`${w*.033}px Georgia`;ctx.textAlign='center';ctx.fillText(book.author.toUpperCase(),w/2,h*.945,w*.86);ctx.shadowBlur=0;
if(book.id==='odyssey'){ctx.strokeStyle='#d1bd7780';ctx.lineWidth=w*.003;ctx.beginPath();ctx.moveTo(w*.15,h*.17);ctx.lineTo(w*.85,h*.17);ctx.stroke();}
}}


function paintCovers(){all('canvas[data-book]').forEach(c=>paintCover(c,c.dataset.book));}
const coverReady=new Map(books.map(b=>[b.id,(async()=>{const im=new Image();im.src=b.cover;await im.decode();textures.set(b.id,im);all('canvas[data-book]').filter(c=>c.dataset.book===b.id).forEach(c=>paintCover(c,b.id));})()]));
const assetsReady=Promise.all(['room','room-wide'].map(loadImage));
assetsReady.catch(()=>notice('The scene could not load. Please refresh to try again.'));
for(const ready of coverReady.values())ready.catch(()=>notice('A cover could not load. Please refresh to try again.'));
function coverButton(book,cls='book-card cover-button'){const b=el('button',cls);b.setAttribute('aria-label',`Open ${book.title}, by ${book.author}`);const c=el('canvas','cover');c.dataset.book=book.id;b.append(c);b.addEventListener('click',()=>openBook(book,c));paintCover(c,book.id);return b;}
[['Featured',books.map((_,i)=>i)],['A different way of seeing',[5,8,2,4,6,9,7,3,1,0]]].forEach(([title,indices])=>{const section=el('section','shelf');section.append(el('h2','',title));const row=el('div','book-row');row.setAttribute('aria-label',title);indices.forEach(i=>row.append(coverButton(books[i])));section.append(row);$('shelves').append(section);});
let featured=books[0];const featuredBooks=[books[0],books[1],books[6],books[7]];
const readingHours={'frankenstein':6,'jekyll-and-hyde':2,'jane-eyre':17,'odyssey':12};
const questions=['What do we owe the intelligence we create?','How well do we know our other selves?','What does it mean to live on your own terms?','How far must we travel to find home?'];
featuredBooks.forEach((book,i)=>{const b=el('button');b.setAttribute('aria-label',`Feature ${book.title}`);b.classList.toggle('active',i===0);b.addEventListener('click',()=>{featured=book;$('hero-time').textContent=`~${readingHours[book.id]} hours to read`;$('hero-title').textContent=book.title;$('hero-author').textContent=book.author+'’s';document.querySelector('.question').textContent=questions[i];$('hero-book').setAttribute('aria-label',`Open ${book.title}`);const c=$('hero-book').querySelector('canvas');c.dataset.book=book.id;paintCover(c,book.id);all('.hero-dots button').forEach((e,j)=>{e.classList.toggle('active',i===j);e.setAttribute('aria-pressed',String(i===j));});});b.setAttribute('aria-pressed',String(i===0));document.querySelector('.hero-dots').append(b);});
$('hero-book').onclick=$('read-featured').onclick=()=>openBook(featured,$('hero-book').querySelector('canvas'));
$('sign-in').onclick=()=>notice('Sign in will be connected in the full reading experience.');
$('header').querySelector('.logo').onclick=e=>{e.preventDefault();if(activeBook)closeBook();else selectCollection('home');};
addEventListener('scroll',()=>$('header').classList.toggle('scrolled',scrollY>20),{passive:true});

// One image, one coordinate system. Rain is clipped to window panes and never
// painted over the table or the bottle silhouette in the foreground.
const panes=[[[521,0],[626,0],[629,155],[526,170]],[[646,0],[839,0],[841,139],[648,154]],[[878,0],[1063,0],[1065,117],[880,136]],[[1077,0],[1137,0],[1149,110],[1080,117]],[[530,191],[629,178],[636,496],[536,504]],[[649,173],[840,157],[845,498],[650,500]],[[883,153],[1066,133],[1075,500],[886,502]],[[1083,130],[1150,122],[1164,500],[1090,503]]];
const widePanes=[[[1050,120],[1114,108],[1114,259],[1049,267]],[[1131,0],[1255,0],[1258,249],[1131,262]],[[1290,0],[1403,0],[1405,231],[1290,245]],[[1423,0],[1487,0],[1490,223],[1423,232]],[[1508,0],[1546,0],[1550,217],[1508,222]],[[1050,287],[1115,280],[1116,417],[1051,417]],[[1133,282],[1259,268],[1262,516],[1134,513]],[[1292,263],[1406,251],[1413,516],[1294,516]],[[1427,249],[1490,240],[1499,514],[1427,517]],[[1510,238],[1552,234],[1557,514],[1517,515]]];
function drawScene(time){if(!images.room||!images['room-wide'])return;const canvas=$('scene'),w=canvas.clientWidth,h=canvas.clientHeight,dpr=Math.min(devicePixelRatio||1,2);if(canvas.width!==Math.round(w*dpr)||canvas.height!==Math.round(h*dpr)){canvas.width=Math.round(w*dpr);canvas.height=Math.round(h*dpr);}const ctx=canvas.getContext('2d');ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,w,h);const wide=w/h>1.2,img=wide?images['room-wide']:images.room;const crop=sceneCrop(w,h,img.naturalWidth,img.naturalHeight,wide?.5:.7);ctx.drawImage(img,crop.x,crop.y,crop.w,crop.h,0,0,w,h);if(reduced.matches)return;ctx.save();ctx.scale(crop.scale,crop.scale);ctx.translate(-crop.x,-crop.y);ctx.beginPath();(wide?widePanes:panes).forEach(p=>{ctx.moveTo(...p[0]);p.slice(1).forEach(q=>ctx.lineTo(...q));ctx.closePath();});ctx.clip();if(!wide){ctx.beginPath();ctx.rect(0,0,1183,1330);ctx.rect(492,178,202,378);ctx.clip('evenodd');}ctx.lineWidth=1.05;for(let i=0;i<140;i++){const x=(wide?1040:520)+(i*73.39)%(wide?520:660),y=((time*.10+i*51.79)%(wide?525:510))-20;ctx.strokeStyle=`rgba(212,235,218,${.15+(i%4)*.04})`;ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x-1.3,y+13+(i%7));ctx.stroke();}ctx.restore();}

let searchOpen=false,openedFromSearch=false,menuOpen=false;
function setSearch(open,preserve=false){searchOpen=open;$('search-panel').hidden=!open;syncLock();if(open){if(menuOpen)setMenu(false);if(!preserve)renderResults();setTimeout(()=>$('search-input').focus(),40);}else{$('search-toggle').focus();}}
export function filterBooks(query){const words=query.toLocaleLowerCase().trim().split(/\s+/);return books.filter(b=>words.every(word=>`${b.title} ${b.author}`.toLocaleLowerCase().includes(word)));}
function renderResults(){const q=$('search-input').value;const matches=filterBooks(q);$('search-label').textContent=q?`${matches.length} ${matches.length===1?'book':'books'} found`:'Explore the library';$('search-results').replaceChildren();matches.forEach(book=>{const b=coverButton(book,'search-result');b.append(el('span','',book.title),el('small','',book.author));$('search-results').append(b);});if(!matches.length)$('search-results').append(el('p','','Try another title or author.'));}
$('search-toggle').onclick=()=>setSearch(true);$('search-cancel').onclick=()=>setSearch(false);$('search-input').oninput=renderResults;

let activeBook=null,sourceCanvas=null,sourceRect=null,bookProgress=0,bookAnim=null,currentTab='preface',edition='original-en',reading=false,tourProgress=0,tourAnimation=null,readerProgress=0,readerAnimation=null;
const editionChoices=new Map();
function choices(){if(!editionChoices.has(activeBook.id))editionChoices.set(activeBook.id,{main:'original-en',audio:'original-en',secondary:'modern-en'});return editionChoices.get(activeBook.id);}
let pageLocked=false,lockedScroll=0;function syncLock(){const lock=!!(activeBook||searchOpen||menuOpen||mode!=='minimized');if(lock===pageLocked)return;pageLocked=lock;if(lock){lockedScroll=scrollY;Object.assign(document.body.style,{position:'fixed',top:-lockedScroll+'px',left:'0',right:'0',overflow:'hidden'});}else{for(const key of ['position','top','left','right','overflow'])document.body.style[key]='';scrollTo({top:lockedScroll,behavior:'instant'});}}
function rectOf(c){const r=c.getBoundingClientRect();return {x:r.x,y:r.y,w:r.width,h:r.height};}
export function bookDestination(){const dest=destination(innerWidth,innerHeight,matchMedia('(pointer:coarse)').matches);if(dest.tour){const u=ease(tourProgress),height=window.visualViewport?.height||innerHeight;const short=height<500,pageWidth=Math.min((innerWidth-40)/.66,short?620:(height-128)/1.5,620),pageHeight=short?height-128:pageWidth*1.5;dest.x=mix(20+pageWidth,0,u);dest.y=mix(60+(height-128-pageHeight)/2,0,u);dest.w=mix(pageWidth,innerWidth,u);dest.h=mix(pageHeight,height,u);}return dest;}
function drawBook(p){const dest=bookDestination();const viewportHeight=window.visualViewport?.height||innerHeight;const flapWidth=dest.tour?Math.min((innerWidth-40)/.66,viewportHeight<500?620:(viewportHeight-128)/1.5,620):dest.w;const flapHeight=dest.tour?(viewportHeight<500?viewportHeight-128:flapWidth*1.5):dest.h;const rp=ease(readerProgress);if(readerProgress){dest.x=mix(dest.x,0,rp);dest.y=mix(dest.y,0,rp);dest.w=mix(dest.w,innerWidth,rp);dest.h=mix(dest.h,window.visualViewport?.height||innerHeight,rp);}const f=bookFrame(sourceRect,dest,p);const shape=$('book-shape');shape.classList.toggle('compact-book',dest.tour);shape.style.width=dest.w+'px';shape.style.setProperty('--slip-font',Math.min(dest.tour?17:15,flapWidth/320*(dest.tour?11:12))+'px');shape.style.setProperty('--portrait-height',Math.min(150,flapWidth/320*96)+'px');$('intro').style.width=dest.tour?innerWidth+'px':'';$('intro').style.height=dest.tour?viewportHeight+'px':'';const back=document.querySelector('.cover-back');back.style.width=flapWidth+'px';back.style.height=flapHeight+'px';back.style.left='0px';shape.style.setProperty('--flap-angle',dest.tour?'-3deg':'-8deg');shape.style.height=f.h/f.scale+'px';shape.style.transform=`translate3d(${f.x}px,${f.y}px,0) scale(${f.scale})`;$('turning-cover').style.transform=`translateZ(1px) rotateY(${f.angle}deg)`;$('turning-cover').style.opacity=1;$('turning-cover').hidden=f.opacity===0;document.querySelector('.cover-front').hidden=false;document.querySelector('.cover-back').hidden=false;document.querySelector('.cover-front').style.visibility=f.angle<=-90?'hidden':'visible';document.querySelector('.cover-back').style.visibility=f.angle<=-90?'visible':'hidden';$('book-backdrop').style.opacity=ease(p);$('intro').style.opacity=(dest.tour?clamp(tourProgress*3,0,1):1)*(1-clamp(readerProgress/.32,0,1));$('reader').style.opacity=clamp((readerProgress-.55)/.45,0,1);$('reader').style.pointerEvents=readerProgress===1?'auto':'none';$('reader').hidden=readerProgress===0; $('intro').inert=readerProgress>0; $('reader').inert=readerProgress!==1;$('intro').style.pointerEvents=p===1&&(!dest.tour||tourProgress===1)?'auto':'none';$('book-slip').style.pointerEvents=p===1?'auto':'none';$('slip-next').hidden=!dest.tour||tourProgress!==0||p!==1;$('tour-back').hidden=!dest.tour||tourProgress!==0||p!==1;$('back-slip').hidden=true;$('intro').setAttribute('aria-hidden',String(dest.tour&&tourProgress!==1));$('book-slip').setAttribute('aria-hidden',String(dest.tour&&tourProgress===1));bookProgress=p;if(p===1)scrollHint();}
function animateBook(to){if(bookAnim)cancelAnimationFrame(bookAnim);const from=bookProgress,start=performance.now(),duration=reduced.matches?0:2150*Math.abs(to-from);return new Promise(resolve=>{function frame(now){const t=duration?clamp((now-start)/duration,0,1):1;drawBook(mix(from,to,t));if(t<1)bookAnim=requestAnimationFrame(frame);else{bookAnim=null;resolve();}}bookAnim=requestAnimationFrame(frame);});}

const shelleyBiography='Mary Shelley’s mother, feminist Mary Wollstonecraft, died days after her birth. Her father, philosopher William Godwin, nurtured her intellect, though she often felt neglected by him. She ran off with Percy Bysshe Shelley at sixteen and began <i>Frankenstein</i> at eighteen, inspired by her friend Lord Byron’s ghost-story challenge. Despite losing three children and her husband, she raised her surviving son and published six novels before dying at fifty-three.';
function setSlip(book){const mary=book.id==='frankenstein';$('slip-portrait').hidden=!mary;$('slip-caption').replaceChildren(el('span','',book.author));if(mary){$('slip-caption').append(el('span','','English novelist · 1797–1851'));$('slip-copy').innerHTML=shelleyBiography;}else $('slip-copy').textContent=book.preface[0];}

async function openBook(book,canvas){if(activeBook)return;if(menuOpen)setMenu(false);await coverReady.get(book.id);if(activeBook)return;activeBook=book;$('turning-cover').style.setProperty('--cover-tone',book.coverTone);sourceCanvas=canvas;sourceRect=rectOf(canvas);openedFromSearch=searchOpen;if(searchOpen)setSearch(false);reading=false;readerProgress=0;tourProgress=0;setSlip(book);$('book-slip').scrollTop=0;updateSaveButton();currentTab='preface';bookProgress=0;renderIntro();paintCover($('moving-cover'),book.id);$('book-overlay').hidden=false;sourceCanvas.style.visibility='hidden';drawBook(0);syncLock();updateLibrarianContext();await animateBook(1);resetIntroScroll();(destination(innerWidth,innerHeight,matchMedia('(pointer:coarse)').matches).tour?$('slip-next'):$('back-library')).focus({preventScroll:true});}
async function closeBook(){if(!activeBook||bookProgress!==1)return;if(mode!=='minimized')minimize();if(readerProgress)await animateReader(0);if(tourProgress)await animateTour(0);if(openedFromSearch){setSearch(true,true);openedFromSearch=false;}sourceRect=rectOf(sourceCanvas);await animateBook(0);$('book-overlay').hidden=true;sourceCanvas.style.visibility='';const focus=sourceCanvas.parentElement;activeBook=null;sourceCanvas=null;syncLock();updateLibrarianContext();focus.focus({preventScroll:true});if(collectionChoice!=='home'){renderCollection();$('collection-back').focus({preventScroll:true});}}
function animateTour(to){if(tourAnimation)cancelAnimationFrame(tourAnimation);const from=tourProgress,start=performance.now();return new Promise(resolve=>{function step(now){const t=reduced.matches?1:clamp((now-start)/950,0,1);tourProgress=mix(from,to,t);drawBook(1);if(t<1)tourAnimation=requestAnimationFrame(step);else{tourAnimation=null;resolve();}}tourAnimation=requestAnimationFrame(step);});}
$('slip-next').onclick=async()=>{if(tourAnimation)return;await animateTour(1);resetIntroScroll();$('back-library').focus({preventScroll:true});};
$('back-slip').onclick=async()=>{if(tourAnimation)return;await animateTour(0);$('slip-next').focus({preventScroll:true});};
$('book-backdrop').onclick=$('tour-back').onclick=closeBook;$('back-library').onclick=()=>{if(currentTab!=='preface'){currentTab='preface';renderIntro();}else if(bookDestination().tour){if(!tourAnimation)animateTour(0).then(()=>$('slip-next').focus({preventScroll:true}));}else{$('book-slip').scrollTop=0;$('book-slip').focus({preventScroll:true});}};
function appendParagraphs(target,paragraphs){paragraphs.forEach(p=>target.append(el('p','',p)));}
function renderIntro(){
 const body=$('intro-body');const footer=document.querySelector('.intro-footer');if(footer.parentElement===body)$('intro').append(footer);body.replaceChildren();resetIntroScroll();requestAnimationFrame(()=>{resetIntroScroll();scrollHint();});
 $('intro-heading').textContent='Introduction';
 document.querySelector('.intro-tabs').hidden=false;
 $('begin-reading').textContent='Begin reading →';
 all('[data-tab]').forEach(b=>{b.hidden=false;b.setAttribute('aria-selected',String(b.dataset.tab===currentTab));b.tabIndex=b.dataset.tab===currentTab?0:-1;});
 if(currentTab==='preface'){appendParagraphs(body,activeBook.preface);if(activeBook.id==='frankenstein')body.append(el('p','preface-credit','Anders K. Hvelplund, Copenhagen Sep 24, 2026'));}
 else if(currentTab==='characters'){introCast(activeBook).forEach((c,i)=>{const section=el('section','character');if(activeBook.id==='frankenstein'){const sketch=el('div','character-sketch');sketch.style.backgroundPosition=i?'50% 0':'0 0';sketch.setAttribute('role','img');sketch.setAttribute('aria-label',`Pencil interpretation of ${c.aliases[0]}`);section.append(sketch);}const text=el('div','character-copy');text.append(el('h3','',c.aliases[0]),el('p','character-role',c.introRole),el('p','',c.subtitle));section.append(text);body.append(section);});}

 else {
  const versions=[['original-en',activeBook.id==='frankenstein'?'Original (1831)':'Original','Hard'],['modern-en','Modern English','Easy'],['modern-da','Moderne Dansk','Easy']];
  for(const [key,label] of [['main','Main version'],['audio','Audiobook version'],['secondary','Secondary version']]){
   const field=el('section','edition-field');field.append(el('h3','',label));
   const details=el('details','edition-select'),summary=el('summary');
   summary.setAttribute('aria-label',label);
   summary.append(el('span','',versions.find(v=>v[0]===choices()[key])[1]),el('span','chevron','⌄'));
   details.append(summary);
   const options=el('div','edition-options');
   versions.forEach(([id,title,difficulty])=>{const b=el('button','edition-option');b.append(el('strong','',title),el('small','',difficulty));if(id!=='original-en')b.append(el('small','','AI-generated'));b.setAttribute('aria-pressed',String(choices()[key]===id));b.onclick=()=>{choices()[key]=id;summary.firstElementChild.textContent=title;options.querySelectorAll('button').forEach(x=>x.setAttribute('aria-pressed',String(x===b)));details.open=false;summary.focus();};options.append(b);});
   details.append(options);details.ontoggle=()=>{if(details.open)all('.edition-select').forEach(d=>{if(d!==details)d.open=false;});};field.append(details);body.append(field);
  }
 }
 if(bookDestination().tour)body.append(footer);
}
function resetIntroScroll(){for(const id of ['intro','intro-body','paper'])$(id).scrollTop=0;}
all('[data-tab]').forEach(b=>{b.onclick=()=>{currentTab=b.dataset.tab;renderIntro();};b.onkeydown=e=>{if(!['ArrowLeft','ArrowRight'].includes(e.key))return;e.preventDefault();const tabs=all('[data-tab]').filter(t=>!t.hidden);const i=tabs.indexOf(b);tabs[(i+(e.key==='ArrowRight'?1:tabs.length-1))%tabs.length].click();all('[data-tab]').find(t=>t.getAttribute('aria-selected')==='true').focus();};});
$('begin-reading').onclick=()=>{if(readerAnimation)return;renderReader();animateReader(1);};
function introCast(book){const selection={'frankenstein':[0,1],'jane-eyre':[0,1,4],'julius-caesar':[0,1,2],'odyssey':[0,1,2,3,4]};const list=(selection[book.id]||[0,1,2]).map(i=>({...book.characters[i]})).filter(c=>c.aliases);return list.map((c,i)=>({...c,introRole:['the-prince','meditations'].includes(book.id)?(i===0?'Author':'Key figure'):(i===0||(book.id==='frankenstein'&&i===1)||(book.id==='pride-and-prejudice'&&i===1)?'Protagonist':'Supporting character')}));}
function scrollHint(){const b=$('intro-body');b.classList.toggle('more-below',b.scrollHeight-b.clientHeight-b.scrollTop>10);}
$('intro-body').addEventListener('scroll',scrollHint,{passive:true});
let readerTheme='book';try{readerTheme=localStorage.getItem('tinct-library-2-reader-theme')||'book';}catch{} 
function applyReaderTheme(){ $('book-overlay').classList.toggle('reader-dark',readerTheme==='dark');[...$('reader-theme').options].forEach(o=>o.selected=o.value===readerTheme); }
function renderReader(){const excerpt=activeBook.excerpts[choices().main]||activeBook.excerpts['original-en'];$('reader-title').textContent=activeBook.title;$('reader-author').textContent=activeBook.author;$('reader-chapter').textContent=excerpt.title;$('reader-text').replaceChildren();appendParagraphs($('reader-text'),excerpt.paragraphs);$('reader-content').scrollTop=0;}
function animateReader(to){if(readerAnimation)cancelAnimationFrame(readerAnimation);const from=readerProgress,start=performance.now();return new Promise(resolve=>{function frame(now){const t=reduced.matches?1:clamp((now-start)/1100,0,1);readerProgress=mix(from,to,t);reading=readerProgress>0;drawBook(1);if(t<1)readerAnimation=requestAnimationFrame(frame);else{readerAnimation=null;reading=to===1;(to?$('reader-back'):$('begin-reading')).focus({preventScroll:true});resolve();}}readerAnimation=requestAnimationFrame(frame);});}
$('reader-back').onclick=()=>{currentTab='preface';renderIntro();animateReader(0);};
$('reader-theme').onchange=()=>{readerTheme=$('reader-theme').value;applyReaderTheme();try{localStorage.setItem('tinct-library-2-reader-theme',readerTheme);}catch{}};applyReaderTheme();


let introGesture=null;
const interactive='button,a,input,select,summary,textarea';
function navigateIntro(direction){if(!activeBook||bookProgress!==1||bookAnim||tourAnimation||readerAnimation||mode!=='minimized')return;const compact=bookDestination().tour;if(compact&&tourProgress===0){if(direction>0)$('slip-next').click();return;}if(reading){const body=$('reader-content');if(direction<0&&body.scrollTop<4){$('reader-back').click();}else body.scrollBy({top:direction*body.clientHeight*.8,behavior:reduced.matches?'instant':'smooth'});return;}const tabs=['preface','characters','edition'],index=tabs.indexOf(currentTab),body=$('intro-body');if(direction>0&&body.scrollTop+body.clientHeight<body.scrollHeight-4){body.scrollBy({top:body.clientHeight*.8,behavior:'smooth'});return;}if(direction<0&&body.scrollTop>4){body.scrollBy({top:-body.clientHeight*.8,behavior:'smooth'});return;}if(index===0&&direction<0){if(compact)animateTour(0);return;}if(index===2&&direction>0){$('begin-reading').click();return;}currentTab=tabs[index+direction];renderIntro();}
$('book-overlay').addEventListener('pointerdown',e=>{if(!bookDestination().tour||e.target.closest(interactive))return;introGesture={x:e.clientX,y:e.clientY};});
$('book-overlay').addEventListener('pointerup',e=>{const g=introGesture;introGesture=null;if(!g||e.target.closest(interactive)||window.getSelection()?.toString())return;const dx=e.clientX-g.x,dy=e.clientY-g.y;if(Math.abs(dx)>45&&Math.abs(dx)>Math.abs(dy)*1.5)navigateIntro(dx<0?1:-1);else if(Math.hypot(dx,dy)<8){if(e.clientX<innerWidth*.18)navigateIntro(-1);else if(e.clientX>innerWidth*.82)navigateIntro(1);}});
$('book-overlay').addEventListener('pointercancel',()=>introGesture=null);
// One persistent particle sphere follows a single interpolated position. There
// is no second orb to cross-fade, and chat never lays it over its controls.
let mode='minimized',orbMotion=null,drag=null,micStream=null,micPending=0,muted=false;
let dock={x:innerWidth-52,y:innerHeight*.43,size:52,edge:'right'},orbPosition={...dock};
function placeOrb(p){orbPosition={...p};const b=$('librarian');b.style.width=p.size+'px';b.style.height=p.size+'px';b.style.transform=`translate3d(${p.x}px,${p.y}px,0)`;if(mode==='minimized')magnet();}
function orbTarget(){const space=$(`librarian-${mode}`).querySelector('.orb-space').getBoundingClientRect();const size=Math.min(space.height,space.width,240);return {x:space.x+(space.width-size)/2,y:space.y+(space.height-size)/2,size};}
function moveOrb(target,done){if(orbMotion)cancelAnimationFrame(orbMotion);const from={...orbPosition},start=performance.now();function frame(now){const t=reduced.matches?1:clamp((now-start)/450,0,1);placeOrb(orbFrame(from,target,t));if(t<1)orbMotion=requestAnimationFrame(frame);else{orbMotion=null;done?.();}}orbMotion=requestAnimationFrame(frame);}
function updateLibrarianContext(){$('librarian-question').textContent=activeBook?'Can I help you prepare your reading of the book?':'Want help finding a book?';$('chat-input').placeholder=activeBook?'Ask about the book…':'What would you like to read?';}
let panelLastRect={x:20,y:80};
function setMode(next){const previous=mode;if(previous!=='minimized'){const r=$('librarian-panel').getBoundingClientRect();panelLastRect={x:r.x,y:r.y};}mode=next;['welcome','talk','chat'].forEach(m=>$(`librarian-${m}`).hidden=m!==next);$('librarian-panel').hidden=next==='minimized';$('librarian').hidden=next==='chat';$('librarian').tabIndex=next==='minimized'?0:-1;if(next!=='minimized')$('librarian').classList.add('expanded');$('librarian').dataset.edge='none';if(next!=='minimized'){$('librarian').dataset.magnet='none';$('librarian').style.borderRadius='50%';}syncLock();if(next==='minimized'){$('librarian').classList.add('returning');const finish=()=>{$('librarian').classList.remove('expanded','returning');$('librarian').dataset.edge=dock.edge;};if(previous==='chat'){placeOrb({x:clamp(panelLastRect.x+24,0,innerWidth-52),y:clamp(panelLastRect.y+65,0,innerHeight-52),size:52});}moveOrb(dock,finish);}else if(next!=='chat'){requestAnimationFrame(()=>moveOrb(orbTarget()));}updateLibrarianContext();}
function openLibrarian(){setMode('welcome');$('minimize').focus({preventScroll:true});}
function stopMic(){micPending++;if(micStream)micStream.getTracks().forEach(t=>t.stop());micStream=null;muted=false;}
function minimize(){stopMic();setMode('minimized');$('librarian').focus({preventScroll:true});}
$('minimize').onclick=minimize;
$('librarian').onclick=e=>{if(drag?.moved){drag=null;e.preventDefault();return;}if(mode==='minimized')openLibrarian();};
function magnet(){const b=$('librarian'),p=orbPosition,size=p.size;const gaps=[p.x,innerWidth-p.x-size,p.y,innerHeight-p.y-size],distance=Math.max(0,Math.min(...gaps)),edge=['left','right','top','bottom'][gaps.indexOf(Math.min(...gaps))];const strength=1-ease(clamp(distance/72,0,1)),round=50*(1-strength);const radii={left:[round,50,50,round],right:[50,round,round,50],top:[round,round,50,50],bottom:[50,50,round,round]};b.style.borderRadius=(radii[edge]||[50,50,50,50]).map(n=>n+'%').join(' ');b.dataset.magnet=distance<72?edge:'none';b.style.setProperty('--tail-size',distance+'px');b.style.setProperty('--tail-opacity',String(strength));}
$('librarian').onpointerdown=e=>{if(mode!=='minimized')return;if(orbMotion)cancelAnimationFrame(orbMotion);drag={x:e.clientX,y:e.clientY,start:{...orbPosition},moved:false};$('librarian').setPointerCapture(e.pointerId);};
$('librarian').onpointermove=e=>{if(!drag)return;const dx=e.clientX-drag.x,dy=e.clientY-drag.y;if(Math.hypot(dx,dy)>5){drag.moved=true;movedLibrarian=true;}if(!drag.moved)return;placeOrb({x:clamp(drag.start.x+dx,0,innerWidth-52),y:clamp(drag.start.y+dy,0,innerHeight-52),size:52});$('librarian').dataset.edge='none';magnet();};
$('librarian').onpointerup=()=>{if(!drag)return;if(drag.moved){dock=dockPosition(orbPosition.x,orbPosition.y,innerWidth,innerHeight);moveOrb(dock,()=>{$('librarian').dataset.edge=dock.edge;});}else drag=null;};
$('librarian').onpointercancel=()=>{drag=null;dock=dockPosition(orbPosition.x,orbPosition.y,innerWidth,innerHeight);moveOrb(dock,()=>{$('librarian').dataset.edge=dock.edge;});};
$('librarian').onkeydown=e=>{if(mode!=='minimized'||!e.key.startsWith('Arrow'))return;e.preventDefault();const step=e.shiftKey?40:10;dock=dockPosition(dock.x+(e.key==='ArrowRight'?step:e.key==='ArrowLeft'?-step:0),dock.y+(e.key==='ArrowDown'?step:e.key==='ArrowUp'?-step:0),innerWidth,innerHeight);$('librarian').dataset.edge=dock.edge;moveOrb(dock);};
$('talk').onclick=async()=>{setMode('talk');$('voice-title').textContent='Let’s talk';$('voice-status').textContent='Connecting to your microphone…';const request=++micPending;try{if(!navigator.mediaDevices?.getUserMedia)throw Error('unavailable');const stream=await navigator.mediaDevices.getUserMedia({audio:true});if(request!==micPending||mode!=='talk'){stream.getTracks().forEach(t=>t.stop());return;}micStream=stream;$('voice-title').textContent='Ready';$('voice-status').textContent='Microphone connected. Live conversation is not connected in this design preview.';}catch(e){if(request!==micPending)return;$('voice-status').textContent=e.name==='NotAllowedError'?'Microphone access is off. You can still chat with your librarian.':'Your microphone is unavailable. You can still use Chat.';}};
$('mute').onclick=()=>{muted=!muted;micStream?.getAudioTracks().forEach(t=>t.enabled=!muted);$('mute').innerHTML=icon(muted?'mute':'mic')+`<span>${muted?'Unmute':'Mute'}</span>`;$('mute').setAttribute('aria-pressed',String(muted));};
$('end-talk').onclick=()=>{stopMic();$('mute').innerHTML=icon('mic')+'<span>Mute</span>';setMode('welcome');};
function startChat(){stopMic();setMode('chat');const messages=$('messages');messages.replaceChildren();const prompts=activeBook?['What should I notice at the beginning?','Who are the main characters?','Help me find an angle that interests me.']:['Help me find a book.','Something atmospheric and unsettling.','A short, thought-provoking read.'];prompts.forEach(text=>{const b=el('button','suggestion',text);b.onclick=()=>sendMessage(text);messages.append(b);});}
$('chat').onclick=startChat;
function sendMessage(text){if(!text.trim())return;all('.suggestion').forEach(b=>b.remove());const m=$('messages');m.append(el('p','message-user',text));m.append(el('p','',activeBook?`For ${activeBook.title}, start with the introduction and the questions it raises. This preview shows the conversation layout; a live librarian will respond to your question in the complete experience.`:'For an atmospheric beginning, try Frankenstein or Dr Jekyll and Mr Hyde. This is a sample response in the design preview.'));m.scrollTop=m.scrollHeight;$('chat-input').value='';}
$('chat-form').onsubmit=e=>{e.preventDefault();sendMessage($('chat-input').value);};
const dots=Array.from({length:520},(_,i)=>{const y=1-(i+.5)/520*2,r=Math.sqrt(1-y*y),a=i*2.3999632297;return [Math.cos(a)*r,y,Math.sin(a)*r];});
function drawOrb(time){const c=$('orb'),size=c.clientWidth,dpr=Math.min(devicePixelRatio||1,2);const pixels=Math.round(size*dpr);if(c.width!==pixels){c.width=pixels;c.height=pixels;}const ctx=c.getContext('2d');ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,size,size);const a=reduced.matches?.4:time*.00012,ca=Math.cos(a),sa=Math.sin(a);const projected=dots.map(([x,y,z])=>{const xx=x*ca+z*sa,zz=z*ca-x*sa;return [xx,y,zz];}).sort((a,b)=>a[2]-b[2]);for(const [x,y,z] of projected){const perspective=1/(1.8-z*.22);ctx.fillStyle=`rgba(236,237,222,${.2+(z+1)*.34})`;ctx.beginPath();ctx.arc(size/2+x*size*.76*perspective,size/2+y*size*.76*perspective,Math.max(.48,size*.0052)*(1+z*.27),0,Math.PI*2);ctx.fill();}}
let lastFrame=0;function ambient(now){if(now-lastFrame>32&&!document.hidden){lastFrame=now;if($('hero').getBoundingClientRect().bottom>0)drawScene(now);if(!$('librarian').hidden)drawOrb(now);}requestAnimationFrame(ambient);}placeOrb(dock);$('librarian').dataset.edge=dock.edge;requestAnimationFrame(ambient);
let movedLibrarian=false;
function fitHero(){const hero=$('hero').getBoundingClientRect(),copy=document.querySelector('.hero-copy').getBoundingClientRect(),b=$('hero-book');document.documentElement.style.setProperty('--hero-height',hero.height+'px');if(innerWidth<700){const top=$('header').getBoundingClientRect().height+10,available=Math.max(90,copy.top-hero.top-top-18),height=Math.min(hero.height*.35,available);b.style.top=top+available-height+'px';b.style.height=height+'px';b.style.bottom='auto';}else{b.style.top='';b.style.height='';b.style.bottom='';}if(!movedLibrarian&&mode==='minimized'&&!activeBook){const r=b.getBoundingClientRect();dock={x:innerWidth-52,y:clamp(r.top+scrollY+r.height/2-26,70,innerHeight-65),size:52,edge:'right'};placeOrb(dock);}}
if(typeof ResizeObserver!=='undefined'){const observer=new ResizeObserver(fitHero);observer.observe(document.querySelector('.hero-copy'));observer.observe($('hero'));}fitHero();
let previousWidth=innerWidth,previousHeight=innerHeight;
addEventListener('resize',()=>{fitHero();const free=dock.edge==='none';dock=dockPosition(free?dock.x/Math.max(1,previousWidth-52)*(innerWidth-52):dock.edge==='right'?innerWidth-52:dock.x,free?dock.y/Math.max(1,previousHeight-52)*(innerHeight-52):dock.edge==='bottom'?innerHeight-52:dock.y,innerWidth,innerHeight);previousWidth=innerWidth;previousHeight=innerHeight;if(mode==='minimized')placeOrb(dock);else if(mode!=='chat')requestAnimationFrame(()=>moveOrb(orbTarget()));if(activeBook){if(bookProgress===1)drawBook(1);else{sourceRect=rectOf(sourceCanvas);drawBook(bookProgress);}}drawScene(performance.now());});
addEventListener('keydown',e=>{if(e.key==='Escape'){if(mode!=='minimized')minimize();else if(activeBook){if(reading)$('reader-back').click();else closeBook();}else if(searchOpen)setSearch(false);else if(menuOpen)setMenu(false);}if(e.key==='Tab'){const modal=mode!=='minimized'?$('librarian-panel'):activeBook&&bookProgress===1?(destination(innerWidth,innerHeight,matchMedia('(pointer:coarse)').matches).tour&&tourProgress===0?$('book-overlay'):reading?$('reader'):$('intro')):searchOpen?$('search-panel'):menuOpen?$('library-menu'):null;if(!modal)return;const items=[...modal.querySelectorAll('button,input,select,[tabindex="0"]')].filter(x=>!x.hidden&&!x.disabled&&!x.closest('[aria-hidden="true"]')&&x.getClientRects().length);if(!items.length)return;const i=items.indexOf(document.activeElement);if(e.shiftKey&&i<=0){e.preventDefault();items.at(-1).focus();}else if(!e.shiftKey&&i===items.length-1){e.preventDefault();items[0].focus();}}});
function fitKeyboard(){document.documentElement.style.setProperty('--visual-height',(window.visualViewport?.height||innerHeight)+'px');document.documentElement.style.setProperty('--visual-top',(window.visualViewport?.offsetTop||0)+'px');}window.visualViewport?.addEventListener('resize',()=>{fitKeyboard();if(activeBook&&bookProgress===1)drawBook(1);});fitKeyboard();
// The desktop panel moves independently of the minimized docking point.
const panel=$('librarian-panel'),handle=$('panel-handle'),grip=$('panel-resize');
panel.append(grip);grip.hidden=false;let panelRect=null,panelGesture=null;
function applyPanel(rect){panelRect=panelBounds(rect,innerWidth,innerHeight);if(innerWidth<700||innerHeight<500){for(const k of ['left','top','width','height','right','bottom'])panel.style[k]='';return;}Object.assign(panel.style,{left:panelRect.x+'px',top:panelRect.y+'px',width:panelRect.w+'px',height:panelRect.h+'px',right:'auto',bottom:'auto'});if(mode!=='minimized'&&mode!=='chat'){if(orbMotion)cancelAnimationFrame(orbMotion);placeOrb(orbTarget());}}
function beginPanel(e,resize){if(innerWidth<700||innerHeight<500||(!resize&&e.target.closest('button')))return;const r=panel.getBoundingClientRect();panelGesture={x:e.clientX,y:e.clientY,rect:{x:r.x,y:r.y,w:r.width,h:r.height},resize};e.currentTarget.setPointerCapture(e.pointerId);e.preventDefault();}
for(const [element,resize] of [[handle,false],[grip,true]]){element.onpointerdown=e=>beginPanel(e,resize);element.onpointermove=e=>{if(!panelGesture)return;const g=panelGesture,dx=e.clientX-g.x,dy=e.clientY-g.y;applyPanel(g.resize?{...g.rect,w:g.rect.w+dx,h:g.rect.h+dy}:{...g.rect,x:g.rect.x+dx,y:g.rect.y+dy});};element.onpointerup=element.onpointercancel=()=>panelGesture=null;element.onkeydown=e=>{if(!e.key.startsWith('Arrow')||innerWidth<700)return;e.preventDefault();const r=panel.getBoundingClientRect(),dx=e.key==='ArrowRight'?12:e.key==='ArrowLeft'?-12:0,dy=e.key==='ArrowDown'?12:e.key==='ArrowUp'?-12:0;applyPanel({x:r.x+(resize?0:dx),y:r.y+(resize?0:dy),w:r.width+(resize?dx:0),h:r.height+(resize?dy:0)});};}
addEventListener('resize',()=>{if(panelRect)applyPanel(panelRect);});
addEventListener('pagehide',stopMic);

// Library navigation uses the same forms and eras as the main catalogue.
let savedBooks=new Set();try{savedBooks=new Set(JSON.parse(localStorage.getItem('tinct-library-2-to-read')||'[]').filter(id=>books.some(b=>b.id===id)));}catch{}
let collectionChoice='home';
function persistSaved(){try{localStorage.setItem('tinct-library-2-to-read',JSON.stringify([...savedBooks]));}catch{}$('saved-count').textContent=savedBooks.size?String(savedBooks.size):'';}
function updateSaveButton(){if(!activeBook)return;$('save-book').textContent=savedBooks.has(activeBook.id)?'✓ Read later':'＋ Read later';$('save-book').setAttribute('aria-pressed',String(savedBooks.has(activeBook.id)));}
function toggleSaved(id){if(savedBooks.has(id))savedBooks.delete(id);else savedBooks.add(id);persistSaved();updateSaveButton();if(collectionChoice!=='home'&&!activeBook)renderCollection();}
$('save-book').onclick=()=>toggleSaved(activeBook.id);
function setMenu(open){menuOpen=open;$('library-menu').hidden=!open;$('menu-toggle').setAttribute('aria-expanded',String(open));syncLock();(open?$('menu-close'):$('menu-toggle')).focus({preventScroll:true});}
$('menu-toggle').onclick=()=>setMenu(!menuOpen);$('menu-close').onclick=()=>setMenu(false);$('library-menu').onclick=e=>{if(e.target===$('library-menu'))setMenu(false);};
function selectCollection(value){collectionChoice=value;setMenu(false);$('library').hidden=value!=='home';$('collection').hidden=value==='home';document.querySelector('#header .brand').hidden=value!=='home';$('collection-back').hidden=value==='home';if(value!=='home')renderCollection();(value==='home'?$('menu-toggle'):$('collection-back')).focus({preventScroll:true});scrollTo({top:0,behavior:'instant'});}
function renderCollection(){const [kind,id]=collectionChoice.split(':');let selected=books,title='All books';if(kind==='saved'){selected=books.filter(b=>savedBooks.has(b.id));title='Read later';}if(kind==='category'){selected=id==='all'?books:books.filter(b=>metadata[b.id]?.form===id);title=categories.find(c=>c.id===id)?.label||title;}if(kind==='era'){selected=books.filter(b=>metadata[b.id]?.era===id);title=eras.find(c=>c.id===id)?.label||title;}$('collection-title').textContent=title;const grid=$('collection-books');grid.replaceChildren();selected.forEach(book=>{const card=el('article','collection-card');const open=coverButton(book);card.append(open,el('h2','',book.title),el('p','',book.author));grid.append(card);});$('collection-empty').hidden=selected.length>0;$('collection-empty').textContent=kind==='saved'?'Save books with “＋ Read later” to find them here.':'No books from this period or category in this ten-book preview yet.';}
for(const c of categories){const b=el('button','',c.label);b.onclick=()=>selectCollection('category:'+c.id);$('menu-categories').append(b);}
for(const e of eras){const b=el('button','',e.label);b.append(el('small','',e.range));b.onclick=()=>selectCollection('era:'+e.id);$('menu-periods').append(b);}
all('[data-collection]').forEach(b=>b.onclick=()=>selectCollection(b.dataset.collection));$('collection-back').onclick=()=>selectCollection('home');persistSaved();

// Warm the optional artwork only after the landing page has finished loading.
function warmCharacterArt(){const warm=()=>{const portrait=new Image();portrait.fetchPriority='low';portrait.decoding='async';portrait.src='assets/frankenstein-character-studies.png';portrait.decode().catch(()=>{});};if('requestIdleCallback' in window)window.requestIdleCallback(warm,{timeout:3000});else setTimeout(warm,1000);}
if(document.readyState==='complete')warmCharacterArt();else addEventListener('load',warmCharacterArt,{once:true});
