# Tinct Decision Log

Live log per CLAUDE.md. Format: one row per decision, logged at the moment it's made.

| Date | Decision | Category | Escalated? | Reasoning |
|---|---|---|---|---|
| 2026-08-27 | `/lab/phone` Bible TOC is Tinct’s books-first tree (Testament → section → book → named chapter rows), not a Kindle 1–50 keypad. Read state is real: empty circle unread, solid tan current/in-progress, checkmark for a finished book. Progress text like “12 of 39 books” never prints “0 of N”. Only one book stays open. One EB Garamond stack in the tree. Production `/app` TocOverlay is unchanged. | design | Yes — Anders rejected the live grid and locked the concept mock | He cannot see what he has read on a number pad. The concept already had the marks. |
| 2026-08-21 | `/lab` is three exclusive states from one IBM Plex Mono status line: Reading · Book 1, Hearing · Book 1, Talking · tap the circle to stop. Hearing replaces the chapter with a lyrics stage (real Butler MP3s; word mark only from manifest/sidecar words). Talking keeps the desktop book visible and only the filled circle alive. Stopping Talk returns to Hearing if that is where they came from. Production `/app` and `/read` chrome and the in-car voice brief stay untouched. | design | Yes — Anders locked the 21 Aug wireframes | The ears cannot hear and talk at once. Listen-as-highlighted-page was the wrong surface. |
| 2026-08-21 | `/lab` typed + voice send the full current chapter with numbered paragraphs and a hard spoiler rule (nothing after this chapter). Lab does not use the in-car 20–30s / resume-audiobook brief. Production `buildVoiceInstructions` used by AudioStrip is unchanged. | design | Yes — Anders demonstrated the spoiler + paste-the-paragraph failure | /lab reusing nearby-paragraph in-car context made the model invent the Odyssey ending and refuse to read Book 1 §2. Lab is a conversation beside the page. |
| 2026-08-21 | `/lab` Listen plays real Odyssey Book 1 Butler MP3s via chapter manifest + `/api/audio-file`. Highlight the playing paragraph; word-level only from manifest `words` or `words.json` sidecar. No invented linear timings. No recut MP3s. | design | Yes — Anders said Listen was a fake clock | The first lab follow walked durations without audio. |
| 2026-08-21 | Desktop `/lab` Ask stays a ChatGPT-style chat window on paper `#ece7db`. Conversation lives in the composer icon (immediate connecting/alive state). No full-page orb overlay on desktop. Phone may still overlay the dimmed page. | design | Yes — Anders sent a screenshot | He likes the chat window; color and the blocking orb were the bugs. Production `/app` and `/read` chrome stay untouched. |
| 2026-08-21 | `/lab` voice uses `useVoiceSession` + the signed-in Supabase `access_token` as `Authorization: Bearer`, same as production Ask. No token means a signed-in notice and idle. Desktop Ask is a ChatGPT-style chat window. The phone keeps the orb overlay. | design | Yes — Anders rejected the decorative orb pane | The first lab Ask chrome faked listening with timers, then a live POST without a bearer 401'd. Anders will try /lab while signed in on tinct.app. Production Reader chrome stays untouched. |
| 2026-08-21 | Ship the locked reading chrome as a noindex `/lab` demo. Production `/app` and `/read/:slug` stay on Chat / Feed / Cast. | design | Yes — Anders locked the experience | Anders wants to see the new chrome while production stays as it is. Lab is Odyssey Book 1, Butler prose, Ask-only desktop pane, phone conversation orb, quiet word follow from real manifest timings only. |
| 2026-06-10 | Modern-en similarity gate (`books/classify-modern-en.py --gate`) is a blocking QA step before any modern-da or audio work. | content | Yes — approved by Anders | The 2026-05 mechanical-modernization failure (539 fake chapters) happened because the quality audit was a one-off postmortem in /tmp. A committed, scripted gate makes the failure class unable to recur silently. |
| 2026-06-10 | Finish-to-publish WIP discipline: no new source parsing while a staged book is blocked only on content work this lane can do. | scope | Yes — approved by Anders | 22 WIP books and near-zero publications; the last mile (threads, onboarding, audio) is where books stall. Default next task = closest-to-publishable per wip_inventory.py. |
| 2026-06-10 | Parallel chapter-render harness (one subagent per chapter → fragment files → independent verification → assembly → per-chapter gate) is the default method for bulk modern-en work. Don Quixote remaining 51 LIGHT chapters are the default pull-work until the book passes the gate. | content | Yes — approved by Anders | Prototype on DQ ch 9–28 batch: 8 chapters in ~6 min wall-clock vs 8 sequential sessions; all dropped from 0.87–0.94 similarity to 0.54–0.66, truncation-clean. |
| 2026-06-10 | AI-crawler policy (Plan 5.7): ALLOW reputable AI search bots (OAI-SearchBot, ChatGPT-User, PerplexityBot, Claude-User) and training bots (GPTBot, ClaudeBot, Google-Extended) — Cloudflare edge opened by Anders; worker UA-list + robots.txt + llms.txt to follow. /data/ and /api/ stay blocked; junk scrapers (Bytespider, CCBot) stay blocked. | external | Yes — Anders decided, opened Cloudflare | Tinct's customer asks AI assistants how to read the classics; being citable there is the growth channel incumbents (ad-funded) cannot follow. Texts are public domain; moat is the product, not the corpus. |
| 2026-06-15 | Frederick Douglass *Narrative*: cleaned original-en — removed 12 parser artifacts (10 trailing CHAPTER headers + stray APPENDIX/THE END), and merged the Appendix opening poem (4 stanzas) and parody hymn (13 stanzas) into one paragraph each, matching the book's existing single-paragraph verse convention (cf. ch8 Whittier). original-en 189→162 paras. | content | No | Artifacts are not reader text. Merging multi-stanza poems is the established storage convention and keeps the verse-heavy Appendix from blowing the similarity gate's identical-paragraph cap. |
| 2026-06-15 | Douglass modern-en racial-terminology policy: preserve Douglass's OWN period terms verbatim (colored, negro, mulatto, black); keep ALL quoted slaveholder speech verbatim including the slur (Auld, Lanman, the shipyard apprentices) as documentary record; modernize only sentence structure and non-racial diction. | content | Flagged to Anders for confirmation | Most faithful, least presumptuous default for a slave narrative — softening the slur in slaveholders' mouths would sanitize exactly what Douglass set out to expose. Awaiting Anders's sign-off on the policy. |
| 2026-06-15 | Douglass modern-en complete and PASSES the gate: weighted sim 0.658, all 12 chapters REAL, 0 LIGHT/MECHANICAL, identical-long 3.9%, 0 truncation, 0 paragraphs under 80%% word ratio. Built via the parallel chapter-render harness; ch3/10/11 needed an aggressive second pass after a too-timid first pass. | content | No | Per the canonical flow, gate must pass before modern-da/audio. |
| 2026-06-15 | Douglass modern-da complete: all 12 chapters translated from modern-en via the parallel harness; 162/162 aligned across all three editions; byte-identity vs modern-en 0.0%% (gate <5%%); 0 English-residue paragraphs; »...« dialogue throughout; verse/scripture/marriage-certificate/footnotes all rendered into Danish; racial terms documentary (farvet/neger/mulat/sort), quoted slaveholder slurs kept faithful. Anders confirmed the faithful racial-terms policy for both editions. | content | No | Editions (original-en cleaned, modern-en gated, modern-da audited) are done; remaining package = onboarding, threads, registry. |
| 2026-06-15 | Douglass content package complete: onboarding (en+da, with Garrison/Phillips/Fuller acclaim fact-checked verbatim against 2+ sources each), threads (12 chars, per-chapter en+da summaries), staged FREDERICK_DOUGLASS registry constant (not in BOOKS). HTML onboarding mirror skipped — no recent book (WoN, AK) ships one; the checklist item is legacy. | content | No | Editions + onboarding + threads + staged registry done. Remaining for Codex publication: Kokoro English audio, BOOKS array, taxonomy, app verify, deploy. |
| 2026-06-16 | Architecture slice 3 (readerSession = single persisted-tuple source) finished as bounded "finish + verify" (NOT the full render-inversion). Added `commitReadingProgress` in positionSync.ts as the single progress-write point; routed `useReadingPosition` (steady-state) and `useReaderController` (chapter-advance, explicit pre-advance chapter) through it; guarded `handleBookComplete` by `readerSession.location.bookId` (no status gate); documented the fresh-start reset as an intentional regression-bypass. New `positionSync.test.ts` pins commitReadingPosition + commitReadingProgress (224 tests pass). | architecture | No | Audit showed the write side was ~90% done already; the doc's "Not started" was stale. Bounded scope avoids touching the fragile render/pagination/mobile-sync inversion. Anders chose bounded scope. |
