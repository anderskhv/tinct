# Tinct Decision Log

> **Language scope — 2026-09-21:** English is the current delivery strategy. Danish is no longer a launch, publication, translation, audio, QA or marketing requirement; older Danish tasks below are superseded. Keep future localization straightforward without starting another language rollout. See [the approved language strategy](STRATEGY.md#language-scope). Existing assets and historical findings are preserved; this note does not change shipped behavior.

Live log per CLAUDE.md. Format: one row per decision, logged at the moment it's made.

| Date | Decision | Category | Escalated? | Reasoning |
|---|---|---|---|---|
| 2026-09-21 | English is the current strategy. Danish is no longer a delivery or launch requirement; older Danish translation, audio, onboarding, QA, SEO and marketing tasks are superseded. Preserve existing assets and make current work easy to localize later; no next language is committed. | scope | Yes — Anders, this conversation | Clarifies the September 11 pause as a strategy decision. See [Language scope](STRATEGY.md#language-scope). Documentation only; no implementation or deployment authorized by this update. |
| 2026-09-19 | Talk voice is xAI `altair` (male) instead of the provider default `eve`. Constant `GROK_VOICE` in `app/src/voice/grokConfig.ts`. | design | Yes | Anders found `eve` British-sounding; picked `altair` from TTS samples of the built-in voices. |
| 2026-09-18 | Talk moves to xAI Grok native speech-to-speech (`grok-voice-latest`, voice `eve`, WebSocket with single-use client secrets minted by the Worker). The GPT Live → Sol delegation chain, the Realtime trial path, `/lab/voice` and `?voiceTrial=` are removed; typed chat, Explain and audiobook narration providers are unchanged; `OPENAI_API_KEY` stays for source research. Prompt is the approved minimal one plus two lines for demonstrated issues (over-long first answer, lookup narration). Rollback: revert the commit. Verification and measured latency (≈360–440 ms speech end → first audio) in `docs/voice-grok-2026-09-18.md`. | architecture | Yes | Anders approved the switch and deploy in one pass (2026-09-18). Grok answers directly from audio with ~0.35 s to first audio; the old chain needed a second model to author answers. |
| 2026-09-18 | Audio-align helper v7 (new pin, v1–v6 frozen) keeps v6 heading scoring and fixes `restore_source_tokens` so prefixes of prose are not heading-stripped. Canary 35323671028 / Hume modern-en/7 p14 (`45. I shall add…`) is the fixture. Gate stays 0.85. notes-from-underground/modern-en/17 fortnight/Fortnite and cellar/seller are ASR-vs-script near-misses; do not add brand/homophone maps; park that chapter. No GPU / canary dispatch in this pin. | architecture | No | v6 `clean_text` emptied the prefix `45. I` during incremental restore (`source='I'` at index 1). That is a helper/restore bug, not bad chapter data. Tightening the heading regex would miss other prefix crashes (`2. A…`, `WORKING MEN OF…`). |
| 2026-09-18 | Audio-align helper v6 (new pin, v1–v5 frozen) strips unspoken whole-paragraph chapter/section headings from the *expected* scoring stream the same way v5 strips speaker/stage/WEB cues. Manifesto modern-en ch4 p21 (`_C. German, or “True,” Socialism_`) is the 0.80→pass fixture. The 0.85 gate, timestamps and source-word emission do not change. No GPU / canary / active-batch change in this pin. Greek transliteration in Poetics ch22 is an ASR-vs-script miss, not a safe heading-style strip. | architecture | No | Canary #15 / 35321419398 rejected a heading-only paragraph at 0.80; lowering 0.85 would hide real misses, and inventing English for Greek would too. |
| 2026-09-17 | Audio-align helper v5 (new pin, v1–v4 frozen) strips ALL-CAPS speaker labels, Enter/Exeunt ALL-CAPS names, `[_…_]` wrappers, WEB `[of]`/`[and]` brackets and superscript verse markers from the *expected* scoring stream so ASR comparison matches what was heard. Source-word emission, interpolation and the 0.85 gate do not change. Known-bad chapters from canary 35204983320 are quarantined until this pin is on `codex/audio-runner-reviewed`. No GPU relaunch in this change. | architecture | No | Zero-pass canaries were `observed_alignment_below_85_percent` on printed cues ASR never says; lowering 0.85 would hide real misses. |
| 2026-08-28 | Bible/audio word follow ships via R2 `words.json` sidecars generated with `app/tts/generate-words-sidecar.py` (faster-whisper + edition-token alignment). App order: manifest `words` → sidecar → client measurement last resort. Kokoro/upload scripts include `words.json`. | content | No | Anders asked for the proper long-term Play sync fix, not MVP linear timings. |
| 2026-08-28 | Cloud/GitHub automated deploy parked. Anders deploys from Mac (`npm run deploy` from `app/`). `docs/cloud-deploy.md` and `.github/workflows/deploy.yml` exist for later; no Cloudflare tokens configured yet. | deploy | No | Anders not at his Mac; lid-shut until he adds Workers deploy token to GitHub/Cursor when ready. |
| 2026-08-28 | `/lab/phone` reader fixes on PR #20: last-line remeasure, selection popup placement, tree scroll-to-current chapter. Tree concept unchanged. | fix | No | Three phone reader bugs; Play highlight alignment handled separately via words.json / measurement. |
| 2026-08-27 | `/lab/phone` Bible TOC is Tinct’s books-first tree (Testament → section → book → named chapter rows), not a Kindle 1–50 keypad. Read state is real: empty circle unread, solid tan current/in-progress, checkmark for a finished book. Progress text like “12 of 39 books” never prints “0 of N”. Only one book stays open. One EB Garamond stack in the tree. Production `/app` TocOverlay is unchanged. | design | Yes — Anders rejected the live grid and locked the concept mock | He cannot see what he has read on a number pad. The concept already had the marks. |
| 2026-08-25 | Bible early-reset recovery prefers newest reading-log lastReadAt / chat location over monotonic highestCompletedChapter+1. Writing the recent earlier chapter is not history-regression. | architecture | No | Production: Jeremiah 18 (763) teleported to James 1 (1147) after a Genesis 1 blip because recovery used progress high-water and then blocked writing 763. Recency is the place they were; high-water is only a fallback. /lab chrome untouched. |
| 2026-08-21 | `/lab` is three exclusive states from one IBM Plex Mono status line: Reading · Book 1, Hearing · Book 1, Talking · tap the circle to stop. Hearing replaces the chapter with a lyrics stage (real Butler MP3s; word mark only from manifest/sidecar words). Talking keeps the desktop book visible and only the filled circle alive. Stopping Talk returns to Hearing if that is where they came from. Production `/app` and `/read` chrome and the in-car voice brief stay untouched. | design | Yes — Anders locked the 21 Aug wireframes | The ears cannot hear and talk at once. Listen-as-highlighted-page was the wrong surface. |
| 2026-08-21 | `/lab` typed + voice send the full current chapter with numbered paragraphs and a hard spoiler rule (nothing after this chapter). Lab does not use the in-car 20–30s / resume-audiobook brief. Production `buildVoiceInstructions` used by AudioStrip is unchanged. | design | Yes — Anders demonstrated the spoiler + paste-the-paragraph failure | /lab reusing nearby-paragraph in-car context made the model invent the Odyssey ending and refuse to read Book 1 §2. Lab is a conversation beside the page. |
| 2026-08-21 | `/lab` Listen plays real Odyssey Book 1 Butler MP3s via chapter manifest + `/api/audio-file`. Highlight the playing paragraph; word-level only from manifest `words` or `words.json` sidecar. No invented linear timings. No recut MP3s. | design | Yes — Anders said Listen was a fake clock | The first lab follow walked durations without audio. |
| 2026-08-21 | Desktop `/lab` Ask stays a ChatGPT-style chat window on paper `#ece7db`. Conversation lives in the composer icon (immediate connecting/alive state). No full-page orb overlay on desktop. Phone may still overlay the dimmed page. | design | Yes — Anders sent a screenshot | He likes the chat window; color and the blocking orb were the bugs. Production `/app` and `/read` chrome stay untouched. |
| 2026-08-21 | `/lab` voice uses `useVoiceSession` + the signed-in Supabase `access_token` as `Authorization: Bearer`, same as production Ask. No token means a signed-in notice and idle. Desktop Ask is a ChatGPT-style chat window. The phone keeps the orb overlay. | design | Yes — Anders rejected the decorative orb pane | The first lab Ask chrome faked listening with timers, then a live POST without a bearer 401'd. Anders will try /lab while signed in on tinct.app. Production Reader chrome stays untouched. |
| 2026-08-21 | Ship the locked reading chrome as a noindex `/lab` demo. Production `/app` and `/read/:slug` stay on Chat / Feed / Cast. | design | Yes — Anders locked the experience | Anders wants to see the new chrome while production stays as it is. Lab is Odyssey Book 1, Butler prose, Ask-only desktop pane, phone conversation orb, quiet word follow from real manifest timings only. |
| 2026-06-10 | Modern-en similarity gate (`books/classify-modern-en.py --gate`) is a blocking QA step before any modern-da or audio work. | content | Yes — approved by Anders | The 2026-05 mechanical-modernization failure (539 fake chapters) happened because the quality audit was a one-off postmortem in /tmp. A committed, scripted gate makes the failure class unable to recur silently. |
| 2026-06-10 | Finish-to-publish WIP discipline: no new source parsing while a staged book is blocked only on content work this lane can do. | scope | Yes — approved by Anders | 22 WIP books and near-zero publications; the last mile (threads, onboarding, audio) is where books stall. Default next task = closest-to-publishable per wip_inventory.py. |
| 2026-06-10 | Parallel chapter-render harness (one subagent per chapter → fragment files → independent verification → assembly → per-chapter gate) is the default method for bulk modern-en work. Don Quixote remaining 51 LIGHT chapters are the default pull-work until the book passes the gate. | content | Yes — approved by Anders | Prototype on DQ ch 9–28 batch: 8 chapters in ~6 min wall-clock vs 8 sequential sessions; all dropped from 0.87–0.94 similarity to 0.54–0.66, truncation-clean. |
| 2026-06-10 | AI-crawler policy (Plan 5.7): ALLOW reputable AI search bots (OAI-SearchBot, ChatGPT-User, PerplexityBot, Claude-User) and training bots (GPTBot, ClaudeBot, Google-Extended) — Cloudflare edge opened by Anders; worker UA-list + robots.txt + llms.txt to follow. /data/ and /api/ stay blocked; junk scrapers (Bytespider, CCBot) stay blocked. | external | Yes — Anders decided, opened Cloudflare | Tinct's customer asks AI assistants how to read the classics; being citable there is the growth channel incumbents (ad-funded) cannot follow. Texts are public domain; moat is the product, not the corpus. |
| 2026-06-15 | Frederick Douglass *Narrative*: cleaned original-en — removed 12 parser artifacts (10 trailing CHAPTER headers + stray APPENDIX/THE END), and merged the Appendix opening poem (4 stanzas) and parody hymn (13 stanzas) into one paragraph each, matching the book's existing single-paragraph verse convention (cf. ch8 Whittier). original-en 189→162 paras. | content | No | Artifacts are not reader text. Merging multi-stanza poems is the established storage convention and keeps the verse-heavy Appendix from blowing the similarity gate's identical-paragraph cap. |
| 2026-06-15 | Douglass modern-en racial-terminology policy: preserve Douglass's OWN period terms verbatim (colored, negro, mulatto, black); keep ALL quoted slaveholder speech verbatim including the slur (Auld, Lanman, the shipyard apprentices) as documentary record; modernize only sentence structure and non-racial diction. | content | Flagged to Anders for confirmation | Most faithful, least presumptuous default for a slave narrative — softening the slur in slaveholders' mouths would sanitize exactly what Douglass set out to expose. Awaiting Anders's sign-off on the policy. |
| 2026-06-15 | Douglass modern-en complete and PASSES the gate: weighted sim 0.658, all 12 chapters REAL, 0 LIGHT/MECHANICAL, identical-long 3.9%, 0 truncation, 0 paragraphs under 80%% word ratio. Built via the parallel chapter-render harness; ch3/10/11 needed an aggressive second pass after a too-timid first pass. | content | No | Per the canonical flow, gate must pass before modern-da/audio. |
| 2026-06-15 | Douglass modern-da complete: all 12 chapters translated from modern-en via the parallel harness; 162/162 aligned across all three editions; byte-identity vs modern-en 0.0%% (gate <5%%); 0 English-residue paragraphs; »...« dialogue throughout; verse/scripture/marriage-certificate/footnotes all rendered into Danish; racial terms documentary (farvet/neger/mulat/sort), quoted slaveholder slurs kept faithful. Anders confirmed the faithful racial-terms policy for both editions. | content | No | Editions (original-en cleaned, modern-en gated, modern-da audited) are done; remaining package = onboarding, threads, registry. |
| 2026-06-15 | Douglass content package complete: onboarding (en+da, with Garrison/Phillips/Fuller acclaim fact-checked verbatim against 2+ sources each), threads (12 chars, per-chapter en+da summaries), staged FREDERICK_DOUGLASS registry constant (not in BOOKS). HTML onboarding mirror skipped — no recent book (WoN, AK) ships one; the checklist item is legacy. | content | No | Editions + onboarding + threads + staged registry done. Remaining for Codex publication: Kokoro English audio, BOOKS array, taxonomy, app verify, deploy. |
| 2026-06-16 | Architecture slice 3 (readerSession = single persisted-tuple source) finished as bounded "finish + verify" (NOT the full render-inversion). Added `commitReadingProgress` in positionSync.ts as the single progress-write point; routed `useReadingPosition` (steady-state) and `useReaderController` (chapter-advance, explicit pre-advance chapter) through it; guarded `handleBookComplete` by `readerSession.location.bookId` (no status gate); documented the fresh-start reset as an intentional regression-bypass. New `positionSync.test.ts` pins commitReadingPosition + commitReadingProgress (224 tests pass). | architecture | No | Audit showed the write side was ~90% done already; the doc's "Not started" was stale. Bounded scope avoids touching the fragile render/pagination/mobile-sync inversion. Anders chose bounded scope. |
| 2026-09-07 | Lab chat history is per registry book and shared with the classic reader: one row `chat-history:{bookId}` (localStorage mirror `tinct:chat-history:{bookId}`, cloud `user_data` row via versioned `commit_user_data` with expected rev, conflict adopts the server row and merges). The retired per-biblical-book blob `tinct:chat-history:lab` (+ Worker KV via `/api/lab-chat-history`) is migrated once (order by timestamp, dedupe by id) and removed from the device; the Worker GET stays for that migration. The Bible is one thread (`bible`, sequential chapter numbers, chapter-label dividers), reversing the earlier "Bible is not one thread" rule. Chat opens on the full history windowed to the newest 40 turns (older on scroll-up); the companion still gets the last 20 turns. Composer autofocus: desktop layout with `(pointer: fine)` only; never on the phone. | architecture | No | Owner asked for the whole per-book conversation to load in the lab as in the classic reader, synced through the same storage layer; the classic key makes old conversations appear without a data move. Phone autofocus popped the keyboard over the thread. |
| 2026-09-11 | Bible `modern-en` and `modern-da` withdrawn from the registry and their text assets removed from the app (chief-of-staff session, branch `claude/happy-bardeen-jy142i`). The 2026-09-11 translation audit confirmed `modern-en` reproduces the copyrighted NIV (2011) verbatim at every sampled location; `modern-da` is a Danish rendering of that same English and carries the same derivative exposure. Saved locations in either edition migrate to `web-en` at the same chapter and paragraph via `app/src/data/withheldEditions.ts` (applied in the readerSession reducer on both local and cloud restore) — never reset to chapter 1. Bible `kjv-en` and `web-en` released from the `missing_audio` hold in `audioAvailability.json` at the same time, because the lab edition picker lists only discoverable editions and the Bible would otherwise have had zero pickable editions (16 of 1,189 chapters lack audio; the reader already degrades per chapter). Lab Compare default for the Bible moved to `web-en`. Bible character-card offsets for `modern-en` dropped. Consequences accepted: no Danish Bible until a public-domain Danish translation is sourced; Bible audio for `modern-en` in R2 is now orphaned and should be deleted by the audio owner (it narrates the same NIV-derived text). The staged BSB replacement (`books/staged-replacements/bible-bsb/` on `claude/upbeat-brown-cuttkn`) is the follow-up, under a new `bsb-en` key rather than reusing `modern-en`. | content | Anders said "yes on bible"; Danish extension and KJV/WEB hold release decided alone | Copyright exposure is live in production; containment first, replacement second. Position preservation is invariant #1. |
| 2026-09-11 | Library "N% read" rule: position-derived paragraph progress for every book except the Bible; the Bible uses the paragraph-weighted set of finished chapters plus the current chapter's fraction. The monotonic `highestCompletedChapter` high-water override in the lab progress math is retired (it produced "97% read" at Proverbs 18). Plan in `docs/library-reader-fixes-plan-2026-09-11.md`, not yet implemented. | design | Yes | Anders chose the simplest split: only the Bible is read non-linearly, so only it needs the finished-set model; linear books keep the smooth position number. |
| 2026-09-11 | Danish is out of scope for now: no new Danish content, Danish audio, or Danish QA until Anders reopens it. Existing `modern-da` editions stay as they are. | scope | Yes | Anders, coordinator thread. Focus the remaining effort on the English reading experience. |
| 2026-09-11 | Audio word-highlighting: until a chapter has verified word timings, the reader keeps paragraph-level follow (already the built-in fallback in `labFollow.ts`). Word-level coverage is driven to completion by the GPU runs; no chapter is held back from audio for lacking word timings. | design | Yes | Anders, coordinator thread. Paragraph follow is acceptable interim behaviour; word follow arrives edition by edition. |
| 2026-09-11 | Audio regeneration follows translations: any edition whose text is replaced or repaired (Bible, Faust, Magna Carta, Jane Eyre, Moby-Dick and the rest of the audit list) gets its recordings and word timings regenerated only after the new text is final. Do not align or record against text that is about to change. | scope | Yes | Anders, coordinator thread. Sequencing rule so audio spend is never wasted on text that will be replaced. |
| 2026-09-11 | Voice surface locked: monochrome dotted-sphere orb on a 2D canvas (no accent colour, no mono/uppercase type, round icon buttons); phone keeps the full-screen call, desktop gets the orb in the companion panel with a minimize-to-pill state. Brief: `docs/design/voice-surface-handoff-2026-09-11.md`, canvas https://claude.ai/code/artifact/ac90dc42-aa66-43ec-8827-a11ea722fe92 | design | Yes | Anders reviewed three rounds of mockups; rejected coloured orbs, mono type and the underlined transcript link; chose Globe + panel/pill over the theatre variant. |
| 2026-09-11 | Modern-English translation scope is five books: The Odyssey, Hamlet, Macbeth, Fear and Trembling, Meditations. Meditations replaces Kant's Groundwork. Meditations runs as its own thread: verify the human translation's identity, provenance, completeness and reuse rights first; stage a suitable public-domain translation unchanged if it already meets the accessibility standard; otherwise modernise from one documented source, Book II first as the pilot, paragraph-level independent review before any further book. Voice rules: compact, self-addressed, no motivational or explanatory turn; stable renderings for nature, reason, ruling faculty, providence, common good; keep numbering. | content | Yes | Anders, coordinator thread, 2026-09-11 afternoon. Translations otherwise remain on hold (Bible BSB integration, Faust/Magna Carta/As You Like It/Henry IV re-ingests, Jane Eyre/Moby-Dick patches). English only; Danish is out. |
| 2026-09-11 | Coordinator rule (correction from Anders): pushed work is not approval to merge another agent's changes. The coordinator merges only branches it was asked to land or that its own child sessions produced under a mandate; another owner's branch is landed only on that owner's or Anders's say-so. The About page merge `a2c61c34` made before this rule is reported to its owning agent, not reverted. Content tasks (Meditations) are content-only: no merges, deploys, app code, registry, library UI or audio. | scope | Yes | Anders, coordinator thread, 2026-09-11 13:05 UTC. Pattern to avoid: treating "pushed" as "ready to ship". |
| 2026-09-11 | Voice surface implemented on `claude/voice-surface-20260911` per the locked brief. Two existing test assertions changed to match it (Transcript button reads "Transcript", End reads "End" with the accessible name "End conversation"); the phone transcript bar keeps its structure but drops the accent and mono/uppercase; the Voice action lab diagnostic overlay is hidden while a V2 call is open; desktop Talk uses the phone's call model (intent, anchor, timeout) with the panel/pill as its rendering. Verification: `docs/voice-surface-2026-09-11.md`. | design | No | The brief retires the underlined link and the uppercase pill buttons, and says no mono/uppercase anywhere on the surface; the diagnostic overlay sat across the panel's controls and intercepted the pill. |
| 2026-09-11 | Audio word-timing tokenizer normalisation approved (coordinator decision within Anders's audio mandate): the aligner's expected-token side may strip text markup that is never spoken — underscore emphasis, bracketed footnote markers such as `[28]` / `[Greek: …]`, spaced ellipses and double hyphens — and may join recogniser-split hyphen compounds (`wage-labour`) for comparison. The 0.85 paragraph threshold does not move; no timestamp or confidence is synthesised; the change ships as a new pinned helper revision with tests and a re-measured canary (Macbeth ch1 must still verify). Missing or defective recordings (class 4, spelled-out speaker names; 4 missing paragraph files) are re-recording work and wait for a later run. | architecture | No | Run 1 rejected 39 chapters whose speech was recognised correctly but whose text carried markup the tokenizer compared literally; that is a comparison artefact, not an alignment failure, so fixing the comparison is inside the "no invented timings" rule. |
| 2026-09-18 | Fish Audio narration pilot built as a contained opt-in (`?narration=fish` in the V2 reader; Odyssey Book 1, `original-en` and `modern-en` only). Server-side Fish access via Worker secret `FISH_AUDIO_API_KEY`; audio generated on demand through `POST /v1/tts/stream/with-timestamp`, validated, and cached in R2 under `narration/fish/` with an explicit edition→recording map; identity hashes the exact narration text plus provider, model, voice and every audio-affecting setting; a text change is a new recording. Kokoro paths, the production reader and edition files untouched. Design and findings: `docs/fish-audio-pilot-2026-09-18.md`. | architecture | No | Regenerating whole audiobooks after each translation repair is the operational cost the pilot is meant to remove; on-demand generation with content-addressed caching removes it without touching existing recordings. |
| 2026-09-18 | Word timings for Fish narration come only from the provider's own alignment mapped onto the reader's tokens (`alignSegmentsToTokens`), with the match ratio stored beside the words and the existing 0.85 sentence-level fallback. Below the threshold the recording is published without words. No estimated or linear timings, ever. | architecture | No | Same rule as the Kokoro sidecars ("no invented timings"); Fish's rendered docs omit the timestamp endpoints, so the OpenAPI document was verified directly before relying on them. |
| 2026-09-18 | Pilot voices are two Fish public-library narrator voices chosen for audition only (`bbb58d69…` "Nathan", `f6a19fe5…` "Abby"), configured as Worker vars so they can be swapped without code. Public-library voices are user-uploaded clones with takedown/retirement fields; a production narrator should be a Tinct-owned Voice Design voice or an explicitly licensed one. Nobody's voice is cloned. | external | Yes | Fish Terms tie commercial rights to paid use of verified voices you own; a takedown would strand cached recordings. Needs Anders's decision before wider rollout. |
| 2026-09-18 | Narration spending ceiling defaults: 200,000 text bytes/day (≈$3) and 1,000,000/month (≈$15) at Fish's $15 per million bytes, enforced in the Worker before every generation; per-user 40 ensure calls/min; three-failure 60 s breaker; two bounded provider retries. Paid testing has not started: no Fish key exists in the environment. | spend | Yes | The brief authorises a contained pilot, not bulk generation; the key and the ceiling are Anders's to grant. |
| 2026-09-18 | Live Fish probe on the free tier (`s2.1-pro-free`, $0) after Anders added the key: word timings covered 113/113 and 362/362 display tokens on Odyssey Book 1 paragraphs 0 and 18 (validation clean), but first audio arrives only when generation completes — 15–22 s for the opening paragraph, 39–52 s for paragraph 18 (generation ≈ 0.4–0.56 × audio length). The paid tier answered 402: the Fish account has no API credit (managed separately from platform credit). `FISH_AUDIO_API_KEY` installed as the production Worker secret. Proposal: synthesise by sentence group (~300 chars) with chunk clips mapped back to paragraphs, before wider rollout. Evidence: `artifacts/fish-narration-pilot-2026-09-18/live-probe-free-tier-2026-09-18.json`. | spend | Yes | Chunking changes the cache unit and the reader's clip model; API credit is Anders's to add; free-tier output is personal-use only and was not cached in production. |
| 2026-09-19 | Fish narration extended per Anders: synthesise by sentence group (≤300 chars, chunker v1 pinned in the cache), scope widened to the featured shelf (16 books, English editions, all chapters), reader prefetches the chapter opening on arrival and the next chapter's opening near the end, chapter 1 of every featured book to be warmed for both voices (≈$21 at list price), spend ceilings raised to 2M bytes/day and 10M/month, and PR #110 to merge once these are in. Warm route gated by a Worker secret `NARRATION_ADMIN_TOKEN`. | spend | Yes | Anders approved credits, chunking and warm-up on 2026-09-19 ("we want a smooth start for each book"); the measured 15–52 s first-audio wait made whole-paragraph synthesis unacceptable. |
| 2026-09-18 | Featured book description on the library page is a five-line preview at every breakpoint with a fixed affordance row (Expand only when cut) and a one-line title fitted by font size, so the block is the same height for every book. The 8/6-line interim clamp is replaced. | design | No — owner approved five lines and the stable layout on 2026-09-18 | Owner's original ask; two-thirds of descriptions exceed five lines at 320pt, so Expand is offered instead of a copy pass. Documented in docs/reader-library-fixes-2026-09-18.md. |
| 2026-09-18 | The Continue-reading card's "so far" block is always laid out at the same size; until a recap exists it shows a zero-cost aside from the catalogue (chapter name, author, or chapter count) instead of being hidden. | design | No — owner approved the stable recap layout on 2026-09-18 | Hiding the block trimmed the card but made its height depend on whether the AI recap had been generated; the aside keeps the card constant and never empty. |
| 2026-09-19 | Fish narration pilot released to production: PR #110 squash-merged (`ba06996a9`), deployed by the `deploy` workflow, verified on tinct.app by API and by a muted anonymous browser check, then chapter 1 of the featured shelf pre-generated for both voices through the admin warm route. Opt-in only (`/reader?narration=fish`); Kokoro stays the default. | deploy | Yes | Anders: "when this is ready you can merge #110" and "generate chapter 1 of each of the featured books". Merge waited for green CI on the merge head; the deploy is the workflow, never raw Wrangler; the warm-up runs through the deployed Worker under the same locks, validation and ceilings as reader requests, ≈$21 for both voices within the 2M-byte daily ceiling. |
| 2026-09-19 | Narration validator: the slow-pace bound (4 chars/s) now carries a fixed 4 s pause allowance (`NARRATION_PAUSE_ALLOWANCE_SECONDS`), and validation failures report the measured duration and character count. | architecture | No | The chapter-1 warm-up refused every bracketed stage direction in Hamlet ("[Exit.]", "[Enter Ghost]", "[Enter Horatio and Marcellus]") as audio_too_long_for_text: seven characters allowed 1.75 s, less than any rendering with lead-in and lead-out silence. Silence around a line does not scale with its length. A full 300-character chunk moves from 75 s to 79 s, far above any real reading, so the guard against runaway or mismatched audio stands. |
| 2026-09-19 | Narration spoken text drops square brackets (`spokenText` in `narrationCore.ts`); display tokens keep them and the aligner maps timings back through its punctuation-blind match. Chunk identities of bracketed lines change, so their earlier blobs are orphaned, never served. | content | No | On production Fish returned audio with zero word segments for Hamlet's "[Exit.]" (0.76 s) and "[Enter Horatio and Marcellus]" (3.2 s and 12.2 s for the same text) while the plain lines beside them aligned 10/10 and 5/5: the provider treats `[...]` as a paralinguistic tag, not words. Stage directions must be read plainly, as in any audiobook of a play. Parentheses are left alone until there is evidence. |
| 2026-09-19 | Narration map writes merge by identity hash and never shorten the listed prefix; a chunk that became valid while we synthesised it is kept, not overwritten; the lock wait polls the map alone; R2 reads retry once; the warm script stops a target after five rounds without progress or 120 requests. | architecture | No | Two Meditations targets (voice b, a 23-chunk paragraph) looped for over an hour on production: the ready prefix wandered between 19 and 22 and fell to 11, each request regenerating chunks. The map was rewritten from the *validated* prefix after every chunk, so one transient miss on chunk k orphaned every chunk after it; content-addressed keys meant a second rendering of the same chunk could tear its audio/meta pair; and the lock wait re-validated every chunk per 750 ms poll, hundreds of subrequests per wait. A single uncontended request advanced the same paragraph from 12 to 21 in 64 s, so generation itself was sound. |
| 2026-09-19 | The narration chapter listing reports the map alone (one R2 read per paragraph, identity-checked, `listedOnly: true`); blob validation stays on the ensure/warm path, which runs before anything plays. | architecture | No | Once The Republic's 430-paragraph Book I was cached, the validating listing needed about 1,300 R2 reads and timed out past 60 s (the Worker allows 1,000 subrequests per request); the warm script would have failed on that book. Only the warm script and admin checks consume the listing; the reader uses ensure. |
| 2026-09-19 | Before synthesising a chunk the Worker probes the recording by its content-addressed identity, independent of the map, and adopts it if valid; paragraphs completed by probes alone get their map rewritten once (`probed` in the response). | architecture | No | In the chapter-1 re-run 41 of 250 batches reported `raced`: after a synthesis the pre-write check found the chunk already valid although the request's initial read had not, with no other generator running. Whether the map or the last-written blob lagged, the identity key does not depend on the map, so a two-read probe replaces a paid synthesis. |
| 2026-09-19 | Explain popover redesign locked (mock-up v8): opener is one sentence ≤25 words rendered whole, card height follows the text, never clamped; "More" on its own line in EB Garamond 14px accent; Chat/Talk are 34px icon-only glass circles centred; one streamed call, not two. Brief: `docs/reader-polish-2026-09-19.md`. | design | Yes | Anders rejected clamped text, floating +, hairline fold and label rows in turn; the plain "More" line in the body face was the version he approved. |
| 2026-09-19 | Speculative Explain fetch fires when a selection starts (400ms stable), not when the popup opens; it is never charged to the user's quota. Quota is charged only on an Explain tap. Cancelled on dismiss/Highlight/Copy; skipped offline. | spend | Yes | Anders: speculation cost is on him; user pays only for what they ask for. |
| 2026-09-19 | Dictionary miss ("No definition found") falls through to the Explain card with the word as passage. Dictionary hits get no extra Explain button for now. | design | Yes | No user should hit a dead end; wrong-sense corner case accepted. |
| 2026-09-19 | Compare Version leaves the super menu on phone and desktop; it becomes a toggle in Reading settings. Phone swipe to Compare stays. | design | Yes | Menu had too many rows. |
| 2026-09-19 | Paused audio transport auto-dismisses on page turn; while audio plays it always stays visible. | design | Yes | Currently needs the X. |
| 2026-09-19 | Phone chat keeps the "← Back to book" pill visible with the keyboard up; tapping it dismisses the keyboard and closes chat in one go. | design | Yes | Two-step exit was annoying. |
| 2026-09-19 | Verse-number highlight and hyphen page-flip issues are v2 `LabPassage.tsx` problems; the legacy-reader Chromium diagnosis (clean marks, `::selection` slit after `.verse-num` margin) does not explain the phone screenshots. On-device diagnosis before any fix; edge-flip re-arm must require leaving the zone, not a timer. | architecture | No | First diagnosis targeted the wrong reader; recorded so the next session does not repeat it. |
| 2026-09-19 | Verse highlight root cause: v2 paints marks with `::highlight` per text fragment, so the small raised verse number painted a small raised box (notch) and its margin an unpainted slit. Fix: `.lab-verse-unit` is inline; the number's word span keeps a real background in the highlight colour under the fragment paint; margin replaced by painted letter-spacing on the preceding space. The `:has()` one-band rule is removed. | design | No | Verified on the dev server at 3x (Genesis 1 "² And"): one rectangle. Superior figures rejected, the served fonts lack `sups`. Needs one look on a real iPhone. |
| 2026-09-19 | Drag-to-select edge turn: one page per visit to the edge (armed flag, reset when the finger leaves the zone), zone 24→32px, hold 700→450ms. The self-repeating 700ms timer is gone. | design | No | The repeat timer was the "super quick second flip". Hyphenated-last-word case unreproduced without a hyphenation dictionary; device log required before any change. |
| 2026-09-19 | Explain prefetch stays sign-in only (existing rule); anonymous readers get no speculation and no charge. The prefetch now fires 400ms after the in-progress selection settles, plus the existing popup-open fallback for desktop native selection. | spend | No | Anonymous speculation would spend API on readers who cannot open Chat anyway. |
