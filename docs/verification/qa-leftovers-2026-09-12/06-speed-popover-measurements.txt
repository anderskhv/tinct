Audio speed popover, measured at 1180x820 on the built dist (Playwright,
chromium), with the transport markup mounted so the shipped CSS lays it out
exactly as playback would.

/lab/desktop and /lab/reader (chrome V1, .lab.is-desktop)
  before: background-color rgba(0, 0, 0, 0)  background-image none
          -> fully transparent; the reader's paragraph lines
             (.lab-hearing-line at y 511-730) read straight through the
             slider and the 0.5x/1x/2x scale.
  after : background-color rgb(236, 231, 219) (var(--lab-paper)), 1px
          var(--lab-border), the panel's own shadow. Opaque in light, night
          and book themes, since --lab-paper is redefined per theme.

/reader (chrome V2, data-desktop-paging="true") - unchanged
  background-color rgb(236, 231, 219) before and after; the V2 desktop rule
  already painted var(--lab-paper) and still wins on specificity.

Phone chrome (390x844, 820x1180) - unchanged by design: the V2 panel fill
(rgba(250,247,239,.35-.44) over a 16px backdrop blur) is the shared surface
of the menu, chapter picker, composer and this popover.
