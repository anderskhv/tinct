# V2 reader fixes — 8 September 2026

Test entry: https://tinct.app/lab/reader?chrome=v2
Baseline: 420621c5, the handoff commit on origin/codex/lab-convergence.
Worktree: /Users/andershvelplund/Documents/Projects/Tinct-reader-stabilization

## Acceptance checklist

1. Chapter pill opens the contents on screen. The V2 backdrop filter had made its zero-height wrapper the containing block for the picker.
2. Audio highlights current words when timings are valid. Untimed paragraphs retain the existing subtle paragraph marker; ordinary line words have no highlight wash.
3. Play after pausing and navigating seeks to the first visible word when timed, without replaying a chapter-title clip. Changes to the audio foot preserve the visible page head, scoped to book, chapter, edition, and layout.
4. V2 chat displays saved date/time stamps. Older saved messages remain available through progressive loading; the history store and cloud merge are retained.
5. Chat microphone edits the draft through browser dictation. Starting/listening feedback and a stop toggle are explicit. Sending, leaving chat, switching books, or starting a call stops dictation; late recognition results cannot rewrite the draft.
6. Voice conversation entered from chat uses the real call lifecycle and keeps the transcript visible with mute/hang-up controls. The menu's Talk entry retains the dedicated call screen.
7. V2 voice waits silently for the companion result and requests the completed answer even if an earlier acknowledgement was spoken. This fixes the suppressed-answer path; end-to-end speech latency and intelligibility still require a live spoken test.
8. Reading settings leave clearance between the heading and the theme-card outlines.
9. Chat has Back to book and bottom spacing around the composer. Returning uses the existing interrupted-audiobook state.
10. Compare menu action changes between Compare Version and Main Version. Mobile swipe behaviour and the standby paginator remain intact.
11. Bible book selector remains above the scrolling chapter list, permitting a direct Jeremiah-to-Psalms jump.
12. Library, book-detail, and reader navigation retain the explicit chrome=v2 flag. Unflagged navigation keeps its old URLs.

## Verification

- Full Vitest suite, V1 DOM snapshot, CI build, and bundle verification are release gates.
- Regressions exercise playback seeking, compare menu toggling, timestamp/older-message rendering, recognition cleanup, and completed-answer delivery after an acknowledgement.
- Local mobile browser checks cover contents visibility, direct Bible-book selection, settings clearance, chat return/spacing, and a library-to-Romeo-and-Juliet round trip retaining V2.
- Production deployment identifiers and screenshots are reported with the release, after deployment.
- Plain TypeScript checking still has pre-existing failures; it is not the repository release gate.

## Timing-pipeline handoff

Finish the English shard plans and report unique chapter results, deduplicated by book/edition/chapter. Validate each timing file against the precise text and audio it accompanies, including paragraph indices and monotonic in-range timings. Provide the combined failure/low-coverage list with failure reason, coverage, and whether it is missing data, transcription/alignment failure, or text/audio mismatch. Retry repairable failures against matching source inputs; do not lower acceptance thresholds globally to make the board green. Genesis should be reported by exact edition. Danish remains outside this pass.

The reader consumes validated words.json files from the existing audio route; no app release is required per uploaded timing file. Missing timings cannot provide exact word seeking. The reader-side whole-page highlight was a separate CSS bug.
