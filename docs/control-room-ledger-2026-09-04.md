# Tinct control-room ledger — 2026-09-04

Control room session: https://claude.ai/code/session_01GXmqM6Sj4F3tgUtr6DmuLN
Execution environment: Claude Code remote Linux container (not Anders's Mac).

## Baseline

| Item | Value |
|---|---|
| Clean production baseline (session start) | `origin/codex/lab-convergence` = `3888548da6d9bfe5c484e6227132203c30a1d9bb` ("Fix stale mobile word timing cache", 2026-09-04 18:45 +0200) |
| Production baseline after release 2.1 | `origin/codex/lab-convergence` = `08f47288` (+ sentence-level follow for weak paragraphs), Worker `0943b5f0-f1a0-4dbb-be64-5d75ae3bf2dd`, bundle `index-C5ArRbVl.js`, deployed 2026-09-05 08:04 UTC |
| Production baseline after release 2 | `origin/codex/lab-convergence` = `ef6ba7ec` (+ reading memory recap), Worker `c3e3e696-6aaf-408f-9a25-c848a552dcd1`, bundle `index-DWzW5uk-.js`, deployed 2026-09-05 07:08 UTC |
| Production baseline after release 1.1 | `origin/codex/lab-convergence` = `923873db` (Voice V2 preview + fixes D, A+C, B), Worker `1636d551-ff28-4ee3-9b63-c6002e2b5da1`, bundle `index-DZGQHspb.js`, deployed 2026-09-05 06:40 UTC |
| Production baseline after release 1 | `origin/codex/lab-convergence` = `5a678f3c2b168f8cd5698ea56901858c733c0533` (Voice V2 preview), Worker `11fd329a-cf4b-4804-a784-928a1c95762f`, bundle `index-CyyvAuYh.js` |
| `origin/main` | `45a4252c` (ancestor of the baseline, 31 commits behind) |
| Baseline unit tests | 809/810 pass; `src/preReader/catalogue.test.ts` "offers only registered editions…" fails under the full run and passes in isolation (pre-existing slow test, not lane-caused) |

Reference commits were initially absent from the remote (unpushed on Anders's Mac). Anders pushed them
on 2026-09-04 evening: `42a019f8` → `origin/codex/ref-explicit-goodbye`, `9684bd4e` →
`origin/codex/ref-word-timing-tooling`, `b4312203` → `origin/codex/ref-reading-memory`. Both running lanes
were notified. The Mac-only artifact folders are being published as `codex/ref-artifacts-2026-09-04`
(pushed; 42 files, pilots and plans included). `app/.env` and all Cloudflare/R2 credentials remain absent here.

## Lane status

Status vocabulary: not started · implementing · tests passing · clean commit available · integrated · deployed · production verified · blocked.

| Lane | Branch / location | Status | Notes |
|---|---|---|---|
| 1 Voice V2 preview | `codex/claude-voice-v2-preview` | **production verified** | `5a678f3c` deployed 20:47 UTC. Worker version `11fd329a-cf4b-4804-a784-928a1c95762f`, bundle `index-CyyvAuYh.js`. Production: `/lab/reader` → `data-voice-version=v1`, `/lab/reader?voice=v2` → `v2` (phone + desktop, no page errors), `/lab/phone?voice=v2` stays v1. Live mic/Realtime still needs a human device test. |
| 2 Word-timing canary | `codex/claude-word-timing-production` | **production verified** (canary) / blocked (GPU benchmark) | Rerun 3 with `CF_R2_TOKEN` (custom token: Account · Workers R2 Storage · Edit): bucket metadata 200, known-object read OK, both Genesis 1 pilots uploaded with no-overwrite check, GET/hash/schema/provenance verified, production serves `bible/web-en/ch1/words.json` (72,454 B) and `bible/modern-en/ch1/words.json` (72,226 B) at 200, headless highlighting check passes at 1× and 2× (word identity follows media time; not perceptual). Report `5462d952`. Learned: R2-page tokens fail wrangler's REST path with 10000; a Workers R2 Storage token works. Rerun 2 accidentally used the 05:31 R2-page token. Calibration measured (`b8d23149`): ≈20× real time on an RTX 4090, 18/18 sidecars valid, 3 genealogy chapters under the 85 % gate. Decisions 2026-09-05: no Danish; four Community pods (~$40); Shards 1–4 relaunched via a RunPod template (secret reference resolves only in the web UI, not the create-pod API); TOKEN=yes on all four; by 11:25 UTC production serves `bible/web-en/ch2/words.json`; by ~20:30 UTC: pod 1 1,615 uploads (Bible both editions done, into shard), pod 2 970, pod 3 305, pod 4 82; 17 failures total. Measured rate ≈ 8× real time (sequential paragraph downloads + one wrangler process per upload leave the GPU idle), not the 20× of the calibration → ~55 h per shard, finish Monday, ≈$165. Options put to Anders: A let it run; B add parallel prefetch + batched upload to the tool and restart the same resumable commands (Sunday evening, ≈$110). Awaiting decision. Secure Cloud pricing (~$65–80 total) accepted. Anders: "Build 1 and 2" → Lanes 9 and 10. |
| 3 Reading memory + recap | `codex/claude-reading-memory-recap` | **production verified** | Live in release 2 (`ef6ba7ec`): 976/976 tests, production recap card shows a truthful location and exact KJV excerpt after real page turns, fresh context shows no card, `/lab/reading-memory.js` 200. Still unexercised by any lane: real signed-in sync/adoption and the automatic summary via the live Worker (Anders tests in production). |
| 4 100-cover collection | Codex on the Mac, `codex/claude-cover-collection` | clean commit available (100 covers) / awaiting Anders's approval | Correction commit `08ca55a3` (Phaedo, Crito, Niels Lyhne, Gilgamesh replaced; registry/quota gate added), batches 03–07 (`f12a25d7`…`3dc84482`), `integration-manifest.json` with 100 titles, exact registry titles/authors and SHA-256. Missing vs brief: no 100-cover / 100-thumbnail sheets and no 540×810 WebP mobile set (control room builds the review sheets itself; WebP conversion happens at integration). Control-room review of the full set pending. |
| 5 Integration + release | `codex/claude-convergence-integration` | deployed (1, 1.1, 2, 2.1, 3, 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8, 3.9, 3.10, 3.11) | Latest live: `16ccb4d3` (release 3.11). |
| 6 Position flip-flop (issue D) | `codex/claude-lab-position-merge-gate` | **production verified** | `7fe2f14c`, live as `b8f38e7d` in release 1.1. |
| 7 Reader chrome (issues A + C) | `codex/claude-lab-reader-chrome` | **production verified** | `6c1d45de`, live as `e583a419` in release 1.1; continued-tail class and title wake confirmed on tinct.app. |
| 8 Chat keyboard (issue B) | `codex/claude-lab-chat-keyboard` | **production verified** | `58fac972`, live as `923873db` in release 1.1; Chat tap focuses the composer on tinct.app (headless); real iOS keyboard pending Anders's phone. |
| 9 Whisper text biasing | `codex/claude-word-timing-production` | clean commit available | `f5b23de7`. `--bias-text {off,prompt,hotwords,both,auto}` in the generator; `auto` cascades both → hotwords → plain with an echo guard and a plain cross-check. Measured on CPU: Genesis 5 WEB 83.5→97.7 %, Genesis 10 WEB 90.5→99.1 %, Genesis 10 Modern 87.5→99.5 %, all paragraphs ≥ 0.85; Genesis 2 control unchanged/improved. Default stays `off` (identical call to the running pods); `auto` costs 1–2.3× wall, so it is the second-pass mode for chapters under the gate. Additive provenance `alignment.bias`. Tests 21/21, 13/13, 2/2. Not listened to by a human. |
| 10 Reader sentence fallback | `codex/claude-lab-follow-fallback` | **production verified** | Live in release 2.1 (`08f47288`): 987/987 tests, production Genesis 1 still word-level (ratio 1.0), zero page errors. A genuine sub-0.85 chapter arrives with the Lane 9 second pass. |
| 11 Locked library design | `codex/claude-lab-library-locked` | **production verified** | Release 3 live 2026-09-05 ~11:20 UTC at `a2a271aa`: tinct.app/lab/ serves the locked library (shelf markup present, `/covers/v2/odyssey.webp` 200 image/webp 38.7 KB). `codex/lab-convergence` fast-forwarded to `a2a271aa`. |
| 12 Library round 2 | `codex/claude-lab-library-round2` | **production verified** (release 3.2, 06:19 UTC) | `06a5159f` live: Worker `284cddeb-c207-471f-af7d-2500ced19cfd`, bundle `index-BAtwbOg0.js`; tests green in the deploy session; curl confirms the new bundle and library markup (grid/section heads) from a second container. Control-room headless check on production (390×844): palette attribute set (`light` with no stored theme, per the new system default), POPULAR head present, 8 shelf covers, search row top identical before/after selecting a cover (no jump), house grid renders, 0 page errors. **production verified.** Convergence promoted to `06a5159f`. Open owner decisions: Popular label, 100-cover integration timing. |
| 13 Companion book context | `codex/claude-lab-chat-book-context` | **production verified** (release 3.3, 06:45 UTC) | `2e3d8285`, 26 files. Reading trail (last 5 chapters) in every lab chat/hop prompt; worker tool loop `read_chapter` + `find_in_book` over edition shards (≤4 rounds, streaming + non-streaming); must-look-it-up rule with banned declines; chat pane opens at newest message and scrolls to the reply's first line; navigation from chat/voice never autoplays unless the session started from playback or the reader asks; bare "yes" to a lookup offer becomes the lookup; silent hop with holding line + 6 s follow-up, praise openers removed; every non-playback utterance escalates to the companion (app-enforced, V1 + V2); companion model → `claude-sonnet-5` via one constant (`app/src/companionModel.ts`), effort low/medium, strict tools. NOTE: the shared `CHAT_MODEL` moves the production reader chat and recap summaries to Sonnet 5 too (cheaper: $2/$10 vs $3/$15). Full 1053/1053, build + verify-bundle pass, browser smoke on chat scroll. Not verifiable here: live Anthropic tool calls, live Realtime timing. Cost: typical request ≈ $0.02, worst case with 4 lookups ≈ $0.07. Integration: cherry-picked as `70379e52` onto `06a5159f`; two conflicts with Lane 14 in `LabApp.tsx` (applyPlaybackSkip now returns the resume outcome AND marks the chapter finished; deps unioned) and `LabApp.test.tsx` (merged test keeps the synced `finished.bible` assertion, drops the legacy-key and chapter-progress assertions that no longer apply when chat stays open). Full suite after merge 1082/1082, build + verify-bundle pass. Live: Worker `b4b4c548-5ac8-4dc7-a1e1-4da8b6e8c0af`; production serves `index-DbtOxDUl.js`, which contains the Sonnet 5 model id and the reading-trail marker (the deploy session's reported `index-CyaVDXNP.js` was its local test build). Convergence promoted to `70379e52`. Live probe from the control room 06:55 UTC: one real question to `/api/lab-chat` with `book` = Bible KJV chapter 782 (Jeremiah 37): "Wasn't Jeremiah just in prison? How did he end up there…" → HTTP 200 in 6.9 s, model `claude-sonnet-5`, answer cites Jer. 37:11–15 (Irijah at the gate of Benjamin, dungeon in Jonathan's house) and 37:17–21 (moved to the court of the prison) — correct and grounded; 2,456 input / 248 output tokens (≈ $0.007). |
| 14 Picker + chapter progress | `codex/claude-lab-picker-progress` | **production verified** | Release 3.1 live 06:04 UTC at `299f5bad`: Worker `d40f2859-8e30-46b4-9fb2-9b37cc6352d5`, bundle `index-C4WsjpfQ.js`; production check: Jeremiah 32 row `aria-current` in viewport, header "7 of 52 finished", 0 page errors. Convergence promoted. |
| 15 Launch-gap audit + switch prep | `codex/claude-lab-launch-gaps` → integrated as `1b2aa68e`; `codex/claude-lab-launch-switch` @ `c0b8f1da` (NOT deployed) | **production verified** (release 3.5, 20:45 UTC); switch branch ready for go/no-go | Audit of production `70379e52` on phone 390×844 and desktop 1440×900, 94 screenshots + reports in `/home/user/tinct-wt/launch-audit-evidence/`: new reader (landing → library → book → edition → reader), returning reader (recap hero, Continue lands at the anchor paragraph), reader chrome (TOC, chapter bounds, gear, Chat, Compare, Talk, highlight, position across reload), account round trip (sign-up → adoption → resume → sign-out → sign-in restores place), voice gating, cross-links and old routes, console: all PASS; 0 page errors. Closed in `989d0f44` (cherry-picked as `1b2aa68e`, 12 files): desktop keyboard page turns (Arrow/PageUp/PageDown/Space, blocked while inputs/overlays own keys), Esc closes settings sheet / This-book panel / highlight menu, Settings › Account gains 'Sign in or create a free account' / 'Manage account', highlights listed under chapter rows for non-Bible books, document titles → 'Tinct'. Suite 1110/1110, build + verify-bundle pass. Live: Worker `c64fdaab-45bb-48ce-83fe-669342234c6b`, bundle `index-LbnBwg_3.js`. Control-room desktop probe: ArrowRight/ArrowLeft turn pages and restore, gear → Account shows the sign-in link, Esc closes the sheet, title 'Tinct', 0 errors. Convergence promoted to `1b2aa68e`. Open gaps (A–G): A sign-out does not wipe device data; B desktop Talk fails silently without a mic; C Cloudflare beacon blocked by CSP (owner decision); D 404 shard probes on non-sharded books (latency, cosmetic); E desktop landing/library inside a 12 px paper frame (design call); F phone Esc does not close Chat; G new accounts show 'Premium account · 100 credits' (confirm intended). A, B and the sign-in return path are in flight as Lane 18. Launch switch (`c0b8f1da`, rebased onto `16ccb4d3`, 1237/1237, build + verify-bundle pass): `/` signed-in → `/library`; landing CTAs → `/library`; `/library` = lab library, indexable; `/read/{bookId}[/n]` in-app → lab reader; `/app` → 302 `/classic` (classic app, noindex); `/lab/*` remain as noindex aliases; sign-in page accepts `/library` as return target. Rollback: redeploy `codex/lab-convergence`. Throwaway account `launch-check-2026-09-06@tinct.app` (Supabase user `0784f6dc-d169-486e-b179-4d6023369380`) created on production — owner to delete. |
| 16 Account prompt | `codex/claude-lab-account-prompt` | **production verified** (release 3.4, 20:15 UTC) | `f39047a1`, 10 files (+853/−5), parent `70379e52`, fast-forwarded onto integration. Policy module `app/src/lab/labAccountPrompt.ts` (`gateLabAiAction`/`recordLabAiAction`, device counter `tinct:lab-ai-actions`, nudge flag `tinct:lab-second-book-nudge`, `LAB_PAID_FEATURES` chat/voice/recap = true, audiobook/compare = undecided, not enforced). Gate hooks in `useLabAsk.sendTyped` (before persist/fetch) and `startVoice` (before token fetch/mic); signed-in readers never counted or prompted; held chat draft is restored to the composer. `LabAccountSheet` (Create free account / Sign in / Not now, links carry returnTo) + `LabSecondBookNudge` (once per device, detected from reading-memory anchors + finished map). Landing `<details>` 'What's free' with the $3/mo Premium line. Full suite 1105/1105 (catalogue flake passed on rerun), build + verify-bundle pass, lane browser smoke 26/26. Live: Worker `38c93eaa-6195-46a4-8772-c44bf640fb48`, bundle `index-DAU4uvfl.js`. Control-room production probe (phone UA, anonymous, counter seeded to 1): send opens the sheet with the right copy and links, no `/api/lab-chat` request fires, dismiss closes it and keeps the draft; 'What's free' present on /lab/. Convergence promoted to `f39047a1`. Open: `labSignIn.safeReturnTo` still maps reader paths to the library after sign-in (position preserved via Return-to-book card); real Supabase sign-up round trip not exercised by the lane (Lane 15 covers new-account creation). |
| 17 Mobile chrome concept | design canvas (no code) | round 4 in progress (07 Sep) |  Canvas "Tinct Reader Chrome" https://claude.ai/code/artifact/913b098b-d9af-4d87-9fa7-4ec75baeadb5 — 21 artboards: flow map, reader rest/dimmed/play-pill/pill+menu/playing states, super-menu variants (icons+labels, labels-only, bottom sheet), Reading settings (Appearance/Text/Audio), Account, Chat, Talk, Compare as vertical swipe with one-time hint, onboarding A/B, six super-menu icon candidates (recommendation: horizontal dots), tradeoffs, 8 questions for Anders. Owner revision applied: small top-bar Play button (Meta-style) expanding a play pill; Play removed from menu, replaced by a Now-playing row while audio runs. Owner review 07 Sep: keep artboard 6 menu (no sub-headers, + Compare/Read version row, no Play), glass audio panel may overlap text until the next page turn, chapter picker in the pill, Play on all books and dims with chrome, compare = whole-page swap anchored on the first word with 'Compare version' next to the progress, bottom bar unchanged, chat dividers as line rules, drop 7/8/12–18 (voice + icon/onboarding to Lanes 23/24), add desktop Play placement and a two-page spread variant with trade-offs. Concepting only, nothing deployed. Watch subscription on the artifact not available from this session. | Owner round 4: justified body text by default and fix bottom overflow (ab 1); super button INVISIBLE (not dimmed) in read mode, revealed by a press mid-screen or anywhere in the top bar, and that first press only reveals (ab 2); transport stacking fixed, top Play stays as a PAUSE while the transport is up and hides with the chrome (two pause buttons accepted), pause and speed selector swapped (ab 3); all menus restyled to Matter (single translucent dark rounded panel, icon gutter + label rows, hairline dividers, no section headers); Compare card REPLACES the bottom tracker as default and tapping cycles the tracker states (ab 10/6b); Reading settings becomes an Apple-Books-style bottom sheet with theme card grid, size pill, and text/audio/compare edition + Show Compare on the main layer, 'Advanced settings' button at the bottom (ab 7); Advanced rebuilt as an Apple 'Customise Theme'-style sheet with live preview, grouped rows, right-aligned values, slider rows (ab 7b); chat overview gains date/time stamps per exchange (ab 9); desktop spread approved unchanged (ab 13); both open questions answered. |
| 18 Launch gaps round 2 | `codex/claude-lab-launch-gaps-2` → fast-forwarded as `bbb6f78c` | **production verified** (release 3.6, 20:55 UTC) | 14 files. A: `data-sign-out` calls `wipeLabDeviceUserData()` (`clearLocalUserData` + `clearDeviceReadingMemory` + best-effort delete of the `tinct-lab` IndexedDB position mirror); allowlist gains `tinct-lab-prefs`, `tinct-lab-device-id`; sign-in runtime tested in jsdom. B: failed voice start on desktop keeps the companion open as Chat with 'Couldn't start voice. Type a question instead.' (root cause: voice-ended effect closed the desktop pane). Known residue: a Voice V2 mid-session error still closes the desktop pane. C: `safeLabReturnTo` (`lab/labSignInReturn.ts`) accepts `/lab/*`, `/library`, `/read/<bookId>[/n]`, rejects other origins/protocol-relative/javascript:/backslashes; `labSignInUrl`/`labAccountUrl` take the current reader path; account sheet already passed returnTo. Suite 1125/1125, build + verify-bundle pass. Live: Worker `3433c2d1-e140-4932-b661-1b8ad75659af`, bundle `index-nI-DvGYc.js`. Control-room probe: Settings › Account link carries `returnTo=/lab/reader`; sign-in page back link resolves to `/lab/reader?voice=v2` for a reader returnTo (label still reads '← Library' — cosmetic). Convergence promoted to `bbb6f78c`. |
| 19 Reading memory audit | `codex/claude-lab-memory-audit` `ac3c9c57` → cherry-picked as `eaf73a9f` (one reconciliation: TOC conversations now read from Lane 22's per-book chat history `readLabBookChat`) | **production verified** (release 3.8, 05:25 UTC; Worker `d212ad0c-ff92-4073-86c8-06c82b00ae5f`, bundle `index-BkrLmNwB.js`; probe: paging past Genesis 1 anonymous writes `finished.bible=[1]`) | Verdict: front end, not stored data. Durable 'read' lives only in the lab position record's `finished` map (KV `lab-position:<uid>`, not Supabase); it was written only when turning PAST a chapter's last page with a next chapter present. Chapters read to the last word then left via TOC/cover, the last chapter of a book, and chatted chapters left only device reading-memory sessions (capped at 50, wiped on sign-out, only re-hydrated by the library recap load) → Jeremiah 35–39 unread. Finished books demoted to Reading now by a hide-time pin write newer than the completed session. Fixes: memory completion signal now also marks the chapter finished in the synced record (incl. a book's final chapter); cloud memory merged into the device mirror on sign-in; new 'Visited' TOC state from chat conversations; `movedBackIntoBook` keeps finished books Finished unless the pin moved earlier. No data repair needed; pruned history is not recoverable. Suite 1132/1132 on the lane, 1183/1183 after merge. Also found for Lane 20: `onRemoteResume` forces bookId 'bible' (cross-book bleed: Crito ch. 3 → Genesis 3), `chapterExistsOnClient` accepts a registry pin against the Bible manifest, boot-load effect can flip the chapter back after a cloud resume. Once observed, not reproduced: phone stuck on the last page of Jeremiah 36 until reload. Demo account `memory-check-2026-09-07@tinct.app` (user `96adcf05-ef79-4426-9cda-9246a23a0506`) — owner to delete. Evidence `/home/user/tinct-wt/memory-audit-evidence/`. |
| 20 No-flicker open + library | `codex/claude-lab-no-flicker` `536bed29` → cherry-picked as `e6cfde85` (one import conflict in `labReadingMemory.ts`, both kept) | **production verified** (release 3.9, 05:40 UTC) | Reader: boot painted the local record, the cloud merge re-navigated via `onRemoteResume`, and a boot load not owned by `chapterNavigationRef` painted the local chapter again. Fix: `useLabPositionSync` initial-resolution phase (cloud GET warmed at token time, bounded 3 s wait, `notePlace` suppressed until resolved, late cloud arrivals only same book + forward + no interaction); `initialResolving` hides label/status/page until one position is chosen; `onRemoteResume` bumps `chapterNavigationRef` and loads the registry book of the pin (`remoteResumeSelection`); `chapterExistsOnClient` takes the library book of the chapter list. Library: `public/lab/library-boot.js` (blocking head script, CSP-safe) stamps the view pre-parse and paints the pill + Reading-now hero from a boot snapshot (`tinct:lab-library-boot`, written by the library after each render and by the reader on Library link/hide); hint = persisted Supabase session or `tinct_auth` cookie, not `tinct:last-user-id`. `/lab/library`, `/library`, `?view=library` resolve to the library. Suite 1161/1161 on the lane, 1219/1219 after merge; frame-strip evidence `/home/user/tinct-wt/no-flicker-evidence/`. Live: Worker `fc133652-942c-4e77-a042-73c9b639db0b`, bundle `index-DWadb3k1.js`. Control-room frame probe on production (phone, 100 ms): reader page hidden → visible once, no second chapter; anonymous /lab/library never shows the landing panel; 0 errors. Convergence promoted to `e6cfde85`. Not verifiable here: real phone session-refresh timing; reading-memory cloud copy. |
| 21 Recap wording + chapter-so-far summary | `codex/claude-lab-recap-summary` `d48c0f9d` → cherry-picked as `c082f361` | **production verified** (release 3.7, 05:15 UTC) | Hero headline from the position fraction: at the start of / in the middle of / near the end of / finished / at the start of the next chapter; 'finished' never inferred from a fraction. `POST /api/lab-recap` (`worker/routes/labRecap.ts`): server loads the chapter through `bookRetrieval.chapterText`, Sonnet 5 low effort, max 300 tokens, spoiler-guarded; `caches.default` 30 d keyed by book/edition/chapter/paragraph bucket (5, chapters ≥ 25 paragraphs); client cache `tinct:lab-recap-summaries`; IP rate limit 6/min; anonymous free-action rule applies client-side. Cost ≈ $0.003–0.01 per uncached summary. Live probe: one real call for Bible chapter 661 (Ecclesiastes 2) returned a plain, accurate, spoiler-safe 110-word summary in one shot, `cached:false`. Follow-up: the older per-session summary path (`readingMemory/summary.ts` via `/api/chat`) still runs once per library load for signed-in readers; retire it. |
| 22 Chat history + mobile focus | `codex/claude-lab-chat-history` `141d6c0d` (ff onto integration) | **production verified** (release 3.7, 05:15 UTC) | Phone: no programmatic focus on Chat open (tab tap, Talk fallback, held draft); desktop focuses once on the open transition, gated by desktop layout AND `(pointer: fine)`. History: classic per-book key `chat-history:{bookId}` (`tinct:chat-history:{bookId}` mirror) via versioned `commit_user_data`; lab and classic chats are one history; the retired `tinct:chat-history:lab` blob and the Worker KV copy fold in once. DECISION (owner to confirm): the Bible is one thread with 'Genesis 1 / Romans 8' divider chips — reverses the earlier per-biblical-book rule. Windowing: newest 40 turns, 40 more on scroll-to-top with stable scroll; companion context unchanged (trail + last 20 turns). Live probe (Worker `23b0fdfe-5b67-44ee-86f9-c1b85cafb18d`, bundle `index-D4ZJGaLO.js`): phone activeElement stays the Chat button, desktop input focused, 0 errors. Convergence promoted to `c082f361`. |
| 23 Voice mode design | canvas "Tinct Voice Mode" https://claude.ai/code/artifact/9ed5ccc8-745c-4994-a674-b893386629a7 (no code) | concept delivered (awaiting owner review) | 18 artboards: 10 phone states, 3 presence directions (recommendation A: hairline baseline under the top bar, ink/teal, breath on lookup, blip on ack), desktop, latency timeline from code. Engineering levers found: spoken 'I'm listening.' greeting is a full model turn that blocks barge-in; companion answer awaited in full before TTS (`firstSpeakableChunk`/`onFirstSpeakable` wired as no-op); 50 ms cover-line poll. 5 questions for the owner. |
| 24 Super button + onboarding | canvas "Tinct Super Button" https://claude.ai/code/artifact/050a1c6a-84cf-4d11-b0e0-521dfe06907a (no code) | round 2 delivered (awaiting owner icon pick) | 6 artboards: 8 monoline glyphs at real size (recommendation: aster, six-arm asterisk, morphs into ×; runner-up: three-point constellation), button states, menu-open check vs chrome artboard 6, 7-frame first-open animation (2.8 s, draw → breath → ring → 12 px menu peek → Play pulse; reduced-motion fallback; fires once after first page settles). 4 questions. Round 2 (owner: none of the eight; wants 'super duper nice, distinctive, simple', bigger set): 29 more glyphs in 5 families at real size/dimmed/3×; shortlist: small six-ray sun, ampersand, spark, pilcrow, ring with a gap; verdict sun today or ampersand with a bespoke drawing. States/menu/animation artboards approved and glyph-agnostic. |
| 25 Desktop landing frame + chat dividers | `codex/claude-lab-desktop-polish` `3acfd3b9`+`177559ee` (ff onto integration) | **production verified** (release 3.10, 09:00 UTC) | Cause: `body{padding:12px}` + `.tov5-simple-landing{width:min(1440px,calc(100vw-24px));aspect-ratio:1440/900;border-radius:24px}` with absolutely positioned header/hero; at 640 px the card was ~400 px tall so the headline climbed into the wordmark and the CTA clipped; `.tov5-zoom` had the same card frame. Fix: full-bleed dark ground at all widths, header in-flow flex row, hero `flex:1; min-height:100dvh`, phone ≤520 keeps the locked artboard; library untouched. Chat `.lab-ask-location` → rule with `::before/::after` lines and grey mono label. 28/28 layout matrix (4 views × 7 widths). Live: Worker `9bbdedae-a2ab-4c99-893a-fef7b051f6c3`, bundle `index-DLFT2lRo.js`; control-room probe at 1440 and 640: body padding 0, wordmark/h1 no overlap, no horizontal scroll, What's free present, hero fills viewport. Convergence promoted to `177559ee`. |
| 26 Follow-highlight flicker | `codex/claude-follow-flicker` `d8ad858b` → cherry-picked as `16ccb4d3` | **production verified** (release 3.11, 09:10 UTC) | Cause: per-frame layout churn, not paint. `LabPassage` ran `markFullContinuedTails()` in a dependency-less `useLayoutEffect` (strip `is-tail-full`, forced relayout, re-add = two forced relayouts of a justified paragraph every frame: 938 attribute mutations / 396 of 397 frames on a page with a continued tail); `useLabListen` rAF tick called `setCurrentTime`/`setFollow` every frame, re-rendering the whole reader. Fix: tail measure only on painted-lines/branch/compare/headline/paragraphs/`layoutKey` (typography prefs) + ResizeObserver; `setFollow` deduped (`isSameFollowTarget`), `setCurrentTime` stepped 0.25 s (`steppedPlaybackTime`); `box-decoration-break: clone` at rest. After: 72 mutations / 10 s, 0 px word movement, fps 25→53. Tests: paint-only CSS scan, computed metrics equal current vs rest, class toggles on same spans, follow-only rerender leaves tails alone. Suite 1229/1229. Live: Worker `a1d71b4f-bbc1-4d54-93ef-a51df0e49616`, bundle `index-YHrcNDoX.js`. Not verified: Safari/macOS hyphenation (owner to confirm in a private window); desktop transport progress now steps 0.25 s. Convergence promoted to `16ccb4d3`. |
| 27 Library experience | `codex/claude-library-experience` @ `/home/user/tinct-wt/library-experience` (base `16ccb4d3`) | implementing | Owner review of the live mobile library. Integrate all 100 covers (they exist only as artifacts on `codex/claude-cover-collection` under `artifacts/tinct-cover-art-2026-09-04/collection-v2/final`; only the 10 pilots ever shipped). Fix: excessive padding under the cover row; scroll-to-centre focus instead of tap-only; back-nav skipping a step (library → book → back lands two back); books with no book page (e.g. pride-and-prejudice); missing time-to-read/finish; one-page rule with Start reading always in view (320–1440). Book page redesign: edition pills → dropdowns listing translations with inline sample text, 'Both' as a button, grey helper text cut. Returning-reader row: no amber accent, horizontal scroll like Popular, '…' summary only for the centred book when >1 in progress. Desktop follows the same principles. |
| 28 Recap policy + sign-in providers | `codex/claude-recap-signin` @ `/home/user/tinct-wt/recap-signin` (base `16ccb4d3`) | implementing | A: recap generates a summary only after ≥1 h away from that book (`LAB_RECAP_MIN_AWAY_MS`), otherwise position line only and no model call. B: Continue-with Google, Apple, GitHub on the lab sign-in (capability-gated; Apple/GitHub need Supabase + Apple Developer config — lane to write the exact dashboard steps). C: answer why the owner's Bible reading produced no recap summary, with the code path, fix if small. |

## Ideas parked (owner said do nothing yet)

- 2026-09-06 (Anders): replace the Read/Chat/Talk button bar with a single menu button in the top bar. On press, if mic permission is already granted, pre-warm the voice session (mic muted) so Talk opens with no latency. Open question: should first-time readers simply discover it, or get a single explainer on first read?

## Decisions

- 2026-09-05 (Anders): "I'm the only real user, others have churned. Just deploy; prefer to test production until we can't do that any more." Control-room policy from here: every clean lane commit is integrated and deployed after `npm test`, `npm run build`, `npm run verify-bundle` pass; production is the test surface; long baseline A/B browser comparisons are dropped; release 2 (reading memory) deploys straight after rebase + gates. Exception: concepting. When Anders is exploring an idea rather than asking for a shipped change, do not build or deploy; if in doubt whether a message is concepting or an implementation request, ask first.

- 2026-09-05 morning feedback (Anders): four lab phone-reader issues A–D investigated by a separate agent; Anders authorized "apply the best fixes and push to production". Fix lanes 6–8 created on the production baseline; Lane 5 integrates and deploys them as release 1.1 before release 2 (reading memory). The investigator's claim that production ran unpushed code was wrong: it compared against `main`; the deployed code is `codex/lab-convergence`.

- Integration lane will push the integrated result to `codex/claude-convergence-integration` (a fast-forward of `codex/lab-convergence` + approved lane commits) rather than rewriting `codex/lab-convergence`, because deploy cannot run from this environment and the repo policy is never to push a convergence state that is not deployed.
