# Odyssey Book 1 — the collision report, dispositioned

Written by `scripts/collision_triage.py`. Every row of every arrow that touches
this Book, with a disposition on each. **Records finding R-2 and blind spot 8**
of `book07/review/findings-v1.md`: the check returned seventy-two rows touching
Book 7, eleven were acted on, and sixty-one were dismissed with no record of
having been read. This file is that record, and it is generated, not typed.

**162 rows touch Book 1.**

| disposition | rows |
|---|---|
| `artifact` | 5 |
| `common-rendering` | 13 |
| `common-word` | 22 |
| `context-rendered` | 4 |
| `free-variation` | 1 |
| `homograph` | 3 |
| `kept` | 22 |
| `kept-elsewhere` | 60 |
| `phrase-not-word` | 8 |
| `same-referent` | 3 |
| `unavoidable-merge` | 7 |
| `variant` | 14 |

The vocabulary is defined in the script's docstring. `homograph`,
`same-referent`, `unavoidable-merge` and `phrase-not-word` are new here;
`homograph` is the one the review asked for by name.

## ARROW A — one Butler word, two or more renderings, across Books

57 rows.

| key | this Book's entries | disposition | reason |
|---|---|---|---|
| `accept` | `receive` (B1-P003) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `accept` → `receive`, and it is the thing to rule on.** Every other entry is Butler's own `accept` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `receive` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `accept` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `around` | `once` (B1-P017) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `around` → `once`, and it is the thing to rule on.** Every other entry is Butler's own `around` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `once` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `around` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `behaving` | `behave` (B1-P016) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `behaving` → `behave`, and it is the thing to rule on.** Every other entry is Butler's own `behaving` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `behave` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `behaving` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `blinded` | `blinding the` (B1-P006) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `blinded` → `blinding the`, and it is the thing to rule on.** Every other entry is Butler's own `blinded` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `blinding the` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `blinded` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `bottom` | `has charge` (B1-P005) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `bottom` → `has charge`, and it is the thing to rule on.** Every other entry is Butler's own `bottom` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `has charge` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `bottom` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `carries` | `holds up` (B1-P005) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `carries` → `holds up`, and it is the thing to rule on.** Every other entry is Butler's own `carries` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `holds up` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `carries` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `cease` | `stop` (B1-P024) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `cease` → `stop`, and it is the thing to rule on.** Every other entry is Butler's own `cease` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `stop` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `cease` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `ceasing` | `end` (B1-P024), `pause` (B1-P002) | **`context-rendered`** | Butler writes `without ceasing` twice in Book 1 and the candidate renders it `without end` (B01-P024) and `without pause` (B01-P002). The two are not interchangeable: a widow's mourning is endless, a god's persecution is unremitting, and `mourn without pause` and `persecuted him without end` are both wrong. The renderings differ because the contexts do. |
| `celebrate` | `celebrate` (B1-P024), `hold` (B1-P019) | **`homograph`** | One Butler spelling, two unrelated senses. `celebrate his funeral rites` is *perform* (B01-P019 and B02-P012 — the same Butler sentence in two Books, rendered `hold` in both, which is the consistency the package asks for). `such as poets love to celebrate` is *sing of*. **The B01-P024 rendering was separately live and is repaired**: see the arrow C row (1, 24, `celebrate`, `sing`). |
| `closely` | `completely` (B1-P017) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `closely` → `completely`, and it is the thing to rule on.** Every other entry is Butler's own `closely` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `completely` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `closely` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `conduct` | `take` (B1-P007) | **`context-rendered`** | `the girl who was to conduct him to the city` -> `guide` (B06-P010) and `I will also conduct him to Sparta` -> `take` (B01-P007). Nausicaa shows Odysseus a way he does not know; Athena proposes to convey Telemachus on a voyage she is arranging. `guide him to Sparta` would say Telemachus cannot find it, and `take him to the city` would take the showing out of Nausicaa's hands. |
| `conducted` | `showed` (B1-P010) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `conducted` → `showed`, and it is the thing to rule on.** Every other entry is Butler's own `conducted` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `showed` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `conducted` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `couple` | `couple` (B1-P018), `pair` (B1-P032) | **`free-variation`** | `a couple of lances` keeps `a couple of` at B01-P018; `a couple of blazing torches` becomes `a pair of` at B01-P032. Fourteen paragraphs apart, no shared referent, and no reader can attach a meaning to the difference — `a couple of` and `a pair of` are interchangeable modern English. Recorded rather than repaired: a successor to an accepted Book for this would buy nothing. |
| `decorated` | `worked` (B1-P010) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `decorated` → `worked`, and it is the thing to rule on.** Every other entry is Butler's own `decorated` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `worked` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `decorated` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `depart` | `leave` (B1-P027) | **`variant`** | every rendering in the row reduces to `leave` once inflection, D9 spelling and D15 compound spacing are normalized |
| `detaining` | `holding` (B1-P014) | **`context-rendered`** | `savages who are detaining him against his will` -> `holding` (B01-P014); `you are detaining me from them` -> `keeping me from them` (B04-P050). The first is captivity and the second is delay, and English does not use one verb for both: `holding me from them` is not idiomatic and `keeping him against his will` loses the imprisonment. |
| `direct` | `guide` (B1-P019) | **`context-rendered`** | Butler's `direct` in `the girl who was to direct him` (B01-P019, B02-P012) is *show the way*, which modern `direct` has all but lost to *instruct* and *manage*; `guide` is the surviving word for it. Book 9 keeps `direct` at B09-P004, where `letting the wind and the steersmen direct our ship` is the living sense. Two senses, each rendered right. |
| `distaff` | `spinning` (B1-P025) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `distaff` → `spinning`, and it is the thing to rule on.** Every other entry is Butler's own `distaff` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `spinning` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `distaff` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `faring` | `faring` (B1-P014) | **`artifact`** | Butler writes `sea-faring` with a hyphen and the tokenizer splits it, so `faring` is not a word either text uses. D15 closes it to `seafaring`, matching accepted B06-P021. There is no rendering pair. |
| `feared` | `fear of` (B1-P032), `feared` (B1-P018) | **`variant`** | One word in two forms. B01-P018 keeps Butler's verb (`Ilus feared the everlasting gods`); B01-P032 recasts `for he feared his wife's resentment` to the noun `for fear of his wife's anger`. Same lexeme, same sense, a part-of-speech change made by the recast. |
| `flower` | `finest` (B1-P014) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `flower` → `finest`, and it is the thing to rule on.** Every other entry is Butler's own `flower` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `finest` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `flower` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `glittering` | `bright` (B1-P008) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `glittering` → `bright`, and it is the thing to rule on.** Every other entry is Butler's own `glittering` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `bright` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `glittering` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `grasped` | `and took up` (B1-P008) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `grasped` → `and took up`, and it is the thing to rule on.** Every other entry is Butler's own `grasped` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `and took up` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `grasped` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `handmaids` | `maids` (B1-P023), `maids` (B1-P026) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `handmaids` → `maids`, and it is the thing to rule on.** Every other entry is Butler's own `handmaids` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `maids` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `handmaids` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `havoc` | `laying waste to` (B1-P017) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `havoc` → `laying waste to`, and it is the thing to rule on.** Every other entry is Butler's own `havoc` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `laying waste to` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `havoc` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `hearers` | `listeners` (B1-P023) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `hearers` → `listeners`, and it is the thing to rule on.** Every other entry is Butler's own `hearers` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `listeners` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `hearers` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `hecatomb` | `great sacrifice` (B1-P003) | **`same-referent`** | A hecatomb IS a sacrifice, so the rendering keeps Butler's referent and marks what would otherwise be lost — the scale — with an adjective: `great sacrifice` (B01-P003) and `fine sacrifice` (B03-P007, where `fine` is carrying Butler's own `goodly` and the scale word has nowhere to stand). **Accepted Book 4 was the one out of step**, rendering `holy hecatombs` as a bare `holy sacrifices` one sentence from Butler's own `sacrifices`; the successor `book04/candidate-v5.json` brings it into line. See the arrow C row (4, 40, `hecatombs`, `sacrifices`). |
| `inherit` | `have inherited` (B1-P017) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `inherit` → `have inherited`, and it is the thing to rule on.** Every other entry is Butler's own `inherit` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `have inherited` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `inherit` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `insolence` | `rudeness` (B1-P010) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `insolence` → `rudeness`, and it is the thing to rule on.** Every other entry is Butler's own `insolence` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `rudeness` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `insolence` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `laying` | `set` (B1-P008) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `laying` → `set`, and it is the thing to rule on.** Every other entry is Butler's own `laying` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `set` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `laying` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `lighted` | `lit` (B1-P032) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `lighted` → `lit`, and it is the thing to rule on.** Every other entry is Butler's own `lighted` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `lit` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `lighted` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `longing` | `longed` (B1-P002) | **`variant`** | every rendering in the row reduces to `long` once inflection, D9 spelling and D15 compound spacing are normalized |
| `looks` | `has charge` (B1-P005) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `looks` → `has charge`, and it is the thing to rule on.** Every other entry is Butler's own `looks` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `has charge` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `looks` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `lordly` | `proud` (B1-P008) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `lordly` → `proud`, and it is the thing to rule on.** Every other entry is Butler's own `lordly` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `proud` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `lordly` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `marvelled` | `listened amazed` (B1-P028) | **`artifact`** | Butler's `as they heard him, and marvelled at the boldness of his speech` became `as they listened, amazed at the boldness of his speech` — `heard` -> `listened` and the finite verb absorbed into a participial phrase. The diff paired `marvelled` with the two-word span `listened amazed`; there is no rendering pair to rule on. **The live question in this family is the opposite direction** and is ruled at (1, B, `marveled`). |
| `meantime` | `meanwhile` (B1-P007) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `meantime` → `meanwhile`, and it is the thing to rule on.** Every other entry is Butler's own `meantime` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `meanwhile` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `meantime` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `nation` | `people` (B1-P013) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `nation` → `people`, and it is the thing to rule on.** Every other entry is Butler's own `nation` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `people` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `nation` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `offended` | `take offense at` (B1-P013) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `offended` → `take offense at`, and it is the thing to rule on.** Every other entry is Butler's own `offended` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `take offense at` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `offended` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `otherwise` | `otherwise` (B1-P017) | **`kept`** | every entry Book 1 contributes is Butler's own `otherwise` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `persist` | `go on` (B1-P007), `go on feeding off` (B1-P027) | **`artifact`** | One rendering, `go on`, in both entries. B01-P027's longer span is Butler's `persist in spunging upon one man`, where `spunging` had to move as well and the diff took the two together as `go on feeding off`. |
| `possessions` | `property` (B1-P030) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `possessions` → `property`, and it is the thing to rule on.** Every other entry is Butler's own `possessions` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `property` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `possessions` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `prevail` | `persuade` (B1-P019) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `prevail` → `persuade`, and it is the thing to rule on.** Every other entry is Butler's own `prevail` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `persuade` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `prevail` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `prevented` | `kept` (B1-P001) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `prevented` → `kept`, and it is the thing to rule on.** Every other entry is Butler's own `prevented` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `kept` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `prevented` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `preventing` | `keeping` (B1-P006) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `preventing` → `keeping`, and it is the thing to rule on.** Every other entry is Butler's own `preventing` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `keeping` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `preventing` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `pulled` | `pulling` (B1-P032) | **`variant`** | every rendering in the row reduces to `pull` once inflection, D9 spelling and D15 compound spacing are normalized |
| `refreshed` | `rested then` (B1-P020) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `refreshed` → `rested then`, and it is the thing to rule on.** Every other entry is Butler's own `refreshed` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `rested then` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `refreshed` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `renown` | `fame` (B1-P017) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `renown` → `fame`, and it is the thing to rule on.** Every other entry is Butler's own `renown` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `fame` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `renown` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `respectable` | `decent` (B1-P016) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `respectable` → `decent`, and it is the thing to rule on.** Every other entry is Butler's own `respectable` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `decent` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `respectable` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `riches` | `wealth` (B1-P029) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `riches` → `wealth`, and it is the thing to rule on.** Every other entry is Butler's own `riches` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `wealth` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `riches` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `seated` | `sitting` (B1-P008) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `seated` → `sitting`, and it is the thing to rule on.** Every other entry is Butler's own `seated` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `sitting` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `seated` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `serve` | `be no use to` (B1-P013) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `serve` → `be no use to`, and it is the thing to rule on.** Every other entry is Butler's own `serve` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `be no use to` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `serve` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `shrift` | `quick end` (B1-P018) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `shrift` → `quick end`, and it is the thing to rule on.** Every other entry is Butler's own `shrift` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `quick end` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `shrift` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `smart` | `good` (B1-P019) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `smart` → `good`, and it is the thing to rule on.** Every other entry is Butler's own `smart` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `good` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `smart` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `storm` | `storms` (B1-P017) | **`variant`** | every rendering in the row reduces to `storm` once inflection, D9 spelling and D15 compound spacing are normalized |
| `tired` | `sick` (B1-P005), `tired` (B1-P014) | **`homograph`** | One spelling, two unrelated senses. `he is tired of life` -> `sick of life` (B01-P005) is satiety; `comes in tired from working his vineyard` (B01-P014) is fatigue, and Butler's word is kept because it is current English there. `sick of life` and `tired from the vineyard` are two words, not one rendered twice. |
| `value` | `valuable` (B1-P020), `value` (B1-P021) | **`variant`** | One word in two forms, twelve lines apart: `a gift of great beauty and value` becomes the adjective `something fine and valuable` (B01-P020), and the noun is kept at B01-P021 (`one of no less value in return`). The recast of the first changed the part of speech, not the word. |
| `yonder` | `there` (B1-P014) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `yonder` → `there`, and it is the thing to rule on.** Every other entry is Butler's own `yonder` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `there` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `yonder` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |

## ARROW B — one rendering, two or more Butler words, across Books

75 rows.

| key | this Book's entries | disposition | reason |
|---|---|---|---|
| `account` | `shall reckon` (B1-P027) | **`phrase-not-word`** | `Jove shall reckon with you in full` -> `Zeus will settle the account with you in full`, word for word the same in both accepted Books, of Butler's own sentence repeated verbatim across them. `settle the account` is one fixed idiom; its two head words happen to coincide with Butler's own `account` (three times in Book 4) and his own `settle` (Books 5 and 7). One idiom, not two borrowed words. Ruled the same way from the other head at (1/2, B, `settle`). |
| `after` | `after` (B1-P001), `after` (B1-P004), `after` (B1-P014) | **`kept`** | every entry Book 1 contributes is Butler's own `after` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `anger` | `resentment she` (B1-P032) | **`unavoidable-merge`** | `anger` carries Butler's own `anger` in five Books, his `wrath of heaven` (B02-P004) and his `wife's resentment` (B01-P032). `wrath` is elevated and archaic and `anger` is its plain modern equivalent; `resentment` is current English and needed no change, but the referent is the same jealousy and the alternative is a register distinction Butler is not making. |
| `apart` | `asunder` (B1-P005) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `apart` → `asunder`, and it is the thing to rule on.** Every other entry is Butler's own `apart` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `asunder` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `apart` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `arrows` | `arrows` (B1-P018) | **`kept`** | every entry Book 1 contributes is Butler's own `arrows` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `being` | `being` (B1-P005) | **`kept`** | every entry Book 1 contributes is Butler's own `being` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `beside` | `beside` (B1-P011) | **`kept`** | every entry Book 1 contributes is Butler's own `beside` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `bright` | `glittering` (B1-P008) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `bright` → `glittering`, and it is the thing to rule on.** Every other entry is Butler's own `bright` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `glittering` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `bright` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `brings` | `brings` (B1-P029), `quells` (B1-P008) | **`phrase-not-word`** | `wherewith she quells the ranks of heroes` -> `with which she brings down the ranks of heroes` (B01-P008). `bring down` is a phrasal verb and Butler's own `it brings both wealth and honor` (B01-P029) is the simple transitive. Different verbs that share a syllable. |
| `carried` | `spirited` (B1-P017) | **`common-rendering`** | `carried` is a word Butler himself uses in 15 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `charge` | `looks after the bottom` (B1-P005) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `charge` → `looks after the bottom`, and it is the thing to rule on.** Every other entry is Butler's own `charge` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `looks after the bottom` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `charge` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `completely` | `closely` (B1-P017) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `completely` → `closely`, and it is the thing to rule on.** Every other entry is Butler's own `completely` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `closely` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `completely` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `decide` | `decide` (B1-P030), `determine` (B1-P019) | **`unavoidable-merge`** | `It rests with heaven to determine` (B01-P019) and `It rests with heaven to decide` (B01-P030) — Butler's own two verbs in the same formula, eleven paragraphs apart, with no difference between them. `decide` is the plain modern word for both and keeping `determine` would invent a distinction Butler did not make. |
| `direction` | `ordering` (B1-P025) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `direction` → `ordering`, and it is the thing to rule on.** Every other entry is Butler's own `direction` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `ordering` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `direction` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `eating` | `eating` (B1-P007), `eating` (B1-P017) | **`kept`** | every entry Book 1 contributes is Butler's own `eating` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `enough` | `enough` (B1-P016) | **`kept`** | every entry Book 1 contributes is Butler's own `enough` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `finest` | `flower` (B1-P014) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `finest` → `flower`, and it is the thing to rule on.** Every other entry is Butler's own `finest` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `flower` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `finest` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `first` | `first` (B1-P003), `first` (B1-P007), `first` (B1-P018), `first` (B1-P019) | **`kept`** | every entry Book 1 contributes is Butler's own `first` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `flattery` | `blandishment` (B1-P005) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `flattery` → `blandishment`, and it is the thing to rule on.** Every other entry is Butler's own `flattery` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `blandishment` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `flattery` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `going` | `going` (B1-P026) | **`kept`** | every entry Book 1 contributes is Butler's own `going` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `great` | `great` (B1-P005), `great` (B1-P008), `great` (B1-P023), `great` (B1-P024), `great` (B1-P029), `hecatomb` (B1-P003) | **`common-rendering`** | `great` is a word Butler himself uses in 66 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `grieving` | `sorrowful` (B1-P024) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `grieving` → `sorrowful`, and it is the thing to rule on.** Every other entry is Butler's own `grieving` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `sorrowful` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `grieving` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `guide` | `direct` (B1-P019) | **`unavoidable-merge`** | `guide` carries Butler's `conduct` (B06-P010), his `direct` (B01-P019 and B02-P012 — the same sentence in two Books, rendered identically) and his own `guide` (B05-P005). All three are Butler's free variants for showing somebody a way; `some heaven-sent message may direct you` and `may guide you` are the same sentence in his own idiom, and no discrimination exists to lose. |
| `heads` | `heads` (B1-P006) | **`kept`** | every entry Book 1 contributes is Butler's own `heads` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `holding` | `who are detaining` (B1-P014) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `holding` → `who are detaining`, and it is the thing to rule on.** Every other entry is Butler's own `holding` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `who are detaining` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `holding` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `hurried` | `hurried` (B1-P030), `were bustling` (B1-P008) | **`unavoidable-merge`** | `Men-servants and pages were bustling about` -> `hurried about` (B01-P008), beside Butler's own `he hurried off` (B01-P030). The two are twenty-two paragraphs apart and share no referent; `bustle` is current English and could have been kept, but nothing in the text turns on the difference between bustling and hurrying servants. |
| `husband` | `husband` (B1-P019), `husband` (B1-P026) | **`kept`** | every entry Book 1 contributes is Butler's own `husband` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `keeping` | `keeping` (B1-P014), `preventing` (B1-P006) | **`unavoidable-merge`** | `keeping` carries Butler's `detaining` (B04-P050), his `preventing him from getting home` (B01-P006) and his own `keeping` in five Books. `keep X from Y` is the plain modern idiom for both, and the three instances are in three Books with no shared referent. |
| `laying` | `making havoc of` (B1-P017) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `laying` → `making havoc of`, and it is the thing to rule on.** Every other entry is Butler's own `laying` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `making havoc of` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `laying` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `leave` | `depart` (B1-P027) | **`common-rendering`** | `leave` is a word Butler himself uses in 26 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `lives` | `lives` (B1-P005), `lives` (B1-P014) | **`kept`** | every entry Book 1 contributes is Butler's own `lives` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `maids` | `handmaids` (B1-P023), `handmaids into her room` (B1-P026), `maids` (B1-P012) | **`common-rendering`** | `maids` is a word Butler himself uses in 23 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `matter` | `matters` (B1-P017) | **`variant`** | every rendering in the row reduces to `matter` once inflection, D9 spelling and D15 compound spacing are normalized |
| `meanwhile` | `in the meantime` (B1-P007) | **`common-rendering`** | `meanwhile` is a word Butler himself uses in 12 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `offense` | `be offended with` (B1-P013) | **`unavoidable-merge`** | D9, `offence` -> `offense`, plus one recast: Butler's `you will not be offended with what I am going to say` (B01-P013) becomes `you will not take offense at what I am about to say`, which is the idiom Butler HIMSELF uses at B02-P008 and B06-P012 (`take offence at`). The merge makes Book 1 agree with two accepted Books on one speech act. |
| `opened` | `opened` (B1-P032) | **`variant`** | every rendering in the row reduces to `open` once inflection, D9 spelling and D15 compound spacing are normalized |
| `others` | `some cutting` (B1-P008) | **`common-rendering`** | `others` is a word Butler himself uses in 24 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `people` | `nation` (B1-P013), `people` (B1-P002), `people` (B1-P007), `people` (B1-P013), `people` (B1-P016), `people` (B1-P019), `people` (B1-P025) | **`common-rendering`** | `people` is a word Butler himself uses in 64 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `persuade` | `prevail upon` (B1-P019) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `persuade` → `prevail upon`, and it is the thing to rule on.** Every other entry is Butler's own `persuade` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `prevail upon` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `persuade` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `places` | `places` (B1-P012) | **`kept`** | every entry Book 1 contributes is Butler's own `places` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `property` | `possessions` (B1-P030) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `property` → `possessions`, and it is the thing to rule on.** Every other entry is Butler's own `property` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `possessions` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `property` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `proud` | `lordly` (B1-P008) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `proud` → `lordly`, and it is the thing to rule on.** Every other entry is Butler's own `proud` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `lordly` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `proud` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `pulling` | `pulled` (B1-P032) | **`variant`** | every rendering in the row reduces to `pull` once inflection, D9 spelling and D15 compound spacing are normalized |
| `quick` | `short shrift` (B1-P018) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `quick` → `short shrift`, and it is the thing to rule on.** Every other entry is Butler's own `quick` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `short shrift` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `quick` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `reaches` | `reaches` (B1-P031) | **`kept`** | every entry Book 1 contributes is Butler's own `reaches` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `rested` | `refreshed yourself` (B1-P020) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `rested` → `refreshed yourself`, and it is the thing to rule on.** Every other entry is Butler's own `rested` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `refreshed yourself` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `rested` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `sacrifice` | `hecatomb` (B1-P003) | **`same-referent`** | The other direction of (1/3, A, `hecatomb`). `sacrifice` carries Butler's `hecatomb` and his own `sacrifice`, and a hecatomb is a sacrifice — Butler's own other word for the same act. Both Books mark what the merge would otherwise cost with an adjective of scale. |
| `scold` | `chide with` (B1-P029) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `scold` → `chide with`, and it is the thing to rule on.** Every other entry is Butler's own `scold` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `chide with` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `scold` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `search` | `quest` (B1-P019) | **`unavoidable-merge`** | **Butler himself alternates, inside one speech, which settles it.** He writes `sail the seas in search of my father who has so long been missing` at B02-P018 and `go to Sparta and to Pylos in quest of my father who has so long been missing` at B02-P012 — the same formula, six paragraphs apart, in his own two words. `quest` is archaic outside fixed phrases; `search` is what he means by both, and three accepted Books render it identically. |
| `servant` | `servant` (B1-P011), `servant` (B1-P012) | **`kept`** | every entry Book 1 contributes is Butler's own `servant` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `settle` | `shall reckon` (B1-P027) | **`phrase-not-word`** | The other head of the same idiom — see (1/2, B, `account`). `settle the account` renders Butler's `reckon with you`, and `settle` in it is not a second use of his own `settle` at B05-P013 and B07-P008. |
| `showed` | `he conducted` (B1-P010) | **`phrase-not-word`** | **Introduced by this package's own repair, and ruled rather than left to be rediscovered.** The B01-P010 repair renders Butler's `he conducted her to a richly decorated seat` as `showed her to a richly worked seat`. `show someone to a seat` is a fixed idiom for conducting them to it; it is not a second use of the simple verb that renders Butler's `shewed`, `manifested` and `displayed` in Books 3 and 4 (see (3/4, B, `showed`)). A repair that trades one collision for another unruled row is not a repair, so the row is ruled here in the same pass that created it. |
| `since` | `inasmuch as` (B1-P004), `since` (B1-P015) | **`common-rendering`** | `since` is a word Butler himself uses in 10 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `sitting` | `seated` (B1-P008), `sitting` (B1-P009) | **`common-rendering`** | `sitting` is a word Butler himself uses in 19 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `someone` | `been missing some one` (B1-P019) | **`artifact`** | `some one` -> `someone`, a typographic normalization (D15, silent). Not a rendering. |
| `something` | `partaken of food` (B1-P009), `something` (B1-P019) | **`common-rendering`** | `something` is a word Butler himself uses in 13 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `spears` | `lances` (B1-P018), `spears` (B1-P010) | **`same-referent`** | `a couple of lances` -> `a couple of spears` (B01-P018), beside Butler's own `spears` eight paragraphs earlier in the same Book. Butler's `lances` and `spears` are the same weapon — he uses `spears` for the rack in Odysseus's hall and `lances` once, of the same arms — so using his own commoner word is precision, not flattening. |
| `spinning` | `distaff` (B1-P025) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `spinning` → `distaff`, and it is the thing to rule on.** Every other entry is Butler's own `spinning` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `distaff` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `spinning` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `their` | `their` (B1-P001), `their` (B1-P004), `their` (B1-P010), `their` (B1-P011), `their` (B1-P012), `their` (B1-P017), `their` (B1-P024), `their` (B1-P028), `their` (B1-P032) | **`kept`** | every entry Book 1 contributes is Butler's own `their` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `there` | `simply` (B1-P017), `there` (B1-P003), `there` (B1-P005), `there` (B1-P008), `there` (B1-P010), `there` (B1-P015), `there` (B1-P016), `there` (B1-P019), `there` (B1-P027), `there` (B1-P029), `there` (B1-P030), `yonder` (B1-P014) | **`common-rendering`** | `there` is a word Butler himself uses in 111 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `those` | `those` (B1-P029) | **`kept`** | every entry Book 1 contributes is Butler's own `those` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `together` | `together` (B1-P006) | **`kept`** | every entry Book 1 contributes is Butler's own `together` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `until` | `until` (B1-P032) | **`phrase-not-word`** | `hitherto` -> `until now` at B07-P018, beside Butler's own `until` elsewhere. `until now` is a fixed adverbial phrase, not a second use of the preposition, and there is no second referent. Upheld. |
| `waste` | `making havoc of` (B1-P017), `waste` (B1-P019) | **`phrase-not-word`** | `they are making havoc of my estate` -> `laying waste to my estate` (B01-P017), beside Butler's own noun `the waste these suitors make` (B01-P019). `lay waste to` is a fixed idiom and the head noun is not a second use of his count noun. |
| `water` | `water` (B1-P008), `water` (B1-P011), `water` (B1-P012) | **`kept`** | every entry Book 1 contributes is Butler's own `water` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `wealth` | `riches` (B1-P029) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `wealth` → `riches`, and it is the thing to rule on.** Every other entry is Butler's own `wealth` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `riches` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `wealth` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `whatever` | `whatsoever` (B1-P001) | **`variant`** | every rendering in the row reduces to `whatever` once inflection, D9 spelling and D15 compound spacing are normalized |
| `where` | `where` (B1-P013), `where` (B1-P014), `where` (B1-P018), `where` (B1-P022), `where` (B1-P030), `whither` (B1-P017) | **`common-rendering`** | `where` is a word Butler himself uses in 66 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `which` | `wherewith` (B1-P008), `which` (B1-P010) | **`common-rendering`** | `which` is a word Butler himself uses in 95 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `wiped` | `cleaning` (B1-P008) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `wiped` → `cleaning`, and it is the thing to rule on.** Every other entry is Butler's own `wiped` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `cleaning` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `wiped` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `without` | `without` (B1-P002), `without` (B1-P017), `without` (B1-P024) | **`kept`** | every entry Book 1 contributes is Butler's own `without` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `woman` | `woman` (B1-P014), `woman` (B1-P032) | **`kept`** | every entry Book 1 contributes is Butler's own `woman` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `worked` | `decorated` (B1-P010) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `worked` → `decorated`, and it is the thing to rule on.** Every other entry is Butler's own `worked` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `decorated` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `worked` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `working` | `pottering about` (B1-P014) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 1's `working` → `pottering about`, and it is the thing to rule on.** Every other entry is Butler's own `working` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `pottering about` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `working` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `young` | `young` (B1-P029), `young` (B1-P032) | **`kept`** | every entry Book 1 contributes is Butler's own `young` carried through unchanged; the decision in this row belongs to whichever Book moved |

## ARROW C — one paragraph, one rendering ← a word Butler keeps in that same paragraph

30 rows.

| key | this Book's entries | disposition | reason |
|---|---|---|---|
| `home` | `back` (B1-P002) | **`common-word`** | `home` is an ordinary word Butler uses in 93 paragraphs and the two uses share no referent |
| `sea` | `ocean` (B1-P005) | **`common-word`** | `sea` is an ordinary word Butler uses in 87 paragraphs and the two uses share no referent |
| `got` | `right` (B1-P005) | **`common-word`** | `got` is an ordinary word Butler uses in 57 paragraphs and the two uses share no referent |
| `down` | `quells` (B1-P008) | **`common-word`** | `down` is an ordinary word Butler uses in 60 paragraphs and the two uses share no referent |
| `set` | `took` (B1-P010) | **`common-word`** | `set` is an ordinary word Butler uses in 48 paragraphs and the two uses share no referent |
| `poured` | `brought` (B1-P011) | **`artifact`** | Butler's `a manservant brought them wine and poured it out for them` became `a manservant poured them wine` — one doublet compressed, not one word rendered as another. The diff paired `brought` with `poured` across the compression; Butler's own `poured it into a silver basin` is kept unchanged. |
| `brought` | `fetched them` (B1-P011) | **`common-word`** | `brought` is an ordinary word Butler uses in 32 paragraphs and the two uses share no referent |
| `set` | `offered` (B1-P011) | **`common-word`** | `set` is an ordinary word Butler uses in 48 paragraphs and the two uses share no referent |
| `truly` | `and tell me true` (B1-P013) | **`variant`** | `tell me and tell me true` and `Tell me also truly` are Butler's own formula in two forms of ONE word, and the candidate renders both `tell me truly`. There is no discrimination to lose: `true` and `truly` are the adjective and adverb of the same lexeme, and the doubling is Homeric formula rather than a distinction. |
| `said` | `declared themselves to be` (B1-P013) | **`common-word`** | `said` is an ordinary word Butler uses in 143 paragraphs and the two uses share no referent |
| `about` | `going` (B1-P013) | **`common-word`** | `about` is an ordinary word Butler uses in 110 paragraphs and the two uses share no referent |
| `people` | `nation` (B1-P013) | **`common-word`** | `people` is an ordinary word Butler uses in 64 paragraphs and the two uses share no referent |
| `tell` | `say that` (B1-P013) | **`common-word`** | `tell` is an ordinary word Butler uses in 62 paragraphs and the two uses share no referent |
| `men` | `those` (B1-P013) | **`common-word`** | `men` is an ordinary word Butler uses in 82 paragraphs and the two uses share no referent |
| `truly` | `and tell me true` (B1-P014) | **`variant`** | The same formula and the same ruling as at B01-P013: `I will tell you truly` and `tell me, and tell me true` are one word in two forms. |
| `away` | `back` (B1-P014) | **`common-word`** | `away` is an ordinary word Butler uses in 48 paragraphs and the two uses share no referent |
| `longer` | `never` (B1-P014) | **`phrase-not-word`** | `he never comes to town now` -> `he no longer comes to town now`. `no longer` is a fixed adverbial phrase, not a second use of the comparative in Butler's own `he will not be away much longer` and `if I keep them waiting longer`, which are durations. Same shape as (B, `until`) at Book 7. |
| `same` | `so also with myself` (B1-P017) | **`homograph`** | **Not a collision, and a pleasing one.** The word arrow C finds `kept` in this paragraph is **`Same` the island** — `Dulichium, Same, and wooded Zacynthus` — a proper noun. The rendering is `they will do the same to me`, the ordinary pronoun. Two words spelled alike with nothing whatever in common, which is `issue` at Book 7 in its purest form. |
| `tell` | `bid` (B1-P019) | **`common-word`** | `tell` is an ordinary word Butler uses in 62 paragraphs and the two uses share no referent |
| `find` | `get` (B1-P019) | **`common-word`** | `find` is an ordinary word Butler uses in 29 paragraphs and the two uses share no referent |
| `marriage` | `marry` (B1-P019) | **`variant`** | `make your mother marry again` -> `give your mother in marriage again`, beside Butler's own `all the marriage gifts` in the same paragraph. One lexeme, verb and noun; the recast changed the part of speech. |
| `home` | `place` (B1-P019) | **`common-word`** | `home` is an ordinary word Butler uses in 93 paragraphs and the two uses share no referent |
| `once` | `then having` (B1-P019) | **`common-word`** | `once` is an ordinary word Butler uses in 43 paragraphs and the two uses share no referent |
| `give` | `make` (B1-P021) | **`common-word`** | `give` is an ordinary word Butler uses in 38 paragraphs and the two uses share no referent |
| `held` | `supported` (B1-P023) | **`phrase-not-word`** | `the bearing posts that supported the roof` -> `the pillars that held up the roof`, beside Butler's own `She held a veil before her face`. `hold up` is a phrasal verb meaning support; `held a veil` is the simple transitive. Two verbs that share a syllable, and no referent is shared. |
| `man` | `fellow` (B1-P025) | **`common-word`** | `man` is an ordinary word Butler uses in 100 paragraphs and the two uses share no referent |
| `took` | `did not take` (B1-P032) | **`common-word`** | `took` is an ordinary word Butler uses in 66 paragraphs and the two uses share no referent |
| `out` | `that looked on to` (B1-P032) | **`common-word`** | `out` is an ordinary word Butler uses in 89 paragraphs and the two uses share no referent |
| `went` | `then returned` (B1-P032) | **`common-word`** | `went` is an ordinary word Butler uses in 85 paragraphs and the two uses share no referent |
| `woman` | `women` (B1-P032) | **`variant`** | One word inflected for number, and the inflection follows the recast: `better than any of the other women in the house` becomes `better than any other woman in the house`, which is singular because the comparison now has one term. Butler's own `A good old woman, Euryclea` stands. The `husbands` ruling at Book 7 exactly. |

