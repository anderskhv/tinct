# Odyssey Book 1 — the collision report, dispositioned

Written by `scripts/collision_triage.py`. Every row of every arrow that touches
this Book, with a disposition on each. **Records finding R-2 and blind spot 8**
of `book07/review/findings-v1.md`: the check returned seventy-two rows touching
Book 7, eleven were acted on, and sixty-one were dismissed with no record of
having been read. This file is that record, and it is generated, not typed.

**164 rows touch Book 1.**

| disposition | rows |
|---|---|
| `artifact` | 4 |
| `common-rendering` | 14 |
| `common-word` | 22 |
| `context-rendered` | 4 |
| `free-variation` | 1 |
| `homograph` | 4 |
| `kept` | 20 |
| `kept-elsewhere` | 58 |
| `phrase-not-word` | 7 |
| `repair` | 5 |
| `same-referent` | 3 |
| `unavoidable-merge` | 7 |
| `variant` | 15 |

The vocabulary is defined in the script's docstring. `homograph`,
`same-referent`, `unavoidable-merge` and `phrase-not-word` are new here;
`homograph` is the one the review asked for by name.

## ARROW A — one Butler word, two or more renderings, across Books

55 rows.

| key | this Book's entries | disposition | reason |
|---|---|---|---|
| `accept` | `receive` (B1-P003) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`receive`); every other entry is Butler's own `accept` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `accept` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `around` | `once` (B1-P017) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`once`); every other entry is Butler's own `around` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `around` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `behaving` | `behave` (B1-P016) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`behave`); every other entry is Butler's own `behaving` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `behaving` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `bottom` | `has charge` (B1-P005) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`has charge`); every other entry is Butler's own `bottom` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `bottom` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `carries` | `holds up` (B1-P005) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`holds up`); every other entry is Butler's own `carries` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `carries` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `cease` | `stop` (B1-P024) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`stop`); every other entry is Butler's own `cease` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `cease` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `ceasing` | `end` (B1-P024), `pause` (B1-P002) | **`context-rendered`** | Butler writes `without ceasing` twice in Book 1 and the candidate renders it `without end` (B01-P024) and `without pause` (B01-P002). The two are not interchangeable: a widow's mourning is endless, a god's persecution is unremitting, and `mourn without pause` and `persecuted him without end` are both wrong. The renderings differ because the contexts do. |
| `celebrate` | `hold` (B1-P019), `sing` (B1-P024) | **`homograph`** | One Butler spelling, two unrelated senses. `celebrate his funeral rites` is *perform* (B01-P019 and B02-P012 — the same Butler sentence in two Books, rendered `hold` in both, which is the consistency the package asks for). `such as poets love to celebrate` is *sing of*. **The B01-P024 rendering was separately live and is repaired**: see the arrow C row (1, 24, `celebrate`, `sing`). |
| `closely` | `completely` (B1-P017) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`completely`); every other entry is Butler's own `closely` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `closely` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `conduct` | `take` (B1-P007) | **`context-rendered`** | `the girl who was to conduct him to the city` -> `guide` (B06-P010) and `I will also conduct him to Sparta` -> `take` (B01-P007). Nausicaa shows Odysseus a way he does not know; Athena proposes to convey Telemachus on a voyage she is arranging. `guide him to Sparta` would say Telemachus cannot find it, and `take him to the city` would take the showing out of Nausicaa's hands. |
| `conducted` | `led` (B1-P010) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`led`); every other entry is Butler's own `conducted` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `conducted` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `couple` | `couple` (B1-P018), `pair` (B1-P032) | **`free-variation`** | `a couple of lances` keeps `a couple of` at B01-P018; `a couple of blazing torches` becomes `a pair of` at B01-P032. Fourteen paragraphs apart, no shared referent, and no reader can attach a meaning to the difference — `a couple of` and `a pair of` are interchangeable modern English. Recorded rather than repaired: a successor to an accepted Book for this would buy nothing. |
| `decorated` | `worked` (B1-P010) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`worked`); every other entry is Butler's own `decorated` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `decorated` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `depart` | `leave` (B1-P027) | **`variant`** | every rendering in the row reduces to `leave` once inflection, D9 spelling and D15 compound spacing are normalized |
| `detaining` | `holding` (B1-P014) | **`context-rendered`** | `savages who are detaining him against his will` -> `holding` (B01-P014); `you are detaining me from them` -> `keeping me from them` (B04-P050). The first is captivity and the second is delay, and English does not use one verb for both: `holding me from them` is not idiomatic and `keeping him against his will` loses the imprisonment. |
| `distaff` | `spinning` (B1-P025) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`spinning`); every other entry is Butler's own `distaff` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `distaff` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `faring` | `faring` (B1-P014) | **`artifact`** | Butler writes `sea-faring` with a hyphen and the tokenizer splits it, so `faring` is not a word either text uses. D15 closes it to `seafaring`, matching accepted B06-P021. There is no rendering pair. |
| `feared` | `fear of` (B1-P032), `feared` (B1-P018) | **`variant`** | One word in two forms. B01-P018 keeps Butler's verb (`Ilus feared the everlasting gods`); B01-P032 recasts `for he feared his wife's resentment` to the noun `for fear of his wife's anger`. Same lexeme, same sense, a part-of-speech change made by the recast. |
| `flower` | `finest` (B1-P014) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`finest`); every other entry is Butler's own `flower` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `flower` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `glittering` | `bright` (B1-P008) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`bright`); every other entry is Butler's own `glittering` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `glittering` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `grasped` | `and took up` (B1-P008) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`and took up`); every other entry is Butler's own `grasped` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `grasped` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `handmaids` | `maids` (B1-P023), `maids` (B1-P026) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`maids`); every other entry is Butler's own `handmaids` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `handmaids` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `havoc` | `laying waste to` (B1-P017) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`laying waste to`); every other entry is Butler's own `havoc` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `havoc` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `hearers` | `listeners` (B1-P023) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`listeners`); every other entry is Butler's own `hearers` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `hearers` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `hecatomb` | `great sacrifice` (B1-P003) | **`same-referent`** | A hecatomb IS a sacrifice, so the rendering keeps Butler's referent and marks what would otherwise be lost — the scale — with an adjective: `great sacrifice` (B01-P003) and `fine sacrifice` (B03-P007, where `fine` is carrying Butler's own `goodly` and the scale word has nowhere to stand). **Accepted Book 4 was the one out of step**, rendering `holy hecatombs` as a bare `holy sacrifices` one sentence from Butler's own `sacrifices`; the successor `book04/candidate-v5.json` brings it into line. See the arrow C row (4, 40, `hecatombs`, `sacrifices`). |
| `inherit` | `have inherited` (B1-P017) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`have inherited`); every other entry is Butler's own `inherit` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `inherit` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `insolence` | `rudeness` (B1-P010) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`rudeness`); every other entry is Butler's own `insolence` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `insolence` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `laying` | `set` (B1-P008) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`set`); every other entry is Butler's own `laying` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `laying` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `lighted` | `lit` (B1-P032) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`lit`); every other entry is Butler's own `lighted` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `lighted` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `living` | `everlasting` (B1-P018) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`everlasting`); every other entry is Butler's own `living` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `living` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `longing` | `longed` (B1-P002) | **`variant`** | every rendering in the row reduces to `long` once inflection, D9 spelling and D15 compound spacing are normalized |
| `looks` | `has charge` (B1-P005) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`has charge`); every other entry is Butler's own `looks` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `looks` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `lordly` | `proud` (B1-P008) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`proud`); every other entry is Butler's own `lordly` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `lordly` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `marvelled` | `listened amazed` (B1-P028) | **`artifact`** | Butler's `as they heard him, and marvelled at the boldness of his speech` became `as they listened, amazed at the boldness of his speech` — `heard` -> `listened` and the finite verb absorbed into a participial phrase. The diff paired `marvelled` with the two-word span `listened amazed`; there is no rendering pair to rule on. **The live question in this family is the opposite direction** and is ruled at (1, B, `marveled`). |
| `meantime` | `meanwhile` (B1-P007) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`meanwhile`); every other entry is Butler's own `meantime` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `meantime` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `nation` | `people` (B1-P013) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`people`); every other entry is Butler's own `nation` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `nation` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `offended` | `take offense at` (B1-P013) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`take offense at`); every other entry is Butler's own `offended` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `offended` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `persist` | `go on` (B1-P007), `go on feeding off` (B1-P027) | **`artifact`** | One rendering, `go on`, in both entries. B01-P027's longer span is Butler's `persist in spunging upon one man`, where `spunging` had to move as well and the diff took the two together as `go on feeding off`. |
| `possessions` | `property` (B1-P030) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`property`); every other entry is Butler's own `possessions` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `possessions` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `prevail` | `urge` (B1-P019) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`urge`); every other entry is Butler's own `prevail` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `prevail` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `prevented` | `kept` (B1-P001) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`kept`); every other entry is Butler's own `prevented` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `prevented` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `pulled` | `pulling` (B1-P032) | **`variant`** | every rendering in the row reduces to `pull` once inflection, D9 spelling and D15 compound spacing are normalized |
| `refreshed` | `rested then` (B1-P020) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`rested then`); every other entry is Butler's own `refreshed` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `refreshed` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `riches` | `wealth` (B1-P029) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`wealth`); every other entry is Butler's own `riches` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `riches` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `seated` | `sitting` (B1-P008) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`sitting`); every other entry is Butler's own `seated` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `seated` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `serve` | `be no use to` (B1-P013) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`be no use to`); every other entry is Butler's own `serve` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `serve` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `shrift` | `quick end` (B1-P018) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`quick end`); every other entry is Butler's own `shrift` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `shrift` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `smart` | `capable` (B1-P019) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`capable`); every other entry is Butler's own `smart` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `smart` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `storm` | `storms` (B1-P017) | **`variant`** | every rendering in the row reduces to `storm` once inflection, D9 spelling and D15 compound spacing are normalized |
| `thence` | `then` (B1-P019) | **`context-rendered`** | `moved them thence and settled them in Scheria` -> `from there` (B06-P001) is spatial; `thence go on to Sparta` -> `then on to Sparta` (B01-P019) is a step in an itinerary, where English says `then`. `moved them then` is wrong and `from there go on to Sparta` reads as though Pylos were the obstacle. |
| `tired` | `sick` (B1-P005), `tired` (B1-P014) | **`homograph`** | One spelling, two unrelated senses. `he is tired of life` -> `sick of life` (B01-P005) is satiety; `comes in tired from working his vineyard` (B01-P014) is fatigue, and Butler's word is kept because it is current English there. `sick of life` and `tired from the vineyard` are two words, not one rendered twice. |
| `value` | `valuable` (B1-P020), `value` (B1-P021) | **`variant`** | One word in two forms, twelve lines apart: `a gift of great beauty and value` becomes the adjective `something fine and valuable` (B01-P020), and the noun is kept at B01-P021 (`one of no less value in return`). The recast of the first changed the part of speech, not the word. |
| `wilderness` | `the wild` (B1-P013) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`the wild`); every other entry is Butler's own `wilderness` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `wilderness` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `wondered` | `in himself marveled` (B1-P022) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`in himself marveled`); every other entry is Butler's own `wondered` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `wondered` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `yonder` | `there` (B1-P014) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`there`); every other entry is Butler's own `yonder` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `yonder` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |

## ARROW B — one rendering, two or more Butler words, across Books

76 rows.

| key | this Book's entries | disposition | reason |
|---|---|---|---|
| `account` | `shall reckon` (B1-P027) | **`phrase-not-word`** | `Jove shall reckon with you in full` -> `Zeus will settle the account with you in full`, word for word the same in both accepted Books, of Butler's own sentence repeated verbatim across them. `settle the account` is one fixed idiom; its two head words happen to coincide with Butler's own `account` (three times in Book 4) and his own `settle` (Books 5 and 7). One idiom, not two borrowed words. Ruled the same way from the other head at (1/2, B, `settle`). |
| `after` | `after` (B1-P001), `after` (B1-P004), `after` (B1-P014) | **`kept`** | every entry Book 1 contributes is Butler's own `after` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `among` | `among` (B1-P002), `among` (B1-P009), `among` (B1-P014), `among` (B1-P029), `among` (B1-P030) | **`variant`** | every rendering in the row reduces to `among` once inflection, D9 spelling and D15 compound spacing are normalized |
| `anger` | `resentment she` (B1-P032) | **`unavoidable-merge`** | `anger` carries Butler's own `anger` in five Books, his `wrath of heaven` (B02-P004) and his `wife's resentment` (B01-P032). `wrath` is elevated and archaic and `anger` is its plain modern equivalent; `resentment` is current English and needed no change, but the referent is the same jealousy and the alternative is a register distinction Butler is not making. |
| `apart` | `asunder` (B1-P005) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`asunder`); every other entry is Butler's own `apart` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `apart` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `arrows` | `arrows` (B1-P018) | **`kept`** | every entry Book 1 contributes is Butler's own `arrows` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `being` | `being` (B1-P005) | **`kept`** | every entry Book 1 contributes is Butler's own `being` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `beside` | `beside` (B1-P011) | **`kept`** | every entry Book 1 contributes is Butler's own `beside` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `bright` | `glittering` (B1-P008) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`glittering`); every other entry is Butler's own `bright` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `bright` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `brings` | `brings` (B1-P029), `quells` (B1-P008) | **`phrase-not-word`** | `wherewith she quells the ranks of heroes` -> `with which she brings down the ranks of heroes` (B01-P008). `bring down` is a phrasal verb and Butler's own `it brings both wealth and honor` (B01-P029) is the simple transitive. Different verbs that share a syllable. |
| `capable` | `capable` (B1-P006), `smart` (B1-P019) | **`repair`** | **Live, and the sharpest of this Book's arrow B rows.** Butler calls Odysseus `no more capable man on earth` (B01-P006) and Telemachus `a fine, smart looking fellow` (B01-P019); the candidate writes `capable` for both. It is a sense change as well as a collision: Butler's `smart looking` is about APPEARANCE — 1900 British `smart` is well turned out — and `capable-looking` makes Athena praise a competence she is at that moment telling him he has not yet shown. Repaired in `book01/candidate-v4.json` to `a fine, good-looking young man`. |
| `carried` | `spirited` (B1-P017) | **`common-rendering`** | `carried` is a word Butler himself uses in 13 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `charge` | `looks after the bottom` (B1-P005) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`looks after the bottom`); every other entry is Butler's own `charge` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `charge` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `completely` | `closely` (B1-P017) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`closely`); every other entry is Butler's own `completely` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `completely` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `decide` | `decide` (B1-P030), `determine` (B1-P019) | **`unavoidable-merge`** | `It rests with heaven to determine` (B01-P019) and `It rests with heaven to decide` (B01-P030) — Butler's own two verbs in the same formula, eleven paragraphs apart, with no difference between them. `decide` is the plain modern word for both and keeping `determine` would invent a distinction Butler did not make. |
| `eating` | `eating` (B1-P007), `eating` (B1-P017) | **`kept`** | every entry Book 1 contributes is Butler's own `eating` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `enough` | `enough` (B1-P016) | **`kept`** | every entry Book 1 contributes is Butler's own `enough` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `everlasting` | `ever living` (B1-P018) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`ever living`); every other entry is Butler's own `everlasting` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `everlasting` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `finest` | `flower` (B1-P014) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`flower`); every other entry is Butler's own `finest` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `finest` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `flattery` | `blandishment` (B1-P005) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`blandishment`); every other entry is Butler's own `flattery` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `flattery` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `front` | `front` (B1-P008) | **`kept`** | every entry Book 1 contributes is Butler's own `front` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `going` | `going` (B1-P026) | **`kept`** | every entry Book 1 contributes is Butler's own `going` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `great` | `great` (B1-P005), `great` (B1-P008), `great` (B1-P023), `great` (B1-P024), `great` (B1-P029), `hecatomb` (B1-P003) | **`common-rendering`** | `great` is a word Butler himself uses in 54 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `grieving` | `sorrowful` (B1-P024) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`sorrowful`); every other entry is Butler's own `grieving` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `grieving` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `guide` | `direct` (B1-P019) | **`unavoidable-merge`** | `guide` carries Butler's `conduct` (B06-P010), his `direct` (B01-P019 and B02-P012 — the same sentence in two Books, rendered identically) and his own `guide` (B05-P005). All three are Butler's free variants for showing somebody a way; `some heaven-sent message may direct you` and `may guide you` are the same sentence in his own idiom, and no discrimination exists to lose. |
| `heads` | `heads` (B1-P006) | **`kept`** | every entry Book 1 contributes is Butler's own `heads` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `himself` | `himself` (B1-P003), `himself` (B1-P010), `himself` (B1-P013), `wondered` (B1-P022) | **`common-rendering`** | `himself` is a word Butler himself uses in 35 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `holding` | `who are detaining` (B1-P014) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`who are detaining`); every other entry is Butler's own `holding` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `holding` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `hurried` | `hurried` (B1-P030), `were bustling` (B1-P008) | **`unavoidable-merge`** | `Men-servants and pages were bustling about` -> `hurried about` (B01-P008), beside Butler's own `he hurried off` (B01-P030). The two are twenty-two paragraphs apart and share no referent; `bustle` is current English and could have been kept, but nothing in the text turns on the difference between bustling and hurrying servants. |
| `husband` | `husband` (B1-P019), `husband` (B1-P026) | **`kept`** | every entry Book 1 contributes is Butler's own `husband` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `keeping` | `keeping` (B1-P014), `preventing` (B1-P006) | **`unavoidable-merge`** | `keeping` carries Butler's `detaining` (B04-P050), his `preventing him from getting home` (B01-P006) and his own `keeping` in five Books. `keep X from Y` is the plain modern idiom for both, and the three instances are in three Books with no shared referent. |
| `laying` | `making havoc of` (B1-P017) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`making havoc of`); every other entry is Butler's own `laying` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `laying` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `leave` | `depart` (B1-P027) | **`common-rendering`** | `leave` is a word Butler himself uses in 23 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `lives` | `lives` (B1-P005), `lives` (B1-P014) | **`kept`** | every entry Book 1 contributes is Butler's own `lives` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `maids` | `handmaids` (B1-P023), `handmaids into her room` (B1-P026), `maids` (B1-P012) | **`common-rendering`** | `maids` is a word Butler himself uses in 23 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `marveled` | `wondered` (B1-P022) | **`repair`** | **Live, and avoidable, which is what makes it worth a successor.** Butler writes `He felt the change, wondered at it` at B01-P022 and the candidate writes `marveled at it` — putting `marveled` where Butler wrote `wondered`, while accepted Books 2 and 3 use `marveled` for his own `marvelled`. Two Butler words flattened into one across Books, and the substitution buys nothing: `wondered at it` is current English and was already right. Repaired in `book01/candidate-v4.json` by restoring Butler's `wondered`. |
| `matter` | `matters` (B1-P017) | **`variant`** | every rendering in the row reduces to `matter` once inflection, D9 spelling and D15 compound spacing are normalized |
| `meanwhile` | `in the meantime` (B1-P007) | **`common-rendering`** | `meanwhile` is a word Butler himself uses in 10 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `morning` | `morning` (B1-P019), `morning` (B1-P027) | **`kept`** | every entry Book 1 contributes is Butler's own `morning` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `offense` | `be offended with` (B1-P013) | **`unavoidable-merge`** | D9, `offence` -> `offense`, plus one recast: Butler's `you will not be offended with what I am going to say` (B01-P013) becomes `you will not take offense at what I am about to say`, which is the idiom Butler HIMSELF uses at B02-P008 and B06-P012 (`take offence at`). The merge makes Book 1 agree with two accepted Books on one speech act. |
| `opened` | `opened` (B1-P032) | **`variant`** | every rendering in the row reduces to `open` once inflection, D9 spelling and D15 compound spacing are normalized |
| `others` | `some cutting` (B1-P008) | **`common-rendering`** | `others` is a word Butler himself uses in 20 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `people` | `nation` (B1-P013), `people` (B1-P002), `people` (B1-P007), `people` (B1-P013), `people` (B1-P016), `people` (B1-P019), `people` (B1-P025) | **`common-rendering`** | `people` is a word Butler himself uses in 54 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `playing` | `playing` (B1-P008) | **`homograph`** | One spelling, two unrelated senses. `the playing that goes with it` at B08-P007 renders Butler's `minstrelsy`; his own `playing` elsewhere is children at play (B01-P008, B07-P023) and a flame playing about a tripod (B08-P040). No referent is shared. |
| `property` | `possessions` (B1-P030) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`possessions`); every other entry is Butler's own `property` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `property` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `proud` | `lordly` (B1-P008) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`lordly`); every other entry is Butler's own `proud` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `proud` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `pulling` | `pulled` (B1-P032) | **`variant`** | every rendering in the row reduces to `pull` once inflection, D9 spelling and D15 compound spacing are normalized |
| `quick` | `short shrift` (B1-P018) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`short shrift`); every other entry is Butler's own `quick` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `quick` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `rested` | `refreshed yourself` (B1-P020) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`refreshed yourself`); every other entry is Butler's own `rested` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `rested` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `sacrifice` | `hecatomb` (B1-P003) | **`same-referent`** | The other direction of (1/3, A, `hecatomb`). `sacrifice` carries Butler's `hecatomb` and his own `sacrifice`, and a hecatomb is a sacrifice — Butler's own other word for the same act. Both Books mark what the merge would otherwise cost with an adjective of scale. |
| `scold` | `chide with` (B1-P029) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`chide with`); every other entry is Butler's own `scold` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `scold` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `search` | `quest` (B1-P019) | **`unavoidable-merge`** | **Butler himself alternates, inside one speech, which settles it.** He writes `sail the seas in search of my father who has so long been missing` at B02-P018 and `go to Sparta and to Pylos in quest of my father who has so long been missing` at B02-P012 — the same formula, six paragraphs apart, in his own two words. `quest` is archaic outside fixed phrases; `search` is what he means by both, and three accepted Books render it identically. |
| `servant` | `servant` (B1-P011), `servant` (B1-P012) | **`kept`** | every entry Book 1 contributes is Butler's own `servant` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `settle` | `shall reckon` (B1-P027) | **`phrase-not-word`** | The other head of the same idiom — see (1/2, B, `account`). `settle the account` renders Butler's `reckon with you`, and `settle` in it is not a second use of his own `settle` at B05-P013 and B07-P008. |
| `since` | `inasmuch as` (B1-P004), `since` (B1-P015) | **`common-rendering`** | `since` is a word Butler himself uses in 9 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `sitting` | `seated` (B1-P008), `sitting` (B1-P009) | **`common-rendering`** | `sitting` is a word Butler himself uses in 18 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `something` | `partaken of food` (B1-P009), `something` (B1-P019) | **`common-rendering`** | `something` is a word Butler himself uses in 13 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `spears` | `lances` (B1-P018), `spears` (B1-P010) | **`same-referent`** | `a couple of lances` -> `a couple of spears` (B01-P018), beside Butler's own `spears` eight paragraphs earlier in the same Book. Butler's `lances` and `spears` are the same weapon — he uses `spears` for the rack in Odysseus's hall and `lances` once, of the same arms — so using his own commoner word is precision, not flattening. |
| `spinning` | `distaff` (B1-P025) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`distaff`); every other entry is Butler's own `spinning` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `spinning` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `their` | `their` (B1-P001), `their` (B1-P004), `their` (B1-P010), `their` (B1-P011), `their` (B1-P012), `their` (B1-P017), `their` (B1-P024), `their` (B1-P028), `their` (B1-P032) | **`kept`** | every entry Book 1 contributes is Butler's own `their` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `there` | `simply` (B1-P017), `there` (B1-P003), `there` (B1-P005), `there` (B1-P008), `there` (B1-P010), `there` (B1-P015), `there` (B1-P016), `there` (B1-P019), `there` (B1-P027), `there` (B1-P029), `there` (B1-P030), `yonder` (B1-P014) | **`common-rendering`** | `there` is a word Butler himself uses in 100 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `those` | `those` (B1-P029) | **`kept`** | every entry Book 1 contributes is Butler's own `those` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `together` | `together` (B1-P006) | **`kept`** | every entry Book 1 contributes is Butler's own `together` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `until` | `until` (B1-P032) | **`phrase-not-word`** | `hitherto` -> `until now` at B07-P018, beside Butler's own `until` elsewhere. `until now` is a fixed adverbial phrase, not a second use of the preposition, and there is no second referent. Upheld. |
| `waste` | `making havoc of` (B1-P017), `waste` (B1-P019) | **`phrase-not-word`** | `they are making havoc of my estate` -> `laying waste to my estate` (B01-P017), beside Butler's own noun `the waste these suitors make` (B01-P019). `lay waste to` is a fixed idiom and the head noun is not a second use of his count noun. |
| `water` | `water` (B1-P008), `water` (B1-P011), `water` (B1-P012) | **`kept`** | every entry Book 1 contributes is Butler's own `water` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `wealth` | `riches` (B1-P029) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`riches`); every other entry is Butler's own `wealth` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `wealth` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `whatever` | `whatsoever` (B1-P001) | **`variant`** | every rendering in the row reduces to `whatever` once inflection, D9 spelling and D15 compound spacing are normalized |
| `where` | `where` (B1-P013), `where` (B1-P014), `where` (B1-P018), `where` (B1-P022), `where` (B1-P030), `whither` (B1-P017) | **`common-rendering`** | `where` is a word Butler himself uses in 58 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `which` | `wherewith` (B1-P008), `which` (B1-P010) | **`common-rendering`** | `which` is a word Butler himself uses in 81 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `wiped` | `cleaning` (B1-P008) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`cleaning`); every other entry is Butler's own `wiped` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `wiped` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `without` | `without` (B1-P002), `without` (B1-P017), `without` (B1-P024) | **`kept`** | every entry Book 1 contributes is Butler's own `without` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `woman` | `woman` (B1-P014), `woman` (B1-P032) | **`kept`** | every entry Book 1 contributes is Butler's own `woman` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `worked` | `decorated` (B1-P010) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`decorated`); every other entry is Butler's own `worked` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `worked` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `working` | `pottering about` (B1-P014) | **`kept-elsewhere`** | Book 1 supplies the row's only rendering (`pottering about`); every other entry is Butler's own `working` carried through unchanged by another Book. There is one rendering decision in this row, not two, so there is nothing to reconcile. **Declared blind:** this class cannot say whether a Book that KEPT `working` should also have moved it. That residue is ruled by hand in `RULINGS_BY_BOOK` wherever the kept word is not current English in its own context (see `luscious` at Book 5). |
| `young` | `young` (B1-P029), `young` (B1-P032) | **`kept`** | every entry Book 1 contributes is Butler's own `young` carried through unchanged; the decision in this row belongs to whichever Book moved |

## ARROW C — one paragraph, one rendering ← a word Butler keeps in that same paragraph

33 rows.

| key | this Book's entries | disposition | reason |
|---|---|---|---|
| `home` | `back` (B1-P002) | **`common-word`** | `home` is an ordinary word Butler uses in 85 paragraphs and the two uses share no referent |
| `sea` | `ocean` (B1-P005) | **`common-word`** | `sea` is an ordinary word Butler uses in 74 paragraphs and the two uses share no referent |
| `got` | `right` (B1-P005) | **`common-word`** | `got` is an ordinary word Butler uses in 44 paragraphs and the two uses share no referent |
| `down` | `quells` (B1-P008) | **`common-word`** | `down` is an ordinary word Butler uses in 47 paragraphs and the two uses share no referent |
| `led` | `he conducted` (B1-P010) | **`repair`** | **Live.** Butler writes `He led the way as he spoke` and, twenty-five words later, `he conducted her to a richly decorated seat`; the candidate writes `led` for both and produces a repetition Butler avoided. This is the M-2 shape exactly. Repaired in `book01/candidate-v4.json` to `showed her to a richly worked seat`, which is the ordinary modern English for conducting somebody to a seat. |
| `set` | `took` (B1-P010) | **`common-word`** | `set` is an ordinary word Butler uses in 45 paragraphs and the two uses share no referent |
| `poured` | `brought` (B1-P011) | **`artifact`** | Butler's `a manservant brought them wine and poured it out for them` became `a manservant poured them wine` — one doublet compressed, not one word rendered as another. The diff paired `brought` with `poured` across the compression; Butler's own `poured it into a silver basin` is kept unchanged. |
| `brought` | `fetched them` (B1-P011) | **`common-word`** | `brought` is an ordinary word Butler uses in 29 paragraphs and the two uses share no referent |
| `set` | `offered` (B1-P011) | **`common-word`** | `set` is an ordinary word Butler uses in 45 paragraphs and the two uses share no referent |
| `truly` | `and tell me true` (B1-P013) | **`variant`** | `tell me and tell me true` and `Tell me also truly` are Butler's own formula in two forms of ONE word, and the candidate renders both `tell me truly`. There is no discrimination to lose: `true` and `truly` are the adjective and adverb of the same lexeme, and the doubling is Homeric formula rather than a distinction. |
| `said` | `declared themselves to be` (B1-P013) | **`common-word`** | `said` is an ordinary word Butler uses in 128 paragraphs and the two uses share no referent |
| `about` | `going` (B1-P013) | **`common-word`** | `about` is an ordinary word Butler uses in 101 paragraphs and the two uses share no referent |
| `people` | `nation` (B1-P013) | **`common-word`** | `people` is an ordinary word Butler uses in 54 paragraphs and the two uses share no referent |
| `tell` | `say that` (B1-P013) | **`common-word`** | `tell` is an ordinary word Butler uses in 56 paragraphs and the two uses share no referent |
| `men` | `those` (B1-P013) | **`common-word`** | `men` is an ordinary word Butler uses in 68 paragraphs and the two uses share no referent |
| `truly` | `and tell me true` (B1-P014) | **`variant`** | The same formula and the same ruling as at B01-P013: `I will tell you truly` and `tell me, and tell me true` are one word in two forms. |
| `away` | `back` (B1-P014) | **`common-word`** | `away` is an ordinary word Butler uses in 41 paragraphs and the two uses share no referent |
| `longer` | `never` (B1-P014) | **`phrase-not-word`** | `he never comes to town now` -> `he no longer comes to town now`. `no longer` is a fixed adverbial phrase, not a second use of the comparative in Butler's own `he will not be away much longer` and `if I keep them waiting longer`, which are durations. Same shape as (B, `until`) at Book 7. |
| `same` | `so also with myself` (B1-P017) | **`homograph`** | **Not a collision, and a pleasing one.** The word arrow C finds `kept` in this paragraph is **`Same` the island** — `Dulichium, Same, and wooded Zacynthus` — a proper noun. The rendering is `they will do the same to me`, the ordinary pronoun. Two words spelled alike with nothing whatever in common, which is `issue` at Book 7 in its purest form. |
| `tell` | `bid` (B1-P019) | **`common-word`** | `tell` is an ordinary word Butler uses in 56 paragraphs and the two uses share no referent |
| `find` | `get` (B1-P019) | **`common-word`** | `find` is an ordinary word Butler uses in 26 paragraphs and the two uses share no referent |
| `marriage` | `marry` (B1-P019) | **`variant`** | `make your mother marry again` -> `give your mother in marriage again`, beside Butler's own `all the marriage gifts` in the same paragraph. One lexeme, verb and noun; the recast changed the part of speech. |
| `home` | `place` (B1-P019) | **`common-word`** | `home` is an ordinary word Butler uses in 85 paragraphs and the two uses share no referent |
| `urge` | `prevail upon` (B1-P019) | **`repair`** | **Live, and inside one speech.** Butler: `I would, however, urge you to set about trying to get rid of these suitors` and, a hundred words later in the same speech by the same speaker, `let me prevail upon you to take the best ship you can get`. The candidate writes `urge` for both. Butler's two verbs are two different degrees of pressing — advice, then persuasion — and the sentence that loses the second is the one where Athena asks for the voyage the whole Book turns on. Repaired in `book01/candidate-v4.json` to `let me persuade you`. |
| `once` | `then having` (B1-P019) | **`common-word`** | `once` is an ordinary word Butler uses in 37 paragraphs and the two uses share no referent |
| `give` | `make` (B1-P021) | **`common-word`** | `give` is an ordinary word Butler uses in 35 paragraphs and the two uses share no referent |
| `held` | `supported` (B1-P023) | **`phrase-not-word`** | `the bearing posts that supported the roof` -> `the pillars that held up the roof`, beside Butler's own `She held a veil before her face`. `hold up` is a phrasal verb meaning support; `held a veil` is the simple transitive. Two verbs that share a syllable, and no referent is shared. |
| `sing` | `celebrate` (B1-P024) | **`repair`** | **Live, and the candidate's own sentence convicts it.** Butler: `you know many another feat of gods and heroes, such as poets love to celebrate. Sing the suitors some one of these`. The candidate writes `that poets love to sing. Sing the suitors one of those` — the word twice at the join of two sentences, where Butler had two words and no repetition. Repaired in `book01/candidate-v4.json` by restoring Butler's `celebrate`, which is current English in this construction and needed no change; the same repair as `fashioned` at Book 7. |
| `man` | `fellow` (B1-P025) | **`common-word`** | `man` is an ordinary word Butler uses in 86 paragraphs and the two uses share no referent |
| `took` | `did not take` (B1-P032) | **`common-word`** | `took` is an ordinary word Butler uses in 54 paragraphs and the two uses share no referent |
| `out` | `that looked on to` (B1-P032) | **`common-word`** | `out` is an ordinary word Butler uses in 69 paragraphs and the two uses share no referent |
| `went` | `then returned` (B1-P032) | **`common-word`** | `went` is an ordinary word Butler uses in 74 paragraphs and the two uses share no referent |
| `woman` | `women` (B1-P032) | **`variant`** | One word inflected for number, and the inflection follows the recast: `better than any of the other women in the house` becomes `better than any other woman in the house`, which is singular because the comparison now has one term. Butler's own `A good old woman, Euryclea` stands. The `husbands` ruling at Book 7 exactly. |

