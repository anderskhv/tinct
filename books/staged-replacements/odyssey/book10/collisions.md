# Odyssey Book 10 — the collision report, dispositioned

Written by `scripts/collision_triage.py`. Every row of every arrow that touches
this Book, with a disposition on each. **Records finding R-2 and blind spot 8**
of `book07/review/findings-v1.md`: the check returned seventy-two rows touching
Book 7, eleven were acted on, and sixty-one were dismissed with no record of
having been read. This file is that record, and it is generated, not typed.

**124 rows touch Book 10.**

| disposition | rows |
|---|---|
| `common-rendering` | 16 |
| `common-word` | 8 |
| `kept` | 64 |
| `kept-elsewhere` | 18 |
| `phrase-not-word` | 1 |
| `unavoidable-merge` | 1 |
| `variant` | 16 |

The vocabulary is defined in the script's docstring. `homograph`,
`same-referent`, `unavoidable-merge` and `phrase-not-word` are new here;
`homograph` is the one the review asked for by name.

## ARROW A — one Butler word, two or more renderings, across Books

33 rows.

| key | this Book's entries | disposition | reason |
|---|---|---|---|
| `apart` | `aside` (B10-P039) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `apart` → `aside`, and it is the thing to rule on.** Every other entry is Butler's own `apart` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `aside` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `apart` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `asking` | `asking` (B10-P002) | **`kept`** | every entry Book 10 contributes is Butler's own `asking` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `assented` | `assented` (B10-P039), `assented` (B10-P040) | **`kept`** | every entry Book 10 contributes is Butler's own `assented` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `baskets` | `baskets` (B10-P029) | **`kept`** | every entry Book 10 contributes is Butler's own `baskets` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `besought` | `entreated` (B10-P040), `entreated` (B10-P045) | **`variant`** | one Butler word with ONE rendering after the draft repair — `entreated` at both P040 and P045. The row is printed because arrow A lists every rendering of a rare Butler word across Books, not because the two disagree; they agree, which is the state the arrow exists to produce. |
| `bustling` | `bustling` (B10-P047) | **`kept`** | every entry Book 10 contributes is Butler's own `bustling` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `certain` | `young man` (B10-P047) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `certain` → `young man`, and it is the thing to rule on.** Every other entry is Butler's own `certain` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `young man` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `certain` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `consent` | `agree` (B10-P027) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `consent` → `agree`, and it is the thing to rule on.** Every other entry is Butler's own `consent` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `agree` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `consent` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `counsels` | `counsels` (B10-P004) | **`kept`** | every entry Book 10 contributes is Butler's own `counsels` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `desire` | `ask` (B10-P023) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `desire` → `ask`, and it is the thing to rule on.** Every other entry is Butler's own `desire` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `ask` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `desire` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `doubted` | `was uncertain` (B10-P012) | **`variant`** | Butler's `I doubted whether ... I would not go on` is a double negative in a construction modern English has lost; `I was uncertain whether to go on` is the same hesitation without it. `uncertain` is free in this Book and `in two minds` is NOT — it is Butler's own at P036, which is why the draft's first rendering was repaired. |
| `enter` | `to come in` (B10-P017) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `enter` → `to come in`, and it is the thing to rule on.** Every other entry is Butler's own `enter` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `to come in` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `enter` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `girdle` | `girdle` (B10-P046) | **`kept`** | every entry Book 10 contributes is Butler's own `girdle` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `guided` | `guided` (B10-P012) | **`kept`** | every entry Book 10 contributes is Butler's own `guided` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `immediately` | `immediately` (B10-P005) | **`kept`** | every entry Book 10 contributes is Butler's own `immediately` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `lighted` | `lighted` (B10-P029) | **`kept`** | every entry Book 10 contributes is Butler's own `lighted` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `liquor` | `drink` (B10-P047) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `liquor` → `drink`, and it is the thing to rule on.** Every other entry is Butler's own `liquor` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `drink` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `liquor` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `matters` | `matters` (B10-P049) | **`kept`** | every entry Book 10 contributes is Butler's own `matters` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `month` | `month` (B10-P002) | **`kept`** | every entry Book 10 contributes is Butler's own `month` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `neighbour` | `neighbor` (B10-P003) | **`variant`** | `neighbour` -> `neighbor` is the edition's spelling normalization (D15's class: it does not change what a reader says aloud), not a rendering decision. The same row will open for every -our/-or word in every Book. |
| `onward` | `onward` (B10-P025) | **`kept`** | every entry Book 10 contributes is Butler's own `onward` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `pulled` | `pulled` (B10-P024) | **`variant`** | every rendering in the row reduces to `pull` once inflection, D9 spelling and D15 compound spacing are normalized |
| `salute` | `greeting` (B10-P008) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `salute` → `greeting`, and it is the thing to rule on.** Every other entry is Butler's own `salute` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `greeting` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `salute` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `senses` | `minds` (B10-P017) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `senses` → `minds`, and it is the thing to rule on.** Every other entry is Butler's own `senses` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `minds` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `senses` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `signs` | `make out any sign` (B10-P012) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `signs` → `make out any sign`, and it is the thing to rule on.** Every other entry is Butler's own `signs` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `make out any sign` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `signs` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `storm` | `storm` (B10-P004) | **`variant`** | every rendering in the row reduces to `storm` once inflection, D9 spelling and D15 compound spacing are normalized |
| `sudden` | `suddenly` (B10-P047) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `sudden` → `suddenly`, and it is the thing to rule on.** Every other entry is Butler's own `sudden` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `suddenly` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `sudden` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `sufficiently` | `long enough` (B10-P013) | **`variant`** | `when they had feasted their eyes upon him sufficiently` -> `long enough`. `sufficiently` survives in modern English only in formal and technical registers; the sentence is a crew looking at a dead stag. |
| `thousands` | `thousands` (B10-P011) | **`variant`** | every rendering in the row reduces to `thousand` once inflection, D9 spelling and D15 compound spacing are normalized |
| `underneath` | `under` (B10-P029) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `underneath` → `under`, and it is the thing to rule on.** Every other entry is Butler's own `underneath` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `under` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `underneath` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `upwards` | `up out of` (B10-P012) | **`variant`** | `the smoke of Circe's house rising upwards amid a dense forest` -> `rising up out of a dense forest`. `upwards` after `rising` is pleonastic in modern English, and `amid` is the word that had to move: the smoke rises OUT of the forest, not among it. |
| `wagged` | `wagged` (B10-P016) | **`kept`** | every entry Book 10 contributes is Butler's own `wagged` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `youth` | `young man` (B10-P047), `youth` (B10-P022) | **`variant`** | TWO SENSES, and this is the one place in the Book where one Butler word must have two renderings. `a young man in the hey-day of his youth` (P022) is the abstract noun and is kept as `youth`; `a certain youth named Elpenor` (P047) is a person, and modern English does not call a person a youth without sounding like a police report. `young man` there is not a flattening: nothing else in the Book renders as `young man`, and the two Butler occurrences are two words that happen to be spelled alike. |

## ARROW B — one rendering, two or more Butler words, across Books

83 rows.

| key | this Book's entries | disposition | reason |
|---|---|---|---|
| `about` | `about` (B10-P002), `about` (B10-P011), `about` (B10-P022), `about` (B10-P029), `about` (B10-P032), `about` (B10-P033), `about` (B10-P039), `about` (B10-P040), `about` (B10-P041), `about` (B10-P042), `about` (B10-P045), `about` (B10-P046), `about` (B10-P047), `about` (B10-P048), `reconnoitre` (B10-P009), `reconnoitre` (B10-P012) | **`common-rendering`** | `about` is a word Butler himself uses in 126 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `after` | `after` (B10-P005), `after` (B10-P011), `after` (B10-P013), `after` (B10-P033), `after` (B10-P037), `after` (B10-P040) | **`kept`** | every entry Book 10 contributes is Butler's own `after` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `agreed` | `agreed` (B10-P033) | **`kept`** | every entry Book 10 contributes is Butler's own `agreed` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `anyone` | `conceivable kind` (B10-P001) | **`common-rendering`** | `every conceivable kind of luxury` -> `every luxury anyone could think of`. `anyone` is a function word, not a rendering: it carries no sense of Butler's that another word carries, and arrow B is keyed on the candidate's side without a stop-word filter on purpose. |
| `aside` | `apart` (B10-P039) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `aside` → `apart`, and it is the thing to rule on.** Every other entry is Butler's own `aside` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `apart` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `aside` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `before` | `before` (B10-P013), `before` (B10-P017), `before` (B10-P032), `before` (B10-P041), `before` (B10-P045), `untimely here` (B10-P043) | **`common-rendering`** | `before` is a word Butler himself uses in 65 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `being` | `being` (B10-P003), `being` (B10-P036) | **`kept`** | every entry Book 10 contributes is Butler's own `being` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `beside` | `beside` (B10-P029) | **`kept`** | every entry Book 10 contributes is Butler's own `beside` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `blessed` | `blessed` (B10-P023) | **`kept`** | every entry Book 10 contributes is Butler's own `blessed` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `brings` | `brings` (B10-P005) | **`kept`** | every entry Book 10 contributes is Butler's own `brings` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `called` | `called` (B10-P011), `called` (B10-P013), `called` (B10-P014), `called` (B10-P017), `called` (B10-P019), `called` (B10-P025), `called` (B10-P039) | **`kept`** | every entry Book 10 contributes is Butler's own `called` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `carried` | `carried` (B10-P004) | **`kept`** | every entry Book 10 contributes is Butler's own `carried` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `caught` | `espied` (B10-P012) | **`common-rendering`** | `caught` is a word Butler himself uses in 15 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `children` | `children` (B10-P012), `children` (B10-P029) | **`kept`** | every entry Book 10 contributes is Butler's own `children` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `clothed` | `arrayed` (B10-P029) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `clothed` → `arrayed`, and it is the thing to rule on.** Every other entry is Butler's own `clothed` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `arrayed` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `clothed` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `command` | `command` (B10-P015) | **`kept`** | every entry Book 10 contributes is Butler's own `command` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `companies` | `companies` (B10-P015) | **`kept`** | every entry Book 10 contributes is Butler's own `companies` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `company` | `company` (B10-P009), `company` (B10-P015) | **`kept`** | every entry Book 10 contributes is Butler's own `company` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `could` | `conceivable kind` (B10-P001), `could` (B10-P002), `could` (B10-P003), `could` (B10-P007), `could` (B10-P008), `could` (B10-P009), `could` (B10-P011), `could` (B10-P012), `could` (B10-P014), `could` (B10-P016), `could` (B10-P018), `could` (B10-P019) | **`common-rendering`** | `could` is a word Butler himself uses in 78 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `crossed` | `traversed` (B10-P043) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `crossed` → `traversed`, and it is the thing to rule on.** Every other entry is Butler's own `crossed` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `traversed` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `crossed` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `drink` | `drink` (B10-P013), `drink` (B10-P023), `drink` (B10-P025), `drink` (B10-P030), `drink` (B10-P031), `drink` (B10-P038), `drink` (B10-P044), `liquor` (B10-P047) | **`common-rendering`** | `drink` is a word Butler himself uses in 44 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `eating` | `eating` (B10-P014), `eating` (B10-P021), `eating` (B10-P030), `eating` (B10-P031), `eating` (B10-P034) | **`kept`** | every entry Book 10 contributes is Butler's own `eating` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `enchanting` | `charming` (B10-P025) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `enchanting` → `charming`, and it is the thing to rule on.** Every other entry is Butler's own `enchanting` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `charming` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `enchanting` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `enough` | `enough` (B10-P011), `sufficiently` (B10-P013) | **`common-rendering`** | `enough` is a word Butler himself uses in 25 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `feeding` | `feeding` (B10-P033) | **`kept`** | every entry Book 10 contributes is Butler's own `feeding` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `first` | `first` (B10-P027), `first` (B10-P032), `first` (B10-P044) | **`kept`** | every entry Book 10 contributes is Butler's own `first` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `going` | `going` (B10-P010), `going` (B10-P012), `going` (B10-P013), `going` (B10-P014), `going` (B10-P022), `going` (B10-P023), `going` (B10-P035), `going` (B10-P039), `going` (B10-P040), `going` (B10-P046) | **`kept`** | every entry Book 10 contributes is Butler's own `going` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `great` | `great` (B10-P005), `great` (B10-P012), `great` (B10-P014), `great` (B10-P016), `great` (B10-P022), `great` (B10-P030), `great` (B10-P034) | **`kept`** | every entry Book 10 contributes is Butler's own `great` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `greeting` | `salute` (B10-P008) | **`common-rendering`** | Butler's `salutes` / `salute` in the Laestrygonian shepherds' passage (P008), rendered `greets` / `greeting`. One Butler word, one rendering, twice; `salute` in modern English is a military gesture and the passage is two shepherds passing on a road. |
| `guide` | `guide` (B10-P042), `guide` (B10-P043) | **`kept`** | every entry Book 10 contributes is Butler's own `guide` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `heads` | `heads` (B10-P013), `heads` (B10-P045) | **`kept`** | every entry Book 10 contributes is Butler's own `heads` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `helping` | `furthering` (B10-P040) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `helping` → `furthering`, and it is the thing to rule on.** Every other entry is Butler's own `helping` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `furthering` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `helping` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `herald` | `herald` (B10-P005) | **`kept`** | every entry Book 10 contributes is Butler's own `herald` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `homeward` | `homeward` (B10-P040), `homewards` (B10-P041) | **`variant`** | every rendering in the row reduces to `homeward` once inflection, D9 spelling and D15 compound spacing are normalized |
| `hurried` | `hurried` (B10-P018) | **`kept`** | every entry Book 10 contributes is Butler's own `hurried` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `husband` | `husband` (B10-P011) | **`kept`** | every entry Book 10 contributes is Butler's own `husband` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `leave` | `leave` (B10-P038) | **`kept`** | every entry Book 10 contributes is Butler's own `leave` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `listened` | `listened` (B10-P040) | **`kept`** | every entry Book 10 contributes is Butler's own `listened` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `lived` | `lived` (B10-P042), `the inhabitants were` (B10-P009) | **`common-rendering`** | `lived` is a word Butler himself uses in 9 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `lives` | `lives` (B10-P011), `lives` (B10-P012), `lives` (B10-P020), `lives` (B10-P035) | **`kept`** | every entry Book 10 contributes is Butler's own `lives` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `loaded` | `loaded` (B10-P001) | **`kept`** | every entry Book 10 contributes is Butler's own `loaded` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `lying` | `lying` (B10-P047) | **`kept`** | every entry Book 10 contributes is Butler's own `lying` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `meanwhile` | `meanwhile` (B10-P029), `meanwhile` (B10-P038) | **`kept`** | every entry Book 10 contributes is Butler's own `meanwhile` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `minds` | `minds` (B10-P036), `senses` (B10-P017) | **`common-rendering`** | `minds` is a word Butler himself uses in 11 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `myself` | `myself` (B10-P004), `myself` (B10-P012), `myself` (B10-P015), `myself` (B10-P027), `myself` (B10-P042) | **`kept`** | every entry Book 10 contributes is Butler's own `myself` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `neighbor` | `neighbour saying` (B10-P003) | **`variant`** | D9, `neighbour` -> `neighbor`. The trailing `saying` in the row is Butler's participle, which the recast `and one would turn to his neighbor and say` absorbed; the diff attached it to the same span. |
| `onward` | `onward` (B10-P025) | **`variant`** | every rendering in the row reduces to `onward` once inflection, D9 spelling and D15 compound spacing are normalized |
| `opened` | `opened` (B10-P019), `opened` (B10-P025), `opened` (B10-P032) | **`variant`** | every rendering in the row reduces to `open` once inflection, D9 spelling and D15 compound spacing are normalized |
| `others` | `others` (B10-P011), `others` (B10-P018), `others` (B10-P019) | **`kept`** | every entry Book 10 contributes is Butler's own `others` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `people` | `people` (B10-P001), `people` (B10-P009), `people` (B10-P010), `people` (B10-P026) | **`kept`** | every entry Book 10 contributes is Butler's own `people` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `pigsties` | `pig styes` (B10-P017), `pigstyes` (B10-P022) | **`variant`** | every rendering in the row reduces to `pigsty` once inflection, D9 spelling and D15 compound spacing are normalized |
| `promise` | `promise` (B10-P040), `promise` (B10-P044) | **`kept`** | every entry Book 10 contributes is Butler's own `promise` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `property` | `property` (B10-P032), `property` (B10-P034) | **`kept`** | every entry Book 10 contributes is Butler's own `property` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `quick` | `quick` (B10-P003) | **`kept`** | every entry Book 10 contributes is Butler's own `quick` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `raise` | `raise` (B10-P043) | **`kept`** | every entry Book 10 contributes is Butler's own `raise` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `sacrifice` | `sacrifice` (B10-P044), `sacrifice` (B10-P045) | **`kept`** | every entry Book 10 contributes is Butler's own `sacrifice` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `servant` | `servant` (B10-P029) | **`kept`** | every entry Book 10 contributes is Butler's own `servant` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `showed` | `shewed` (B10-P024), `showed` (B10-P003) | **`variant`** | FALSE POSITIVE of the same kind as Book 9's `answer` row. `shewed` (P024) and `showed` (P003) are not two Butler words: `shewed` is Butler's own archaic spelling of `showed`, and the edition normalizes the spelling under D15. One word, one rendering. |
| `sight` | `espied` (B10-P012), `sight` (B10-P010) | **`common-rendering`** | `sight` is a word Butler himself uses in 11 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `sitting` | `sitting` (B10-P030) | **`kept`** | every entry Book 10 contributes is Butler's own `sitting` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `slung` | `slung` (B10-P020) | **`kept`** | every entry Book 10 contributes is Butler's own `slung` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `smell` | `savour` (B10-P001) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `smell` → `savour`, and it is the thing to rule on.** Every other entry is Butler's own `smell` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `savour` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `smell` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `something` | `something` (B10-P013), `something` (B10-P016) | **`kept`** | every entry Book 10 contributes is Butler's own `something` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `start` | `start` (B10-P048) | **`kept`** | every entry Book 10 contributes is Butler's own `start` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `suddenly` | `on a sudden` (B10-P047) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 10's `suddenly` → `on a sudden`, and it is the thing to rule on.** Every other entry is Butler's own `suddenly` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `on a sudden` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `suddenly` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `talked` | `talked` (B10-P004) | **`unavoidable-merge`** | `talked` carries Butler's `converse` (B04-P052, B07-P028) and his own `talked` (B02-P025). `converse` as an intransitive verb is archaic and `talk` is its only plain modern equivalent; the alternative is a register difference Butler does not make. B07-P028 matches accepted B04-P052 word for word, which is the consistency the package asks for. |
| `their` | `their` (B10-P001), `their` (B10-P007), `their` (B10-P009), `their` (B10-P010), `their` (B10-P011), `their` (B10-P012), `their` (B10-P013), `their` (B10-P015), `their` (B10-P016), `their` (B10-P017), `their` (B10-P022), `their` (B10-P029), `their` (B10-P032), `their` (B10-P033), `their` (B10-P035), `their` (B10-P040), `their` (B10-P043), `their` (B10-P045), `their` (B10-P049) | **`kept`** | every entry Book 10 contributes is Butler's own `their` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `there` | `the inhabitants were` (B10-P009), `there` (B10-P003), `there` (B10-P008), `there` (B10-P009), `there` (B10-P010), `there` (B10-P011), `there` (B10-P012), `there` (B10-P014), `there` (B10-P015), `there` (B10-P016), `there` (B10-P019), `there` (B10-P022), `there` (B10-P025), `there` (B10-P029), `there` (B10-P030), `there` (B10-P041), `there` (B10-P043), `there` (B10-P045) | **`common-rendering`** | `there` is a word Butler himself uses in 128 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `think` | `conceivable kind` (B10-P001), `think` (B10-P031), `think` (B10-P039), `think` (B10-P048) | **`common-rendering`** | `think` is a word Butler himself uses in 28 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `those` | `those` (B10-P035) | **`kept`** | every entry Book 10 contributes is Butler's own `those` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `together` | `together` (B10-P013), `together` (B10-P048) | **`kept`** | every entry Book 10 contributes is Butler's own `together` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `treachery` | `treachery` (B10-P019) | **`kept`** | every entry Book 10 contributes is Butler's own `treachery` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `troubled` | `troubled` (B10-P025) | **`kept`** | every entry Book 10 contributes is Butler's own `troubled` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `under` | `under` (B10-P009), `under` (B10-P025), `under` (B10-P029), `underneath` (B10-P029) | **`common-rendering`** | `under` is a word Butler himself uses in 43 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `until` | `until` (B10-P031) | **`phrase-not-word`** | `hitherto` -> `until now` at B07-P018, beside Butler's own `until` elsewhere. `until now` is a fixed adverbial phrase, not a second use of the preposition, and there is no second referent. Upheld. |
| `water` | `water` (B10-P005), `water` (B10-P010), `water` (B10-P011), `water` (B10-P029), `water` (B10-P044) | **`kept`** | every entry Book 10 contributes is Butler's own `water` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `whatever` | `whatever` (B10-P003), `whatever` (B10-P024) | **`variant`** | every rendering in the row reduces to `whatever` once inflection, D9 spelling and D15 compound spacing are normalized |
| `where` | `where` (B10-P001), `where` (B10-P005), `where` (B10-P008), `where` (B10-P012), `where` (B10-P014), `where` (B10-P019), `where` (B10-P021), `where` (B10-P032), `where` (B10-P033), `where` (B10-P034), `where` (B10-P043), `whither` (B10-P022) | **`common-rendering`** | `where` is a word Butler himself uses in 77 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `which` | `which` (B10-P002), `which` (B10-P010), `which` (B10-P013), `which` (B10-P022), `which` (B10-P043) | **`kept`** | every entry Book 10 contributes is Butler's own `which` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `without` | `without` (B10-P008), `without` (B10-P012), `without` (B10-P022), `without` (B10-P025), `without` (B10-P029), `without` (B10-P030), `without` (B10-P047), `without` (B10-P049) | **`kept`** | every entry Book 10 contributes is Butler's own `without` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `woman` | `woman` (B10-P010), `woman` (B10-P016), `woman` (B10-P019) | **`kept`** | every entry Book 10 contributes is Butler's own `woman` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `working` | `working` (B10-P016), `working` (B10-P019), `working` (B10-P023) | **`kept`** | every entry Book 10 contributes is Butler's own `working` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `young` | `certain youth` (B10-P047), `young` (B10-P010), `young` (B10-P022) | **`common-rendering`** | `young` is a word Butler himself uses in 35 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |

## ARROW C — one paragraph, one rendering ← a word Butler keeps in that same paragraph

8 rows.

| key | this Book's entries | disposition | reason |
|---|---|---|---|
| `out` | `forth` (B10-P004) | **`common-word`** | `out` is an ordinary word Butler uses in 106 paragraphs and the two uses share no referent |
| `while` | `whereon` (B10-P011) | **`common-word`** | `while` is an ordinary word Butler uses in 76 paragraphs and the two uses share no referent |
| `out` | `discover signs` (B10-P012) | **`common-word`** | `out` is an ordinary word Butler uses in 106 paragraphs and the two uses share no referent |
| `from` | `thence` (B10-P012) | **`common-word`** | `from` is an ordinary word Butler uses in 170 paragraphs and the two uses share no referent |
| `out` | `upwards amid` (B10-P012) | **`common-word`** | `out` is an ordinary word Butler uses in 106 paragraphs and the two uses share no referent |
| `long` | `sufficiently` (B10-P013) | **`common-word`** | `long` is an ordinary word Butler uses in 52 paragraphs and the two uses share no referent |
| `took` | `laid` (B10-P020) | **`common-word`** | `took` is an ordinary word Butler uses in 74 paragraphs and the two uses share no referent |
| `under` | `underneath` (B10-P029) | **`common-word`** | `under` is an ordinary word Butler uses in 43 paragraphs and the two uses share no referent |

