# Odyssey Book 9 — the collision report, dispositioned

Written by `scripts/collision_triage.py`. Every row of every arrow that touches
this Book, with a disposition on each. **Records finding R-2 and blind spot 8**
of `book07/review/findings-v1.md`: the check returned seventy-two rows touching
Book 7, eleven were acted on, and sixty-one were dismissed with no record of
having been read. This file is that record, and it is generated, not typed.

**123 rows touch Book 9.**

| disposition | rows |
|---|---|
| `artifact` | 2 |
| `common-rendering` | 9 |
| `common-word` | 9 |
| `context-rendered` | 2 |
| `discrimination` | 1 |
| `kept` | 71 |
| `kept-elsewhere` | 15 |
| `unavoidable-merge` | 1 |
| `variant` | 13 |

The vocabulary is defined in the script's docstring. `homograph`,
`same-referent`, `unavoidable-merge` and `phrase-not-word` are new here;
`homograph` is the one the review asked for by name.

## ARROW A — one Butler word, two or more renderings, across Books

38 rows.

| key | this Book's entries | disposition | reason |
|---|---|---|---|
| `apart` | `apart` (B9-P013) | **`kept`** | every entry Book 9 contributes is Butler's own `apart` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `blinded` | `blinded` (B9-P033) | **`kept`** | every entry Book 9 contributes is Butler's own `blinded` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `bloom` | `blossom` (B9-P003) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `bloom` → `blossom`, and it is the thing to rule on.** Every other entry is Butler's own `bloom` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `blossom` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `bloom` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `compassion` | `pity on` (B9-P022) | **`common-rendering`** | `take compassion upon me` is an archaic construction, not an archaic word: modern English keeps `compassion` as a noun and has lost `take compassion upon`. `take pity on me` is the living form of the same request and is what the sentence is doing. |
| `continue` | `go on` (B9-P001) | **`variant`** | `nor yet how to continue and conclude my tale` -> `nor how to go on and end my tale`. Butler's `continue` is current English; the pair with `conclude` is the Victorian doublet, and `go on and end` is the plain modern form of the same two verbs. No discrimination of Butler's is lost — he uses the word once in the Book. |
| `direct` | `direct` (B9-P004) | **`kept`** | every entry Book 9 contributes is Butler's own `direct` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `dozen` | `six` (B9-P003) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `dozen` → `six`, and it is the thing to rule on.** Every other entry is Butler's own `dozen` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `six` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `dozen` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `entered` | `came in` (B9-P008) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `entered` → `came in`, and it is the thing to rule on.** Every other entry is Butler's own `entered` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `came in` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `entered` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `firmament` | `firmament` (B9-P041) | **`kept`** | every entry Book 9 contributes is Butler's own `firmament` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `hence` | `on from there` (B9-P006) | **`variant`** | `We sailed hence` -> `We sailed on from there`. `hence` in the spatial sense is gone from modern English; the temporal and inferential senses that survive are not what Butler means here. |
| `hosts` | `two sides` (B9-P003) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `hosts` → `two sides`, and it is the thing to rule on.** Every other entry is Butler's own `hosts` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `two sides` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `hosts` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `humour` | `mood` (B9-P017) | **`variant`** | `unless I am in the humour for doing so` -> `unless I am in the mood for it`. Butler's sense of `humour` is the obsolete one; the modern word means something else, which makes this a false friend rather than a formal word. |
| `inwardly` | `to myself` (B9-P030) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `inwardly` → `to myself`, and it is the thing to rule on.** Every other entry is Butler's own `inwardly` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `to myself` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `inwardly` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `liquor` | `liquor` (B9-P022) | **`kept`** | every entry Book 9 contributes is Butler's own `liquor` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `loose` | `loose` (B9-P011), `loose` (B9-P044), `unyoke` (B9-P003) | **`context-rendered`** | Two senses, and each rendering is wrong in the other's place. `when men loose their oxen` (B09-P003) is unyoking at the day's end — the Homeric time-of-day formula — and `loose the hawsers` (B09-P011, B09-P044) is casting off, where `loose` is still the nautical word and is KEPT. `unyoke the hawsers` and `loose their oxen` are both wrong. |
| `lotus` | `it` (B9-P005), `lotus` (B9-P005) | **`common-rendering`** | `gave them to eat of the lotus, which was so delicious` -> `gave them the lotus to eat. It was so delicious`. The second mention becomes a pronoun because the sentence was divided; the word is kept where Butler first writes it and three times after. |
| `luscious` | `luscious` (B9-P007) | **`kept`** | every entry Book 9 contributes is Butler's own `luscious` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `numbers` | `many` (B9-P003), `numbers` (B9-P007) | **`context-rendered`** | `great numbers of sheep and oxen` -> `a great many sheep and oxen` (B09-P003), against `breed there in great numbers` KEPT (B09-P007). The first counts a slaughter and takes the plain quantifier; the second is the idiom for an animal population and has no plainer form. |
| `onward` | `on` (B9-P004) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `onward` → `on`, and it is the thing to rule on.** Every other entry is Butler's own `onward` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `on` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `onward` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `otherwise` | `to` (B9-P016) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `otherwise` → `to`, and it is the thing to rule on.** Every other entry is Butler's own `otherwise` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `to` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `otherwise` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `precincts` | `precincts` (B9-P012) | **`discrimination`** | **Ruled at Book 8, and the two renderings are deliberate.** B07-P012 is a man crossing a threshold into an enclosure, and there `precincts` was rendered `courtyard` for two reasons (finding M-1): `walls` was a sense change, and the Book's walls are literally bronze. B08-P004 is the plural grounds of a palace — *the outbuildings, the yards, and all the precincts were filled with crowds* — where `precincts` is current English and needs no change. Recorded in `book08/continuity.md` §6. |
| `preventing` | `preventing` (B9-P027) | **`kept`** | every entry Book 9 contributes is Butler's own `preventing` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `refrain` | `keep` (B9-P012) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `refrain` → `keep`, and it is the thing to rule on.** Every other entry is Butler's own `refrain` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `keep` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `refrain` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `remained` | `remained` (B9-P032) | **`artifact`** | Butler's `who as long as he remained with her had taken as good care of him` became `who for as long as he was with her` — a phrase-level recast made to keep `stayed` free for Butler's own `staid`/`stayed` (B03-P015, B04-P009, B07-P022). The diff paired `remained` with `was`; there is no rendering pair to rule on. |
| `renown` | `renown` (B9-P016) | **`kept`** | every entry Book 9 contributes is Butler's own `renown` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `respectable` | `respectable` (B9-P016) | **`kept`** | every entry Book 9 contributes is Butler's own `respectable` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `rovers` | `rovers` (B9-P015) | **`kept`** | every entry Book 9 contributes is Butler's own `rovers` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `scheme` | `scheme` (B9-P020) | **`kept`** | every entry Book 9 contributes is Butler's own `scheme` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `senses` | `senses` (B9-P016) | **`kept`** | every entry Book 9 contributes is Butler's own `senses` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `signs` | `signs` (B9-P034), `signs` (B9-P036) | **`kept`** | every entry Book 9 contributes is Butler's own `signs` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `sudden` | `sudden` (B9-P019) | **`kept`** | every entry Book 9 contributes is Butler's own `sudden` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `suffer` | `suffer` (B9-P007) | **`kept`** | every entry Book 9 contributes is Butler's own `suffer` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `uncivilised` | `wild` (B9-P010) | **`variant`** | `uncivilised savages` -> `wild savages`. Butler's pair is tautologous in modern English — a savage is by definition uncivilized — and the contrast the sentence draws is with `a hospitable and civilized race`, which KEEPS the root. One word of the pair carries it. |
| `underneath` | `underneath` (B9-P032) | **`kept`** | every entry Book 9 contributes is Butler's own `underneath` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `upwards` | `upward` (B9-P026), `upward` (B9-P031) | **`variant`** | Book 9 renders Butler's `face upwards` as `face upward` — the adverb without its -s, which is the edition's standing form — and Book 10 drops `rising upwards` to `rising up out of`, where the -s form is pleonastic after `rising`. Same word, two positions, no flattening. |
| `value` | `value` (B9-P012) | **`kept`** | every entry Book 9 contributes is Butler's own `value` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `vouchsafed` | `did not answer` (B9-P019) | **`variant`** | `vouchsafed me not one word of answer` -> `gave me not one word of answer`. `vouchsafe` survives only in legal and liturgical registers; the sentence's force is in `not one word`, which is kept exactly. |
| `wrath` | `wrath` (B9-P016) | **`kept`** | every entry Book 9 contributes is Butler's own `wrath` carried through unchanged; the decision in this row belongs to whichever Book moved |

## ARROW B — one rendering, two or more Butler words, across Books

76 rows.

| key | this Book's entries | disposition | reason |
|---|---|---|---|
| `about` | `about` (B9-P002), `about` (B9-P005), `about` (B9-P012), `about` (B9-P017), `about` (B9-P021), `about` (B9-P026), `about` (B9-P030), `about` (B9-P032), `about` (B9-P039) | **`kept`** | every entry Book 9 contributes is Butler's own `about` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `account` | `account` (B9-P006) | **`kept`** | every entry Book 9 contributes is Butler's own `account` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `after` | `after` (B9-P020), `after` (B9-P041) | **`kept`** | every entry Book 9 contributes is Butler's own `after` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `agreed` | `agreed` (B9-P043) | **`kept`** | every entry Book 9 contributes is Butler's own `agreed` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `anger` | `anger` (B9-P017) | **`kept`** | every entry Book 9 contributes is Butler's own `anger` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `answer` | `answer` (B9-P017), `vouchsafed` (B9-P019) | **`variant`** | FALSE POSITIVE of arrow B's keying. Butler writes `answer` in both paragraphs — `a pitiless answer` at P017 and `not one word of answer` at P019 — so the candidate's `answer` at P019 renders HIS noun, not his `vouchsafed`; the recast turns the noun into the verb it was already the object of. The row exists because the arrow keys on the rarest word in the aligned span and `vouchsafed` is rarer than `answer`. |
| `anyone` | `anyone` (B9-P030) | **`kept`** | every entry Book 9 contributes is Butler's own `anyone` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `apart` | `apart` (B9-P013) | **`kept`** | every entry Book 9 contributes is Butler's own `apart` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `arrows` | `arrows` (B9-P009), `arrows` (B9-P020) | **`kept`** | every entry Book 9 contributes is Butler's own `arrows` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `aside` | `aside` (B9-P014) | **`kept`** | every entry Book 9 contributes is Butler's own `aside` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `before` | `before` (B9-P004), `before` (B9-P008), `before` (B9-P025), `before` (B9-P033), `before` (B9-P035), `before` (B9-P036) | **`kept`** | every entry Book 9 contributes is Butler's own `before` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `begged` | `begged` (B9-P013), `begged` (B9-P023), `begged` (B9-P036) | **`kept`** | every entry Book 9 contributes is Butler's own `begged` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `being` | `being` (B9-P011) | **`kept`** | every entry Book 9 contributes is Butler's own `being` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `besides` | `booty` (B9-P003) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `besides` → `booty`, and it is the thing to rule on.** Every other entry is Butler's own `besides` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `booty` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `besides` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `blessed` | `blessed` (B9-P017) | **`kept`** | every entry Book 9 contributes is Butler's own `blessed` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `blossom` | `bloom` (B9-P003) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `blossom` → `bloom`, and it is the thing to rule on.** Every other entry is Butler's own `blossom` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `bloom` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `blossom` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `called` | `called` (B9-P002), `called` (B9-P009), `called` (B9-P024), `thrice invoked` (B9-P004) | **`common-rendering`** | `called` is a word Butler himself uses in 23 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `carried` | `carried` (B9-P031), `carried` (B9-P036) | **`kept`** | every entry Book 9 contributes is Butler's own `carried` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `caught` | `caught` (B9-P014), `caught` (B9-P018), `caught` (B9-P031) | **`kept`** | every entry Book 9 contributes is Butler's own `caught` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `company` | `company` (B9-P005) | **`kept`** | every entry Book 9 contributes is Butler's own `company` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `could` | `could` (B9-P003), `could` (B9-P007), `could` (B9-P008), `could` (B9-P009), `could` (B9-P013), `could` (B9-P021), `could` (B9-P024), `could` (B9-P031), `could` (B9-P033), `could` (B9-P040) | **`kept`** | every entry Book 9 contributes is Butler's own `could` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `direction` | `direction` (B9-P034) | **`kept`** | every entry Book 9 contributes is Butler's own `direction` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `drink` | `drink` (B9-P014), `drink` (B9-P019), `drink` (B9-P022), `drink` (B9-P033), `drink` (B9-P039), `drink` (B9-P044) | **`kept`** | every entry Book 9 contributes is Butler's own `drink` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `eating` | `eating` (B9-P020), `eating` (B9-P022), `eating` (B9-P035) | **`kept`** | every entry Book 9 contributes is Butler's own `eating` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `enough` | `enough` (B9-P014), `enough` (B9-P030), `enough` (B9-P032), `enough` (B9-P037) | **`kept`** | every entry Book 9 contributes is Butler's own `enough` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `faint` | `faint` (B9-P026) | **`kept`** | every entry Book 9 contributes is Butler's own `faint` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `first` | `first` (B9-P003), `first` (B9-P013), `first` (B9-P019), `first` (B9-P033), `first` (B9-P034), `first` (B9-P042), `firstly` (B9-P002) | **`common-rendering`** | `first` is a word Butler himself uses in 40 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `forward` | `forward` (B9-P039) | **`variant`** | every rendering in the row reduces to `forward` once inflection, D9 spelling and D15 compound spacing are normalized |
| `going` | `going` (B9-P009), `going` (B9-P014), `going` (B9-P030), `going` (B9-P032), `going` (B9-P044) | **`kept`** | every entry Book 9 contributes is Butler's own `going` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `great` | `great` (B9-P003), `great` (B9-P007), `great` (B9-P011), `great` (B9-P012), `great` (B9-P016), `great` (B9-P021), `great` (B9-P022), `great` (B9-P026), `great` (B9-P031), `great` (B9-P032), `great` (B9-P039), `great` (B9-P041) | **`kept`** | every entry Book 9 contributes is Butler's own `great` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `heads` | `heads` (B9-P037) | **`kept`** | every entry Book 9 contributes is Butler's own `heads` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `housekeeper` | `housekeeper` (B9-P012) | **`variant`** | every rendering in the row reduces to `housekeeper` once inflection, D9 spelling and D15 compound spacing are normalized |
| `hurried` | `hurried` (B9-P032) | **`kept`** | every entry Book 9 contributes is Butler's own `hurried` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `keeping` | `keeping` (B9-P031) | **`kept`** | every entry Book 9 contributes is Butler's own `keeping` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `leave` | `leave` (B9-P004), `leave` (B9-P033) | **`kept`** | every entry Book 9 contributes is Butler's own `leave` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `lived` | `lived` (B9-P003), `lived` (B9-P012), `lived` (B9-P026) | **`kept`** | every entry Book 9 contributes is Butler's own `lived` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `lives` | `lives` (B9-P036), `lives` (B9-P038) | **`kept`** | every entry Book 9 contributes is Butler's own `lives` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `loaded` | `loaded` (B9-P001), `loaded` (B9-P013) | **`kept`** | every entry Book 9 contributes is Butler's own `loaded` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `lying` | `lying` (B9-P017), `lying` (B9-P021) | **`kept`** | every entry Book 9 contributes is Butler's own `lying` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `matter` | `matter` (B9-P026) | **`variant`** | every rendering in the row reduces to `matter` once inflection, D9 spelling and D15 compound spacing are normalized |
| `meaning` | `intending` (B9-P021) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `meaning` → `intending`, and it is the thing to rule on.** Every other entry is Butler's own `meaning` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `intending` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `meaning` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `meanwhile` | `meanwhile` (B9-P003), `meanwhile` (B9-P014) | **`kept`** | every entry Book 9 contributes is Butler's own `meanwhile` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `myself` | `inwardly` (B9-P030), `myself` (B9-P020), `myself` (B9-P021), `myself` (B9-P031) | **`common-rendering`** | `myself` is a word Butler himself uses in 37 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `onward` | `onwards` (B9-P042) | **`variant`** | every rendering in the row reduces to `onward` once inflection, D9 spelling and D15 compound spacing are normalized |
| `others` | `others` (B9-P002), `others` (B9-P014), `others` (B9-P031), `others` (B9-P034) | **`kept`** | every entry Book 9 contributes is Butler's own `others` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `ourselves` | `ourselves` (B9-P008), `ourselves` (B9-P009) | **`variant`** | every rendering in the row reduces to `ourselv` once inflection, D9 spelling and D15 compound spacing are normalized |
| `parts` | `parts` (B9-P012) | **`kept`** | every entry Book 9 contributes is Butler's own `parts` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `people` | `people` (B9-P001), `people` (B9-P003), `people` (B9-P005), `people` (B9-P006), `people` (B9-P007), `people` (B9-P010), `people` (B9-P011), `people` (B9-P016), `people` (B9-P022), `people` (B9-P029) | **`kept`** | every entry Book 9 contributes is Butler's own `people` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `persuade` | `persuade` (B9-P002) | **`kept`** | every entry Book 9 contributes is Butler's own `persuade` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `places` | `orderly` (B9-P001), `places` (B9-P004), `places` (B9-P005), `places` (B9-P007), `places` (B9-P011), `places` (B9-P034), `places` (B9-P044) | **`common-rendering`** | `places` is a word Butler himself uses in 13 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `prayer` | `prayer` (B9-P042) | **`kept`** | every entry Book 9 contributes is Butler's own `prayer` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `reaches` | `ascends` (B9-P002) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `reaches` → `ascends`, and it is the thing to rule on.** Every other entry is Butler's own `reaches` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `ascends` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `reaches` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `sacrifice` | `sacrifice` (B9-P014), `sacrifice` (B9-P043) | **`kept`** | every entry Book 9 contributes is Butler's own `sacrifice` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `settled` | `colonised` (B9-P007) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `settled` → `colonised`, and it is the thing to rule on.** Every other entry is Butler's own `settled` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `colonised` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `settled` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `sides` | `hosts` (B9-P003) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `sides` → `hosts`, and it is the thing to rule on.** Every other entry is Butler's own `sides` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `hosts` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `sides` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `sight` | `sight` (B9-P001), `sight` (B9-P014), `sight` (B9-P019), `sight` (B9-P039) | **`kept`** | every entry Book 9 contributes is Butler's own `sight` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `since` | `since` (B9-P001) | **`kept`** | every entry Book 9 contributes is Butler's own `since` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `sitting` | `sitting` (B9-P001) | **`kept`** | every entry Book 9 contributes is Butler's own `sitting` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `someone` | `expecting some one` (B9-P039) | **`artifact`** | `some one` -> `someone`, a typographic normalization of the kind D15 calls silent. Not a rendering at all. |
| `spears` | `spears` (B9-P003), `spears` (B9-P009) | **`kept`** | every entry Book 9 contributes is Butler's own `spears` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `strength` | `strength` (B9-P012), `strength` (B9-P026), `strength` (B9-P039) | **`kept`** | every entry Book 9 contributes is Butler's own `strength` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `telling` | `bidding` (B9-P011) | **`kept-elsewhere`** | **The one rendering decision in this row is Book 9's `telling` → `bidding`, and it is the thing to rule on.** Every other entry is Butler's own `telling` carried through unchanged by another Book, so there is no cross-Book inconsistency — which is a different question from whether `bidding` is the right word here, and this class cannot answer that one. **Declared blind, twice:** it cannot say whether a Book that KEPT `telling` should also have moved it (ruled by hand in `RULINGS_BY_BOOK` where the kept word is not current English in context — see `luscious` at Book 5), and it cannot say whether the single rendering is accurate to Butler's sense (Book 9 S-4: `humane` is *merciful*, and `civilized` is not). |
| `their` | `orderly` (B9-P001), `their` (B9-P003), `their` (B9-P005), `their` (B9-P006), `their` (B9-P007), `their` (B9-P009), `their` (B9-P011), `their` (B9-P017), `their` (B9-P019), `their` (B9-P026), `their` (B9-P032), `their` (B9-P034), `their` (B9-P036), `their` (B9-P044) | **`common-rendering`** | `their` is a word Butler himself uses in 124 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `there` | `hence` (B9-P006), `there` (B9-P001), `there` (B9-P002), `there` (B9-P003), `there` (B9-P004), `there` (B9-P007), `there` (B9-P008), `there` (B9-P011), `there` (B9-P029), `there` (B9-P031), `there` (B9-P039) | **`common-rendering`** | `there` is a word Butler himself uses in 128 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `think` | `think` (B9-P031) | **`kept`** | every entry Book 9 contributes is Butler's own `think` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `those` | `those` (B9-P003), `those` (B9-P005), `those` (B9-P034) | **`kept`** | every entry Book 9 contributes is Butler's own `those` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `three` | `three` (B9-P009), `three` (B9-P024), `three` (B9-P031), `thrice invoked` (B9-P004) | **`common-rendering`** | `three` is a word Butler himself uses in 17 paragraphs across seven Books, so its reuse as a rendering is not a decision; arrow B is ungated on this side on purpose and arrow C carries the residue |
| `times` | `thrice invoked` (B9-P004), `times` (B9-P024) | **`unavoidable-merge`** | `thrice invoked each one of the poor fellows` (B09-P004) and `three times did I fill the bowl` (B09-P024). **Modern English has no one-word `thrice`**, so both of Butler's expressions land on `three times`. The merge is forced by the language, not chosen: the alternatives (`three separate times`, `over and over`) either pad or change the count. |
| `together` | `together` (B9-P001), `together` (B9-P031) | **`kept`** | every entry Book 9 contributes is Butler's own `together` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `under` | `under` (B9-P005), `under` (B9-P016), `under` (B9-P021), `under` (B9-P031), `under` (B9-P034) | **`kept`** | every entry Book 9 contributes is Butler's own `under` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `water` | `water` (B9-P005), `water` (B9-P007), `water` (B9-P012), `water` (B9-P026) | **`kept`** | every entry Book 9 contributes is Butler's own `water` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `whatever` | `whatever` (B9-P008) | **`variant`** | every rendering in the row reduces to `whatever` once inflection, D9 spelling and D15 compound spacing are normalized |
| `where` | `where` (B9-P002), `where` (B9-P007), `where` (B9-P012), `where` (B9-P015), `where` (B9-P017), `where` (B9-P019), `where` (B9-P033), `where` (B9-P043) | **`kept`** | every entry Book 9 contributes is Butler's own `where` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `which` | `which` (B9-P003), `which` (B9-P009), `which` (B9-P011), `which` (B9-P012), `which` (B9-P021), `which` (B9-P026), `which` (B9-P031), `which` (B9-P034), `which` (B9-P037), `which` (B9-P039) | **`kept`** | every entry Book 9 contributes is Butler's own `which` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `without` | `without` (B9-P005), `without` (B9-P006), `without` (B9-P024) | **`kept`** | every entry Book 9 contributes is Butler's own `without` carried through unchanged; the decision in this row belongs to whichever Book moved |
| `young` | `young` (B9-P013), `young` (B9-P014), `young` (B9-P020), `young` (B9-P021) | **`kept`** | every entry Book 9 contributes is Butler's own `young` carried through unchanged; the decision in this row belongs to whichever Book moved |

## ARROW C — one paragraph, one rendering ← a word Butler keeps in that same paragraph

9 rows.

| key | this Book's entries | disposition | reason |
|---|---|---|---|
| `great` | `much` (B9-P003) | **`common-word`** | `great` is an ordinary word Butler uses in 73 paragraphs and the two uses share no referent |
| `from` | `thence` (B9-P003) | **`common-word`** | `from` is an ordinary word Butler uses in 170 paragraphs and the two uses share no referent |
| `down` | `sprang forth` (B9-P004) | **`common-word`** | `down` is an ordinary word Butler uses in 71 paragraphs and the two uses share no referent |
| `from` | `thence` (B9-P004) | **`common-word`** | `from` is an ordinary word Butler uses in 170 paragraphs and the two uses share no referent |
| `kind` | `manner` (B9-P005) | **`common-word`** | `kind` is an ordinary word Butler uses in 24 paragraphs and the two uses share no referent |
| `from` | `thence` (B9-P005) | **`common-word`** | `from` is an ordinary word Butler uses in 170 paragraphs and the two uses share no referent |
| `ships` | `these` (B9-P007) | **`common-word`** | `ships` is an ordinary word Butler uses in 30 paragraphs and the two uses share no referent |
| `down` | `with such a noise` (B9-P014) | **`common-word`** | `down` is an ordinary word Butler uses in 71 paragraphs and the two uses share no referent |
| `out` | `from` (B9-P026) | **`common-word`** | `out` is an ordinary word Butler uses in 106 paragraphs and the two uses share no referent |

