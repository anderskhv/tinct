# OS
.DS_Store

# macOS / iCloud / Dropbox sync-conflict duplicates
*\ 2.*
*\ 3.*
*\ copy.*
*\ copy

# Editor / generation backup files
*.bak
*backup*.json

# IDE
.vscode/
.idea/

# Dependencies
node_modules/
app/dist-*/

# Claude Code
.claude/

# Large binary/generated assets (audio is on R2 CDN, regeneratable from scripts)
models/
test.wav
app/public/audio/
app/audio/
app/audio-backup/
app/public-audio-backup/
app/tts/audio/
app/src/data/editions/

# Local dev tooling — TTS pipelines, raw book sources, audio voice tests.
# All regeneratable; heavy; not used by the deployed app.
app/tts/
books/raw/
books/tts-test/
books/*.log
books/logs/

# Local design handoffs / nested scratch checkouts. Move anything meant to
# become source into app/ or docs/ before committing it.
design_handoff_tinct_library/
tinct/

# Archived heavy binaries — kept locally for reference but never tracked
archive/build-artifacts/
